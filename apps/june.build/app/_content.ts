// AUTO-GENERATED by `june build` — edit content/**/*.md, not this file.
export type ContentEntry = { slug: string; data: Record<string, string | string[]>; body: string; original: string; html: string };
export const POSTS: ContentEntry[] = [
  {
    "slug": "2026-06-12-building-june-build-with-june",
    "data": {
      "title": "Building june.build with June",
      "date": "2026-06-12",
      "description": "Our site is its own demo — every page serves humans and agents from one definition.",
      "tags": [
        "dogfood",
        "dual-audience"
      ]
    },
    "body": "This site is built with June and deployed on Cloudflare Workers. That sentence\nis most of the pitch, so let's prove it instead of repeating it.\n\n## One route, four surfaces\n\nEvery page here is a single page module with projections:\n\n```\nGET /why            → streamed HTML (you, probably)\nGET /why.md         → clean markdown (an agent, probably)\nGET /blog.json      → structured frontmatter (a crawler with taste)\nPOST /mcp           → tools/call search_site, get_page (an agent at work)\n```\n\nThe markdown an agent fetches for this very post is **the file we authored,\nbyte for byte** — frontmatter included. Most frameworks reconstruct markdown\nfrom rendered HTML; June serves the source. There is nothing to drift.\n\n## Try it from your terminal\n\n```bash\ncurl https://june.build/blog/2026-06-12-building-june-build-with-june.md\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/call\",\"params\":{\"name\":\"search_site\",\"arguments\":{\"query\":\"cold start\"}}}'\n```\n\n## What the human side gets\n\nHover any link: the page prerenders in the background (Speculation Rules) and\nthe click is a zero-network activation with a view transition. No client\nJavaScript was written for any of this — the browser is the client runtime.\n",
    "original": "---\ntitle: Building june.build with June\ndate: 2026-06-12\ndescription: Our site is its own demo — every page serves humans and agents from one definition.\ntags: [dogfood, dual-audience]\n---\n\nThis site is built with June and deployed on Cloudflare Workers. That sentence\nis most of the pitch, so let's prove it instead of repeating it.\n\n## One route, four surfaces\n\nEvery page here is a single page module with projections:\n\n```\nGET /why            → streamed HTML (you, probably)\nGET /why.md         → clean markdown (an agent, probably)\nGET /blog.json      → structured frontmatter (a crawler with taste)\nPOST /mcp           → tools/call search_site, get_page (an agent at work)\n```\n\nThe markdown an agent fetches for this very post is **the file we authored,\nbyte for byte** — frontmatter included. Most frameworks reconstruct markdown\nfrom rendered HTML; June serves the source. There is nothing to drift.\n\n## Try it from your terminal\n\n```bash\ncurl https://june.build/blog/2026-06-12-building-june-build-with-june.md\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/call\",\"params\":{\"name\":\"search_site\",\"arguments\":{\"query\":\"cold start\"}}}'\n```\n\n## What the human side gets\n\nHover any link: the page prerenders in the background (Speculation Rules) and\nthe click is a zero-network activation with a view transition. No client\nJavaScript was written for any of this — the browser is the client runtime.\n",
    "html": "<p>This site is built with June and deployed on Cloudflare Workers. That sentence\nis most of the pitch, so let&#39;s prove it instead of repeating it.</p>\n<h2>One route, four surfaces</h2>\n<p>Every page here is a single page module with projections:</p>\n<pre><code>GET /why            → streamed HTML (you, probably)\nGET /why.md         → clean markdown (an agent, probably)\nGET /blog.json      → structured frontmatter (a crawler with taste)\nPOST /mcp           → tools/call search_site, get_page (an agent at work)\n</code></pre>\n<p>The markdown an agent fetches for this very post is <strong>the file we authored,\nbyte for byte</strong> — frontmatter included. Most frameworks reconstruct markdown\nfrom rendered HTML; June serves the source. There is nothing to drift.</p>\n<h2>Try it from your terminal</h2>\n<pre><code class=\"language-bash\">curl https://june.build/blog/2026-06-12-building-june-build-with-june.md\ncurl -X POST https://june.build/mcp \\\n  -H &#39;content-type: application/json&#39; \\\n  -d &#39;{&quot;jsonrpc&quot;:&quot;2.0&quot;,&quot;id&quot;:1,&quot;method&quot;:&quot;tools/call&quot;,&quot;params&quot;:{&quot;name&quot;:&quot;search_site&quot;,&quot;arguments&quot;:{&quot;query&quot;:&quot;cold start&quot;}}}&#39;\n</code></pre>\n<h2>What the human side gets</h2>\n<p>Hover any link: the page prerenders in the background (Speculation Rules) and\nthe click is a zero-network activation with a view transition. No client\nJavaScript was written for any of this — the browser is the client runtime.</p>\n"
  },
  {
    "slug": "2026-06-12-built-in-og-image",
    "data": {
      "title": "Built-in og:image: every page gets a live social card",
      "date": "2026-06-12",
      "description": "No pre-rendered files, no headless browser — /og/<slug>.png typesets each page's card in the worker, and dev previews the same pixels.",
      "tags": [
        "og-image",
        "fonts",
        "edge"
      ]
    },
    "body": "Share any page of this site and the social card you see was typeset **at\nrequest time**, in the worker. This post's own card:\n\n![The social card for this very post, rendered live](/og/2026-06-12-built-in-og-image.png)\n\n## Every page, one mechanism\n\n`/og/<slug>.png` resolves blog posts, docs pages, and the core pages alike —\neach HTML page points to its card with a plain `og:image` meta tag the route\ndeclares in its `metadata`. There is no image directory to regenerate and\nnothing to forget: a new post gets a card the moment it exists, because the\ncard *is* the post's title rendered through one JSX definition.\n\n## No browser anywhere\n\nsatori lays out the JSX (its own flexbox + font shaping), resvg — Rust\ncompiled to WASM — rasterizes it to PNG. The classic alternative is a\nheadless Chromium screenshotting a hidden page in a cron job; this is a\n50KB-class route instead.\n\nFonts load at runtime with Google Fonts' `text=` subsetting: only the glyphs\nthe title actually uses, a few dozen KB, cached at the edge. That is also\nwhat makes CJK titles viable — the full story is in\n[Typesetting CJK at the edge](/blog/2026-06-10-typesetting-cjk-at-the-edge).\n\n## Dev shows the real card\n\nThe same card definition renders on both hosts: workers-og rasterizes it on\nworkerd; on the dev host the identical JSX goes through satori plus a native\nresvg binding. `localhost:3000/og/anything.png` during development IS the\ncard that ships — no \"works on prod, blank in dev\" class of surprise.\n\n## Try it\n\n```bash\ncurl -o card.png https://june.build/og/why.png            # a core page\ncurl -o card.png https://june.build/og/features-mcp.png   # a docs page\ncurl -o card.png https://june.build/og/$(date +%s).png    # unknown slug → site card\n```\n",
    "original": "---\ntitle: \"Built-in og:image: every page gets a live social card\"\ndate: 2026-06-12\ndescription: No pre-rendered files, no headless browser — /og/<slug>.png typesets each page's card in the worker, and dev previews the same pixels.\ntags: [og-image, fonts, edge]\n---\n\nShare any page of this site and the social card you see was typeset **at\nrequest time**, in the worker. This post's own card:\n\n![The social card for this very post, rendered live](/og/2026-06-12-built-in-og-image.png)\n\n## Every page, one mechanism\n\n`/og/<slug>.png` resolves blog posts, docs pages, and the core pages alike —\neach HTML page points to its card with a plain `og:image` meta tag the route\ndeclares in its `metadata`. There is no image directory to regenerate and\nnothing to forget: a new post gets a card the moment it exists, because the\ncard *is* the post's title rendered through one JSX definition.\n\n## No browser anywhere\n\nsatori lays out the JSX (its own flexbox + font shaping), resvg — Rust\ncompiled to WASM — rasterizes it to PNG. The classic alternative is a\nheadless Chromium screenshotting a hidden page in a cron job; this is a\n50KB-class route instead.\n\nFonts load at runtime with Google Fonts' `text=` subsetting: only the glyphs\nthe title actually uses, a few dozen KB, cached at the edge. That is also\nwhat makes CJK titles viable — the full story is in\n[Typesetting CJK at the edge](/blog/2026-06-10-typesetting-cjk-at-the-edge).\n\n## Dev shows the real card\n\nThe same card definition renders on both hosts: workers-og rasterizes it on\nworkerd; on the dev host the identical JSX goes through satori plus a native\nresvg binding. `localhost:3000/og/anything.png` during development IS the\ncard that ships — no \"works on prod, blank in dev\" class of surprise.\n\n## Try it\n\n```bash\ncurl -o card.png https://june.build/og/why.png            # a core page\ncurl -o card.png https://june.build/og/features-mcp.png   # a docs page\ncurl -o card.png https://june.build/og/$(date +%s).png    # unknown slug → site card\n```\n",
    "html": "<p>Share any page of this site and the social card you see was typeset <strong>at\nrequest time</strong>, in the worker. This post&#39;s own card:</p>\n<p><img src=\"/og/2026-06-12-built-in-og-image.png\" alt=\"The social card for this very post, rendered live\"></p>\n<h2>Every page, one mechanism</h2>\n<p><code>/og/&lt;slug&gt;.png</code> resolves blog posts, docs pages, and the core pages alike —\neach HTML page points to its card with a plain <code>og:image</code> meta tag the route\ndeclares in its <code>metadata</code>. There is no image directory to regenerate and\nnothing to forget: a new post gets a card the moment it exists, because the\ncard <em>is</em> the post&#39;s title rendered through one JSX definition.</p>\n<h2>No browser anywhere</h2>\n<p>satori lays out the JSX (its own flexbox + font shaping), resvg — Rust\ncompiled to WASM — rasterizes it to PNG. The classic alternative is a\nheadless Chromium screenshotting a hidden page in a cron job; this is a\n50KB-class route instead.</p>\n<p>Fonts load at runtime with Google Fonts&#39; <code>text=</code> subsetting: only the glyphs\nthe title actually uses, a few dozen KB, cached at the edge. That is also\nwhat makes CJK titles viable — the full story is in\n<a href=\"/blog/2026-06-10-typesetting-cjk-at-the-edge\">Typesetting CJK at the edge</a>.</p>\n<h2>Dev shows the real card</h2>\n<p>The same card definition renders on both hosts: workers-og rasterizes it on\nworkerd; on the dev host the identical JSX goes through satori plus a native\nresvg binding. <code>localhost:3000/og/anything.png</code> during development IS the\ncard that ships — no &quot;works on prod, blank in dev&quot; class of surprise.</p>\n<h2>Try it</h2>\n<pre><code class=\"language-bash\">curl -o card.png https://june.build/og/why.png            # a core page\ncurl -o card.png https://june.build/og/features-mcp.png   # a docs page\ncurl -o card.png https://june.build/og/$(date +%s).png    # unknown slug → site card\n</code></pre>\n"
  },
  {
    "slug": "2026-06-10-anatomy-of-a-59ms-cold-start",
    "data": {
      "title": "59ms: anatomy of a dev cold start",
      "date": "2026-06-10",
      "description": "Where every millisecond of `june dev` goes, and the three cuts that took 244ms to 59ms.",
      "tags": [
        "performance",
        "runtime"
      ]
    },
    "body": "*(The native Rust runtime is an experimental track — v0.1's `june dev` is the\nBun/Node host. These numbers are why the track exists.)*\n\nWe benchmarked our Rust dev runtime's cold start — process spawn to first HTML\nbyte — and then took it apart. Three cuts, all measured (medians, reproducible\nscripts in the repo):\n\n| cut | before | after |\n| --- | --- | --- |\n| listen-early (dev scripts off the critical path) | ~100ms blocking | 3ms to listening |\n| V8 startup snapshot (Web globals baked into the heap) | 24.4ms | 5µs |\n| React vendors in the snapshot's module map | 98ms | 18ms |\n\nEnd to end: **244ms → 59ms** on a hello-world page, with the remaining budget\nsplit between process spawn (~10ms) and first-render JIT (~30ms) — the latter\nis V8 physics for unbundled dev, and the bundled production path has already\nmeasured **22ms**.\n\nThe honest caveats: one dev machine, medians not marketing minimums, and\n`next dev`/Vite comparisons are class-level, not same-app shootouts yet.\nBenchmark rigor is its own roadmap item.\n\nOne number we did not expect: rebuilding a worker isolate after a file edit\ndropped from 137ms to **16ms**, which is what makes push-based HMR (the server\nre-renders and pushes the diff over the live channel) feel instant.\n",
    "original": "---\ntitle: \"59ms: anatomy of a dev cold start\"\ndate: 2026-06-10\ndescription: Where every millisecond of `june dev` goes, and the three cuts that took 244ms to 59ms.\ntags: [performance, runtime]\n---\n\n*(The native Rust runtime is an experimental track — v0.1's `june dev` is the\nBun/Node host. These numbers are why the track exists.)*\n\nWe benchmarked our Rust dev runtime's cold start — process spawn to first HTML\nbyte — and then took it apart. Three cuts, all measured (medians, reproducible\nscripts in the repo):\n\n| cut | before | after |\n| --- | --- | --- |\n| listen-early (dev scripts off the critical path) | ~100ms blocking | 3ms to listening |\n| V8 startup snapshot (Web globals baked into the heap) | 24.4ms | 5µs |\n| React vendors in the snapshot's module map | 98ms | 18ms |\n\nEnd to end: **244ms → 59ms** on a hello-world page, with the remaining budget\nsplit between process spawn (~10ms) and first-render JIT (~30ms) — the latter\nis V8 physics for unbundled dev, and the bundled production path has already\nmeasured **22ms**.\n\nThe honest caveats: one dev machine, medians not marketing minimums, and\n`next dev`/Vite comparisons are class-level, not same-app shootouts yet.\nBenchmark rigor is its own roadmap item.\n\nOne number we did not expect: rebuilding a worker isolate after a file edit\ndropped from 137ms to **16ms**, which is what makes push-based HMR (the server\nre-renders and pushes the diff over the live channel) feel instant.\n",
    "html": "<p><em>(The native Rust runtime is an experimental track — v0.1&#39;s <code>june dev</code> is the\nBun/Node host. These numbers are why the track exists.)</em></p>\n<p>We benchmarked our Rust dev runtime&#39;s cold start — process spawn to first HTML\nbyte — and then took it apart. Three cuts, all measured (medians, reproducible\nscripts in the repo):</p>\n<table>\n<thead>\n<tr>\n<th>cut</th>\n<th>before</th>\n<th>after</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>listen-early (dev scripts off the critical path)</td>\n<td>~100ms blocking</td>\n<td>3ms to listening</td>\n</tr>\n<tr>\n<td>V8 startup snapshot (Web globals baked into the heap)</td>\n<td>24.4ms</td>\n<td>5µs</td>\n</tr>\n<tr>\n<td>React vendors in the snapshot&#39;s module map</td>\n<td>98ms</td>\n<td>18ms</td>\n</tr>\n</tbody></table>\n<p>End to end: <strong>244ms → 59ms</strong> on a hello-world page, with the remaining budget\nsplit between process spawn (~10ms) and first-render JIT (~30ms) — the latter\nis V8 physics for unbundled dev, and the bundled production path has already\nmeasured <strong>22ms</strong>.</p>\n<p>The honest caveats: one dev machine, medians not marketing minimums, and\n<code>next dev</code>/Vite comparisons are class-level, not same-app shootouts yet.\nBenchmark rigor is its own roadmap item.</p>\n<p>One number we did not expect: rebuilding a worker isolate after a file edit\ndropped from 137ms to <strong>16ms</strong>, which is what makes push-based HMR (the server\nre-renders and pushes the diff over the live channel) feel instant.</p>\n"
  },
  {
    "slug": "2026-06-10-typesetting-cjk-at-the-edge",
    "data": {
      "title": "Typesetting CJK at the edge: og:image and font subsetting",
      "date": "2026-06-10",
      "description": "Designing live og:image typesetting on Cloudflare Workers — CJK titles included.",
      "tags": [
        "og-image",
        "fonts",
        "edge",
        "cjk"
      ]
    },
    "body": "An og:image should not be a pre-generated file — it should be a route that\nreturns a PNG: satori lays out JSX inside a V8 isolate, resvg (Rust compiled\nto WASM) rasterizes it, and no browser is involved anywhere. This site's\n`/og/<slug>.png` works exactly this way.\n\nCJK is the real test: a full CJK family (Noto Sans TC/JP/KR) weighs several\nMB per script — far too heavy to ship in a worker. The answer is Google\nFonts' `text=` parameter — **download only the glyphs the title actually\nuses**. A subset is a few dozen KB, cached for a week through workerd's\nCache API.\n\n```\n/og/<slug>.png → detect CJK → fetch font subset (text=title) → satori → resvg → PNG\n```\n\nThe same pipeline covers every CJK script with no per-language work — and\nthe distinctions matter: Traditional and Simplified Chinese are different\nglyph sets served by different fonts (Noto Sans TC vs SC), and Japanese\nmixes kana with its own kanji forms:\n\n- エッジで日本語を組版する (Japanese — kana and kanji)\n- 邊緣排版與字型子集化 (Traditional Chinese)\n- 边缘排版与字体子集化 (Simplified Chinese)\n- 글꼴 서브셋 (Korean)\n\nA common alternative is self-hosting fonts at build time (our asset pipeline\nwill do that too), but a build cannot know the glyph set of a dynamic title\nahead of time — runtime subsetting is the right call for the og:image case.\n",
    "original": "---\ntitle: \"Typesetting CJK at the edge: og:image and font subsetting\"\ndate: 2026-06-10\ndescription: Designing live og:image typesetting on Cloudflare Workers — CJK titles included.\ntags: [og-image, fonts, edge, cjk]\n---\n\nAn og:image should not be a pre-generated file — it should be a route that\nreturns a PNG: satori lays out JSX inside a V8 isolate, resvg (Rust compiled\nto WASM) rasterizes it, and no browser is involved anywhere. This site's\n`/og/<slug>.png` works exactly this way.\n\nCJK is the real test: a full CJK family (Noto Sans TC/JP/KR) weighs several\nMB per script — far too heavy to ship in a worker. The answer is Google\nFonts' `text=` parameter — **download only the glyphs the title actually\nuses**. A subset is a few dozen KB, cached for a week through workerd's\nCache API.\n\n```\n/og/<slug>.png → detect CJK → fetch font subset (text=title) → satori → resvg → PNG\n```\n\nThe same pipeline covers every CJK script with no per-language work — and\nthe distinctions matter: Traditional and Simplified Chinese are different\nglyph sets served by different fonts (Noto Sans TC vs SC), and Japanese\nmixes kana with its own kanji forms:\n\n- エッジで日本語を組版する (Japanese — kana and kanji)\n- 邊緣排版與字型子集化 (Traditional Chinese)\n- 边缘排版与字体子集化 (Simplified Chinese)\n- 글꼴 서브셋 (Korean)\n\nA common alternative is self-hosting fonts at build time (our asset pipeline\nwill do that too), but a build cannot know the glyph set of a dynamic title\nahead of time — runtime subsetting is the right call for the og:image case.\n",
    "html": "<p>An og:image should not be a pre-generated file — it should be a route that\nreturns a PNG: satori lays out JSX inside a V8 isolate, resvg (Rust compiled\nto WASM) rasterizes it, and no browser is involved anywhere. This site&#39;s\n<code>/og/&lt;slug&gt;.png</code> works exactly this way.</p>\n<p>CJK is the real test: a full CJK family (Noto Sans TC/JP/KR) weighs several\nMB per script — far too heavy to ship in a worker. The answer is Google\nFonts&#39; <code>text=</code> parameter — <strong>download only the glyphs the title actually\nuses</strong>. A subset is a few dozen KB, cached for a week through workerd&#39;s\nCache API.</p>\n<pre><code>/og/&lt;slug&gt;.png → detect CJK → fetch font subset (text=title) → satori → resvg → PNG\n</code></pre>\n<p>The same pipeline covers every CJK script with no per-language work — and\nthe distinctions matter: Traditional and Simplified Chinese are different\nglyph sets served by different fonts (Noto Sans TC vs SC), and Japanese\nmixes kana with its own kanji forms:</p>\n<ul>\n<li>エッジで日本語を組版する (Japanese — kana and kanji)</li>\n<li>邊緣排版與字型子集化 (Traditional Chinese)</li>\n<li>边缘排版与字体子集化 (Simplified Chinese)</li>\n<li>글꼴 서브셋 (Korean)</li>\n</ul>\n<p>A common alternative is self-hosting fonts at build time (our asset pipeline\nwill do that too), but a build cannot know the glyph set of a dynamic title\nahead of time — runtime subsetting is the right call for the og:image case.</p>\n"
  }
];
export const post = (slug: string): ContentEntry | null => POSTS.find((p) => p.slug === slug) ?? null;
export const DOCS: ContentEntry[] = [
  {
    "slug": "features-live-updates",
    "data": {
      "title": "Live updates: a connection that survives navigation",
      "nav": "Live updates",
      "description": "Put a server-push connection (SSE or WebSocket) inside a persist island — it keeps streaming across soft navigations instead of reconnecting on every click. Plus what to know about connection limits when you deploy.",
      "date": "2026-06-15",
      "section": "Features",
      "order": "9.6"
    },
    "body": "## The feature\n\nA live surface — a dashboard, a token stream, a build log, tool-call status —\nneeds a connection that **outlives the page it started on**. The\n[Client Router](/docs/features-client-router) gives you the seam: open your\nconnection inside an [island](/docs/features-islands), mark it `persist`, and\nJune carries that live node — React state, open socket and all — across a soft\nnavigation instead of tearing it down and reconnecting.\n\n```tsx\n// app/StatusFeed.tsx — an island that owns its own connection\nimport { useEffect, useState } from \"react\";\n\nexport function StatusFeed() {\n  const [events, setEvents] = useState<string[]>([]);\n\n  useEffect(() => {\n    const es = new EventSource(\"/api/status\");           // server push (SSE)\n    es.onmessage = (e) => setEvents((prev) => [e.data, ...prev].slice(0, 50));\n    return () => es.close();\n  }, []);\n\n  return (\n    <ul>\n      {events.map((line, i) => <li key={i}>{line}</li>)}\n    </ul>\n  );\n}\n```\n\n```tsx\n// app/dashboard/layout.tsx — persist it so it survives navigation\nimport { Island } from \"@junejs/core/islands\";\nimport { StatusFeed } from \"../StatusFeed\";\n\nexport default function Layout({ children }: { children: React.ReactNode }) {\n  return (\n    <>\n      <Island name=\"StatusFeed\" component={StatusFeed} persist />\n      {children}\n    </>\n  );\n}\n```\n\nClick between pages inside the dashboard and the feed keeps streaming — no flash,\nno dropped connection, no re-subscribe. Without `persist`, every navigation would\nrestart the `EventSource` from scratch.\n\n## SSE or WebSocket — pick by direction\n\nThe island owns the transport, so use whichever fits:\n\n- **Server-Sent Events** (`EventSource`) for one-way server→client streams (live\n  logs, notifications, AI token streams). It has **built-in auto-reconnect** and\n  resumes with `Last-Event-ID` — the least code for the most common case.\n- **WebSocket** when the client also talks back (chat, collaborative editing,\n  interactive control planes).\n\nEither way the page stays a complete, projectable document — `.md`, `.json`, and\n[`/mcp`](/docs/features-mcp) are untouched. Live updates live only on the human\nsurface; the agent surface still gets the same clean snapshot.\n\n## Deploying a connection: what to know\n\nA held-open connection behaves differently from a request/response route, and the\nlimits depend on where you deploy:\n\n- **A single connection is capped (~5 minutes by default on most hosts).** This is\n  normal — design for it. SSE's auto-reconnect handles it for free; for WebSocket,\n  reconnect on close. Treat a drop as routine, not an error.\n- **An open connection bills for the time it's held**, even while idle — it's\n  occupancy, not CPU (waiting is free). Push only when data actually changes, and\n  the connection stays cheap. On **Cloudflare**, long-lived connections belong in a\n  **Durable Object** (with WebSocket Hibernation, an idle connection costs nothing);\n  on **Vercel**, a function streams up to its `maxDuration` then reconnects.\n- **Offer a fallback.** If a connection can't be established (a strict proxy, an old\n  client), poll the same endpoint on an interval. The UI code doesn't care how the\n  next update arrived.\n\n## You only pay for the pages that want it\n\nLive updates are inherently opt-in: a page with no connection island is still\n[zero-JS](/docs/features-web-standards) and costs nothing extra. The streaming\nsurfaces are exactly the ones you marked — everything else stays a plain, fast,\ncacheable document. Add a connection where the experience needs one, not site-wide.\n\n## Why it matters\n\nMost frameworks make you choose between \"instant document\" and \"live app\" at the\nproject level. June lets you keep the document floor everywhere and add a surviving\nconnection precisely where a surface earns it — one island, one `persist`, your own\ntransport. The hard part (keeping the live node alive across navigation) is the\nframework's job; the connection itself stays plain web-standard code you fully\ncontrol.\n",
    "original": "---\ntitle: \"Live updates: a connection that survives navigation\"\nnav: \"Live updates\"\ndescription: Put a server-push connection (SSE or WebSocket) inside a persist island — it keeps streaming across soft navigations instead of reconnecting on every click. Plus what to know about connection limits when you deploy.\ndate: 2026-06-15\nsection: Features\norder: \"9.6\"\n---\n## The feature\n\nA live surface — a dashboard, a token stream, a build log, tool-call status —\nneeds a connection that **outlives the page it started on**. The\n[Client Router](/docs/features-client-router) gives you the seam: open your\nconnection inside an [island](/docs/features-islands), mark it `persist`, and\nJune carries that live node — React state, open socket and all — across a soft\nnavigation instead of tearing it down and reconnecting.\n\n```tsx\n// app/StatusFeed.tsx — an island that owns its own connection\nimport { useEffect, useState } from \"react\";\n\nexport function StatusFeed() {\n  const [events, setEvents] = useState<string[]>([]);\n\n  useEffect(() => {\n    const es = new EventSource(\"/api/status\");           // server push (SSE)\n    es.onmessage = (e) => setEvents((prev) => [e.data, ...prev].slice(0, 50));\n    return () => es.close();\n  }, []);\n\n  return (\n    <ul>\n      {events.map((line, i) => <li key={i}>{line}</li>)}\n    </ul>\n  );\n}\n```\n\n```tsx\n// app/dashboard/layout.tsx — persist it so it survives navigation\nimport { Island } from \"@junejs/core/islands\";\nimport { StatusFeed } from \"../StatusFeed\";\n\nexport default function Layout({ children }: { children: React.ReactNode }) {\n  return (\n    <>\n      <Island name=\"StatusFeed\" component={StatusFeed} persist />\n      {children}\n    </>\n  );\n}\n```\n\nClick between pages inside the dashboard and the feed keeps streaming — no flash,\nno dropped connection, no re-subscribe. Without `persist`, every navigation would\nrestart the `EventSource` from scratch.\n\n## SSE or WebSocket — pick by direction\n\nThe island owns the transport, so use whichever fits:\n\n- **Server-Sent Events** (`EventSource`) for one-way server→client streams (live\n  logs, notifications, AI token streams). It has **built-in auto-reconnect** and\n  resumes with `Last-Event-ID` — the least code for the most common case.\n- **WebSocket** when the client also talks back (chat, collaborative editing,\n  interactive control planes).\n\nEither way the page stays a complete, projectable document — `.md`, `.json`, and\n[`/mcp`](/docs/features-mcp) are untouched. Live updates live only on the human\nsurface; the agent surface still gets the same clean snapshot.\n\n## Deploying a connection: what to know\n\nA held-open connection behaves differently from a request/response route, and the\nlimits depend on where you deploy:\n\n- **A single connection is capped (~5 minutes by default on most hosts).** This is\n  normal — design for it. SSE's auto-reconnect handles it for free; for WebSocket,\n  reconnect on close. Treat a drop as routine, not an error.\n- **An open connection bills for the time it's held**, even while idle — it's\n  occupancy, not CPU (waiting is free). Push only when data actually changes, and\n  the connection stays cheap. On **Cloudflare**, long-lived connections belong in a\n  **Durable Object** (with WebSocket Hibernation, an idle connection costs nothing);\n  on **Vercel**, a function streams up to its `maxDuration` then reconnects.\n- **Offer a fallback.** If a connection can't be established (a strict proxy, an old\n  client), poll the same endpoint on an interval. The UI code doesn't care how the\n  next update arrived.\n\n## You only pay for the pages that want it\n\nLive updates are inherently opt-in: a page with no connection island is still\n[zero-JS](/docs/features-web-standards) and costs nothing extra. The streaming\nsurfaces are exactly the ones you marked — everything else stays a plain, fast,\ncacheable document. Add a connection where the experience needs one, not site-wide.\n\n## Why it matters\n\nMost frameworks make you choose between \"instant document\" and \"live app\" at the\nproject level. June lets you keep the document floor everywhere and add a surviving\nconnection precisely where a surface earns it — one island, one `persist`, your own\ntransport. The hard part (keeping the live node alive across navigation) is the\nframework's job; the connection itself stays plain web-standard code you fully\ncontrol.\n",
    "html": "<h2>The feature</h2>\n<p>A live surface — a dashboard, a token stream, a build log, tool-call status —\nneeds a connection that <strong>outlives the page it started on</strong>. The\n<a href=\"/docs/features-client-router\">Client Router</a> gives you the seam: open your\nconnection inside an <a href=\"/docs/features-islands\">island</a>, mark it <code>persist</code>, and\nJune carries that live node — React state, open socket and all — across a soft\nnavigation instead of tearing it down and reconnecting.</p>\n<pre><code class=\"language-tsx\">// app/StatusFeed.tsx — an island that owns its own connection\nimport { useEffect, useState } from &quot;react&quot;;\n\nexport function StatusFeed() {\n  const [events, setEvents] = useState&lt;string[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const es = new EventSource(&quot;/api/status&quot;);           // server push (SSE)\n    es.onmessage = (e) =&gt; setEvents((prev) =&gt; [e.data, ...prev].slice(0, 50));\n    return () =&gt; es.close();\n  }, []);\n\n  return (\n    &lt;ul&gt;\n      {events.map((line, i) =&gt; &lt;li key={i}&gt;{line}&lt;/li&gt;)}\n    &lt;/ul&gt;\n  );\n}\n</code></pre>\n<pre><code class=\"language-tsx\">// app/dashboard/layout.tsx — persist it so it survives navigation\nimport { Island } from &quot;@junejs/core/islands&quot;;\nimport { StatusFeed } from &quot;../StatusFeed&quot;;\n\nexport default function Layout({ children }: { children: React.ReactNode }) {\n  return (\n    &lt;&gt;\n      &lt;Island name=&quot;StatusFeed&quot; component={StatusFeed} persist /&gt;\n      {children}\n    &lt;/&gt;\n  );\n}\n</code></pre>\n<p>Click between pages inside the dashboard and the feed keeps streaming — no flash,\nno dropped connection, no re-subscribe. Without <code>persist</code>, every navigation would\nrestart the <code>EventSource</code> from scratch.</p>\n<h2>SSE or WebSocket — pick by direction</h2>\n<p>The island owns the transport, so use whichever fits:</p>\n<ul>\n<li><strong>Server-Sent Events</strong> (<code>EventSource</code>) for one-way server→client streams (live\nlogs, notifications, AI token streams). It has <strong>built-in auto-reconnect</strong> and\nresumes with <code>Last-Event-ID</code> — the least code for the most common case.</li>\n<li><strong>WebSocket</strong> when the client also talks back (chat, collaborative editing,\ninteractive control planes).</li>\n</ul>\n<p>Either way the page stays a complete, projectable document — <code>.md</code>, <code>.json</code>, and\n<a href=\"/docs/features-mcp\"><code>/mcp</code></a> are untouched. Live updates live only on the human\nsurface; the agent surface still gets the same clean snapshot.</p>\n<h2>Deploying a connection: what to know</h2>\n<p>A held-open connection behaves differently from a request/response route, and the\nlimits depend on where you deploy:</p>\n<ul>\n<li><strong>A single connection is capped (~5 minutes by default on most hosts).</strong> This is\nnormal — design for it. SSE&#39;s auto-reconnect handles it for free; for WebSocket,\nreconnect on close. Treat a drop as routine, not an error.</li>\n<li><strong>An open connection bills for the time it&#39;s held</strong>, even while idle — it&#39;s\noccupancy, not CPU (waiting is free). Push only when data actually changes, and\nthe connection stays cheap. On <strong>Cloudflare</strong>, long-lived connections belong in a\n<strong>Durable Object</strong> (with WebSocket Hibernation, an idle connection costs nothing);\non <strong>Vercel</strong>, a function streams up to its <code>maxDuration</code> then reconnects.</li>\n<li><strong>Offer a fallback.</strong> If a connection can&#39;t be established (a strict proxy, an old\nclient), poll the same endpoint on an interval. The UI code doesn&#39;t care how the\nnext update arrived.</li>\n</ul>\n<h2>You only pay for the pages that want it</h2>\n<p>Live updates are inherently opt-in: a page with no connection island is still\n<a href=\"/docs/features-web-standards\">zero-JS</a> and costs nothing extra. The streaming\nsurfaces are exactly the ones you marked — everything else stays a plain, fast,\ncacheable document. Add a connection where the experience needs one, not site-wide.</p>\n<h2>Why it matters</h2>\n<p>Most frameworks make you choose between &quot;instant document&quot; and &quot;live app&quot; at the\nproject level. June lets you keep the document floor everywhere and add a surviving\nconnection precisely where a surface earns it — one island, one <code>persist</code>, your own\ntransport. The hard part (keeping the live node alive across navigation) is the\nframework&#39;s job; the connection itself stays plain web-standard code you fully\ncontrol.</p>\n"
  },
  {
    "slug": "features-client-router",
    "data": {
      "title": "Client Router: the opt-in SPA layer",
      "nav": "Client Router",
      "description": "Off by default — turn it on for app-like surfaces that need in-memory state to survive navigation. Soft swaps over the same HTML the server already serves, with <Island persist> for websockets.",
      "date": "2026-06-14",
      "section": "Features",
      "order": "9.5"
    },
    "body": "## The feature\n\n[Navigation](/docs/features-navigation) is the floor: the browser navigates,\nJune ships no router. But some surfaces — a dashboard, a builder, anything\nwith a live connection — need in-memory state to *survive* a navigation, not\nreset on every click. For those, flip one switch:\n\n```ts\n// june.config.ts\nexport default defineJune({ clientRouter: true });\n```\n\nNow same-origin link clicks become **soft swaps**: June fetches the next page\n— the *same* complete HTML document the server already serves, no special\npayload format — replaces the page region, re-hydrates that page's\n[islands](/docs/features-islands), and animates the swap with View\nTransitions. History (`pushState`/back-forward) just works. There is no\nclient route table and no [Flight payload](/docs/features-rsc): the\nfull-HTML-per-URL contract *is* the navigation transport.\n\n## Persist a live island\n\nBecause June composes layouts into the page itself, a normal swap would tear\ndown everything — including an open websocket. Mark an island `persist` and\nthe router carries its **live node** (React state, open connections and all)\nacross the navigation instead of re-creating it:\n\n```tsx\n// app/dashboard/layout.tsx\nimport { Island } from \"@junejs/core/islands\";\nimport { LiveFeed } from \"./LiveFeed\";\n\nexport default function DashboardLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <>\n      <Island name=\"LiveFeed\" component={LiveFeed} persist />  {/* socket stays open */}\n      {children}\n    </>\n  );\n}\n```\n\n## Pure progressive enhancement\n\nThe router only ever *adds* behavior on top of pages that already work:\n\n- **It degrades.** No JS, a failed fetch, or a response it doesn't recognize\n  all fall back to a normal browser navigation — never a broken page.\n- **It's race-safe.** A navigation-generation token discards a stale response\n  when a newer navigation overtakes it (the classic click-then-back bug).\n- **The agent surface is untouched.** Every URL is still a complete,\n  projectable document — `.md`, `.json`, and `/mcp`\n  ([MCP](/docs/features-mcp)) are exactly what they were. The router lives\n  only on the human surface.\n\nOff by default. This very site keeps it off — it's documents, so the browser\nnavigates and we ship [~1KB of rules, no router](/docs/features-navigation).\n\n## Why it matters\n\nThe real question was never \"does June have an SPA mode\" — it's \"do you want\nto carry a client router.\" For most sites the answer is no, and the\n[Standards](/docs/features-web-standards) floor already feels instant. For\nthe app-like minority that needs state to outlive a navigation, the router is\nthere as one config line — and even then it's an enhancement you can remove,\nnot a runtime your whole site now depends on.\n",
    "original": "---\ntitle: \"Client Router: the opt-in SPA layer\"\nnav: \"Client Router\"\ndescription: Off by default — turn it on for app-like surfaces that need in-memory state to survive navigation. Soft swaps over the same HTML the server already serves, with <Island persist> for websockets.\ndate: 2026-06-14\nsection: Features\norder: \"9.5\"\n---\n## The feature\n\n[Navigation](/docs/features-navigation) is the floor: the browser navigates,\nJune ships no router. But some surfaces — a dashboard, a builder, anything\nwith a live connection — need in-memory state to *survive* a navigation, not\nreset on every click. For those, flip one switch:\n\n```ts\n// june.config.ts\nexport default defineJune({ clientRouter: true });\n```\n\nNow same-origin link clicks become **soft swaps**: June fetches the next page\n— the *same* complete HTML document the server already serves, no special\npayload format — replaces the page region, re-hydrates that page's\n[islands](/docs/features-islands), and animates the swap with View\nTransitions. History (`pushState`/back-forward) just works. There is no\nclient route table and no [Flight payload](/docs/features-rsc): the\nfull-HTML-per-URL contract *is* the navigation transport.\n\n## Persist a live island\n\nBecause June composes layouts into the page itself, a normal swap would tear\ndown everything — including an open websocket. Mark an island `persist` and\nthe router carries its **live node** (React state, open connections and all)\nacross the navigation instead of re-creating it:\n\n```tsx\n// app/dashboard/layout.tsx\nimport { Island } from \"@junejs/core/islands\";\nimport { LiveFeed } from \"./LiveFeed\";\n\nexport default function DashboardLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <>\n      <Island name=\"LiveFeed\" component={LiveFeed} persist />  {/* socket stays open */}\n      {children}\n    </>\n  );\n}\n```\n\n## Pure progressive enhancement\n\nThe router only ever *adds* behavior on top of pages that already work:\n\n- **It degrades.** No JS, a failed fetch, or a response it doesn't recognize\n  all fall back to a normal browser navigation — never a broken page.\n- **It's race-safe.** A navigation-generation token discards a stale response\n  when a newer navigation overtakes it (the classic click-then-back bug).\n- **The agent surface is untouched.** Every URL is still a complete,\n  projectable document — `.md`, `.json`, and `/mcp`\n  ([MCP](/docs/features-mcp)) are exactly what they were. The router lives\n  only on the human surface.\n\nOff by default. This very site keeps it off — it's documents, so the browser\nnavigates and we ship [~1KB of rules, no router](/docs/features-navigation).\n\n## Why it matters\n\nThe real question was never \"does June have an SPA mode\" — it's \"do you want\nto carry a client router.\" For most sites the answer is no, and the\n[Standards](/docs/features-web-standards) floor already feels instant. For\nthe app-like minority that needs state to outlive a navigation, the router is\nthere as one config line — and even then it's an enhancement you can remove,\nnot a runtime your whole site now depends on.\n",
    "html": "<h2>The feature</h2>\n<p><a href=\"/docs/features-navigation\">Navigation</a> is the floor: the browser navigates,\nJune ships no router. But some surfaces — a dashboard, a builder, anything\nwith a live connection — need in-memory state to <em>survive</em> a navigation, not\nreset on every click. For those, flip one switch:</p>\n<pre><code class=\"language-ts\">// june.config.ts\nexport default defineJune({ clientRouter: true });\n</code></pre>\n<p>Now same-origin link clicks become <strong>soft swaps</strong>: June fetches the next page\n— the <em>same</em> complete HTML document the server already serves, no special\npayload format — replaces the page region, re-hydrates that page&#39;s\n<a href=\"/docs/features-islands\">islands</a>, and animates the swap with View\nTransitions. History (<code>pushState</code>/back-forward) just works. There is no\nclient route table and no <a href=\"/docs/features-rsc\">Flight payload</a>: the\nfull-HTML-per-URL contract <em>is</em> the navigation transport.</p>\n<h2>Persist a live island</h2>\n<p>Because June composes layouts into the page itself, a normal swap would tear\ndown everything — including an open websocket. Mark an island <code>persist</code> and\nthe router carries its <strong>live node</strong> (React state, open connections and all)\nacross the navigation instead of re-creating it:</p>\n<pre><code class=\"language-tsx\">// app/dashboard/layout.tsx\nimport { Island } from &quot;@junejs/core/islands&quot;;\nimport { LiveFeed } from &quot;./LiveFeed&quot;;\n\nexport default function DashboardLayout({ children }: { children: React.ReactNode }) {\n  return (\n    &lt;&gt;\n      &lt;Island name=&quot;LiveFeed&quot; component={LiveFeed} persist /&gt;  {/* socket stays open */}\n      {children}\n    &lt;/&gt;\n  );\n}\n</code></pre>\n<h2>Pure progressive enhancement</h2>\n<p>The router only ever <em>adds</em> behavior on top of pages that already work:</p>\n<ul>\n<li><strong>It degrades.</strong> No JS, a failed fetch, or a response it doesn&#39;t recognize\nall fall back to a normal browser navigation — never a broken page.</li>\n<li><strong>It&#39;s race-safe.</strong> A navigation-generation token discards a stale response\nwhen a newer navigation overtakes it (the classic click-then-back bug).</li>\n<li><strong>The agent surface is untouched.</strong> Every URL is still a complete,\nprojectable document — <code>.md</code>, <code>.json</code>, and <code>/mcp</code>\n(<a href=\"/docs/features-mcp\">MCP</a>) are exactly what they were. The router lives\nonly on the human surface.</li>\n</ul>\n<p>Off by default. This very site keeps it off — it&#39;s documents, so the browser\nnavigates and we ship <a href=\"/docs/features-navigation\">~1KB of rules, no router</a>.</p>\n<h2>Why it matters</h2>\n<p>The real question was never &quot;does June have an SPA mode&quot; — it&#39;s &quot;do you want\nto carry a client router.&quot; For most sites the answer is no, and the\n<a href=\"/docs/features-web-standards\">Standards</a> floor already feels instant. For\nthe app-like minority that needs state to outlive a navigation, the router is\nthere as one config line — and even then it&#39;s an enhancement you can remove,\nnot a runtime your whole site now depends on.</p>\n"
  },
  {
    "slug": "features-data",
    "data": {
      "title": "Data: ambient db + cache magic",
      "nav": "Data magic",
      "description": "Declare db/kv/blob once and reach them with `import { db } from \"@junejs/db\"` — no request object threaded; a write auto-invalidates cached reads and N reads auto-batch.",
      "date": "2026-06-13",
      "section": "Features",
      "order": "10"
    },
    "body": "## The feature\n\nData is declared, not wired. Declaring a resource in `june.config.ts` enables\nit — omit it and it doesn't exist. Each gets a zero-config local default in\n`june dev` and a deploy binding on Workers:\n\n```ts\n// june.config.ts\nimport { defineJune } from \"@junejs/core/config\";\nimport { sqlite } from \"@junejs/server/db\";\n\nexport default defineJune({\n  resources: {\n    db: sqlite(), // dev: ./.june/dev.sqlite · deploy: D1\n  },\n});\n```\n\nYou reach it from anywhere with an **ambient** handle — there is no request\nobject to thread:\n\n```ts\nimport { db } from \"@junejs/db\";\n\nexport const loader = async () => ({\n  users: await db.query(\"select id, name from users order by id\"),\n});\n```\n\nThat same `import` works unchanged in a loader, a view, a `defineAction()`, or a\nplain model file three calls deep. Keeping `db` off `ctx` is deliberate:\n\n> **`ctx` is identity; `db`/`kv`/`blob` are capability.** `ctx` answers *who is\n> calling* (user, session, url, params) — what authorization needs. The\n> resources answer *what tools exist*. Mixing them onto one object forces every\n> helper to thread `ctx` just to touch the database (the Express `req.db`\n> anti-pattern). Instead the host runs each request inside a scope that holds\n> the opened resources, and `db`/`kv`/`blob` read it through\n> `AsyncLocalStorage` — so domain code never sees the request. It stays\n> edge-safe: the async context is loaded lazily, so nothing pulls a static\n> `node:*` into the worker.\n\nThe dev default is a plain file (`.june/dev.sqlite`) that survives the dev\nserver's reload-on-save restarts — and the `sqlite3` already on your machine\nopens it directly:\n\n```bash\nsqlite3 .june/dev.sqlite '.tables'\n```\n\n`kv` and `blob` follow the same shape — `memoryKv()` / `localBlob()` in dev,\n`redisKv()` / `r2()` on deploy — reached as ambient `kv` and `blob`.\n\n## Schema is explicit (migrations)\n\nA connected database does **not** invent tables for you. Schema lives in\nversioned SQL you can read and diff:\n\n```sql\n-- db/migrations/0001_init.sql\ncreate table users (id integer primary key, name text not null);\n```\n\n`june dev` applies pending migrations on startup — **safe, additive ones\nautomatically** (`create table`, `add column`, a new index). A **destructive**\nchange (`drop`, a narrowing `alter`, …) halts with the safe prefix already\napplied and asks first; you run it deliberately with\n`june db migrate --allow-destructive`. Add the next change as\n`db/migrations/0002_*.sql` — never edit an applied file. The same ordered ledger\nruns at deploy against D1, so dev and production converge on one schema.\n\n## The magic\n\nReads and writes go through Juno (the default data layer), which emits a public\ntrace contract — `recordTableRead` / `recordTableWrite`. That is all the\nframework needs to:\n\n- **auto-tag** every `cache()` entry by the tables it read,\n- **auto-invalidate** those entries when an action writes the table — no manual\n  `revalidate()`, ever,\n- **auto-batch** N component reads of the same query into one.\n\n```ts\nimport { db } from \"@junejs/db\";\n\nconst listUsers = cache(() => db.table(\"users\").all());   // tagged [table:users]\nawait invokeAction(\"createUser\", { name: \"Ada\" });        // writes users →\nawait listUsers();                                        // cache MISS — fresh data\n```\n\nThe magic is a property of the *trace contract*, not of Juno: bring Prisma or\nDrizzle untouched (Tier 1), run them over the same connection to share config\n(Tier 2), or add a thin shim that names the table and get the same magic\n(Tier 3).\n\n## Why it matters\n\nStale-cache bugs are the classic failure mode of hand-wired `revalidate()`\ncalls — and agents writing code forget them exactly as often as humans do.\nMaking invalidation a consequence of the write removes the class of bug. And\nbecause `db` rides an ambient scope rather than `ctx`, the code an agent writes\nto read or write data is identical in a route and in a helper — nothing to\nthread, nothing to mock.\n",
    "original": "---\ntitle: \"Data: ambient db + cache magic\"\nnav: \"Data magic\"\ndescription: Declare db/kv/blob once and reach them with `import { db } from \"@junejs/db\"` — no request object threaded; a write auto-invalidates cached reads and N reads auto-batch.\ndate: 2026-06-13\nsection: Features\norder: \"10\"\n---\n## The feature\n\nData is declared, not wired. Declaring a resource in `june.config.ts` enables\nit — omit it and it doesn't exist. Each gets a zero-config local default in\n`june dev` and a deploy binding on Workers:\n\n```ts\n// june.config.ts\nimport { defineJune } from \"@junejs/core/config\";\nimport { sqlite } from \"@junejs/server/db\";\n\nexport default defineJune({\n  resources: {\n    db: sqlite(), // dev: ./.june/dev.sqlite · deploy: D1\n  },\n});\n```\n\nYou reach it from anywhere with an **ambient** handle — there is no request\nobject to thread:\n\n```ts\nimport { db } from \"@junejs/db\";\n\nexport const loader = async () => ({\n  users: await db.query(\"select id, name from users order by id\"),\n});\n```\n\nThat same `import` works unchanged in a loader, a view, a `defineAction()`, or a\nplain model file three calls deep. Keeping `db` off `ctx` is deliberate:\n\n> **`ctx` is identity; `db`/`kv`/`blob` are capability.** `ctx` answers *who is\n> calling* (user, session, url, params) — what authorization needs. The\n> resources answer *what tools exist*. Mixing them onto one object forces every\n> helper to thread `ctx` just to touch the database (the Express `req.db`\n> anti-pattern). Instead the host runs each request inside a scope that holds\n> the opened resources, and `db`/`kv`/`blob` read it through\n> `AsyncLocalStorage` — so domain code never sees the request. It stays\n> edge-safe: the async context is loaded lazily, so nothing pulls a static\n> `node:*` into the worker.\n\nThe dev default is a plain file (`.june/dev.sqlite`) that survives the dev\nserver's reload-on-save restarts — and the `sqlite3` already on your machine\nopens it directly:\n\n```bash\nsqlite3 .june/dev.sqlite '.tables'\n```\n\n`kv` and `blob` follow the same shape — `memoryKv()` / `localBlob()` in dev,\n`redisKv()` / `r2()` on deploy — reached as ambient `kv` and `blob`.\n\n## Schema is explicit (migrations)\n\nA connected database does **not** invent tables for you. Schema lives in\nversioned SQL you can read and diff:\n\n```sql\n-- db/migrations/0001_init.sql\ncreate table users (id integer primary key, name text not null);\n```\n\n`june dev` applies pending migrations on startup — **safe, additive ones\nautomatically** (`create table`, `add column`, a new index). A **destructive**\nchange (`drop`, a narrowing `alter`, …) halts with the safe prefix already\napplied and asks first; you run it deliberately with\n`june db migrate --allow-destructive`. Add the next change as\n`db/migrations/0002_*.sql` — never edit an applied file. The same ordered ledger\nruns at deploy against D1, so dev and production converge on one schema.\n\n## The magic\n\nReads and writes go through Juno (the default data layer), which emits a public\ntrace contract — `recordTableRead` / `recordTableWrite`. That is all the\nframework needs to:\n\n- **auto-tag** every `cache()` entry by the tables it read,\n- **auto-invalidate** those entries when an action writes the table — no manual\n  `revalidate()`, ever,\n- **auto-batch** N component reads of the same query into one.\n\n```ts\nimport { db } from \"@junejs/db\";\n\nconst listUsers = cache(() => db.table(\"users\").all());   // tagged [table:users]\nawait invokeAction(\"createUser\", { name: \"Ada\" });        // writes users →\nawait listUsers();                                        // cache MISS — fresh data\n```\n\nThe magic is a property of the *trace contract*, not of Juno: bring Prisma or\nDrizzle untouched (Tier 1), run them over the same connection to share config\n(Tier 2), or add a thin shim that names the table and get the same magic\n(Tier 3).\n\n## Why it matters\n\nStale-cache bugs are the classic failure mode of hand-wired `revalidate()`\ncalls — and agents writing code forget them exactly as often as humans do.\nMaking invalidation a consequence of the write removes the class of bug. And\nbecause `db` rides an ambient scope rather than `ctx`, the code an agent writes\nto read or write data is identical in a route and in a helper — nothing to\nthread, nothing to mock.\n",
    "html": "<h2>The feature</h2>\n<p>Data is declared, not wired. Declaring a resource in <code>june.config.ts</code> enables\nit — omit it and it doesn&#39;t exist. Each gets a zero-config local default in\n<code>june dev</code> and a deploy binding on Workers:</p>\n<pre><code class=\"language-ts\">// june.config.ts\nimport { defineJune } from &quot;@junejs/core/config&quot;;\nimport { sqlite } from &quot;@junejs/server/db&quot;;\n\nexport default defineJune({\n  resources: {\n    db: sqlite(), // dev: ./.june/dev.sqlite · deploy: D1\n  },\n});\n</code></pre>\n<p>You reach it from anywhere with an <strong>ambient</strong> handle — there is no request\nobject to thread:</p>\n<pre><code class=\"language-ts\">import { db } from &quot;@junejs/db&quot;;\n\nexport const loader = async () =&gt; ({\n  users: await db.query(&quot;select id, name from users order by id&quot;),\n});\n</code></pre>\n<p>That same <code>import</code> works unchanged in a loader, a view, a <code>defineAction()</code>, or a\nplain model file three calls deep. Keeping <code>db</code> off <code>ctx</code> is deliberate:</p>\n<blockquote>\n<p><strong><code>ctx</code> is identity; <code>db</code>/<code>kv</code>/<code>blob</code> are capability.</strong> <code>ctx</code> answers <em>who is\ncalling</em> (user, session, url, params) — what authorization needs. The\nresources answer <em>what tools exist</em>. Mixing them onto one object forces every\nhelper to thread <code>ctx</code> just to touch the database (the Express <code>req.db</code>\nanti-pattern). Instead the host runs each request inside a scope that holds\nthe opened resources, and <code>db</code>/<code>kv</code>/<code>blob</code> read it through\n<code>AsyncLocalStorage</code> — so domain code never sees the request. It stays\nedge-safe: the async context is loaded lazily, so nothing pulls a static\n<code>node:*</code> into the worker.</p>\n</blockquote>\n<p>The dev default is a plain file (<code>.june/dev.sqlite</code>) that survives the dev\nserver&#39;s reload-on-save restarts — and the <code>sqlite3</code> already on your machine\nopens it directly:</p>\n<pre><code class=\"language-bash\">sqlite3 .june/dev.sqlite &#39;.tables&#39;\n</code></pre>\n<p><code>kv</code> and <code>blob</code> follow the same shape — <code>memoryKv()</code> / <code>localBlob()</code> in dev,\n<code>redisKv()</code> / <code>r2()</code> on deploy — reached as ambient <code>kv</code> and <code>blob</code>.</p>\n<h2>Schema is explicit (migrations)</h2>\n<p>A connected database does <strong>not</strong> invent tables for you. Schema lives in\nversioned SQL you can read and diff:</p>\n<pre><code class=\"language-sql\">-- db/migrations/0001_init.sql\ncreate table users (id integer primary key, name text not null);\n</code></pre>\n<p><code>june dev</code> applies pending migrations on startup — <strong>safe, additive ones\nautomatically</strong> (<code>create table</code>, <code>add column</code>, a new index). A <strong>destructive</strong>\nchange (<code>drop</code>, a narrowing <code>alter</code>, …) halts with the safe prefix already\napplied and asks first; you run it deliberately with\n<code>june db migrate --allow-destructive</code>. Add the next change as\n<code>db/migrations/0002_*.sql</code> — never edit an applied file. The same ordered ledger\nruns at deploy against D1, so dev and production converge on one schema.</p>\n<h2>The magic</h2>\n<p>Reads and writes go through Juno (the default data layer), which emits a public\ntrace contract — <code>recordTableRead</code> / <code>recordTableWrite</code>. That is all the\nframework needs to:</p>\n<ul>\n<li><strong>auto-tag</strong> every <code>cache()</code> entry by the tables it read,</li>\n<li><strong>auto-invalidate</strong> those entries when an action writes the table — no manual\n<code>revalidate()</code>, ever,</li>\n<li><strong>auto-batch</strong> N component reads of the same query into one.</li>\n</ul>\n<pre><code class=\"language-ts\">import { db } from &quot;@junejs/db&quot;;\n\nconst listUsers = cache(() =&gt; db.table(&quot;users&quot;).all());   // tagged [table:users]\nawait invokeAction(&quot;createUser&quot;, { name: &quot;Ada&quot; });        // writes users →\nawait listUsers();                                        // cache MISS — fresh data\n</code></pre>\n<p>The magic is a property of the <em>trace contract</em>, not of Juno: bring Prisma or\nDrizzle untouched (Tier 1), run them over the same connection to share config\n(Tier 2), or add a thin shim that names the table and get the same magic\n(Tier 3).</p>\n<h2>Why it matters</h2>\n<p>Stale-cache bugs are the classic failure mode of hand-wired <code>revalidate()</code>\ncalls — and agents writing code forget them exactly as often as humans do.\nMaking invalidation a consequence of the write removes the class of bug. And\nbecause <code>db</code> rides an ambient scope rather than <code>ctx</code>, the code an agent writes\nto read or write data is identical in a route and in a helper — nothing to\nthread, nothing to mock.</p>\n"
  },
  {
    "slug": "features-styling",
    "data": {
      "title": "Styling: global CSS, Tailwind, CSS Modules",
      "nav": "Styling",
      "description": "app/global.css is auto-linked (no import); Tailwind v4 is the blessed default; *.module.css scopes deterministically. Dev serves readable CSS with HMR, build ships hashed, immutable, minified.",
      "date": "2026-06-13",
      "section": "Features",
      "order": "8.5"
    },
    "body": "## The feature\n\nStyling is convention, not configuration. Drop `app/global.css` and June links\nit on every page — no `import \"./global.css\"`, no `<link>` to wire:\n\n```css\n/* app/global.css — auto-linked, recompiled on save, shipped as a static asset */\nbody { font-family: system-ui; }\n```\n\nPlain CSS works with zero dependencies. That's the floor; the rest is opt-in.\n\n## Tailwind v4 (the blessed default)\n\nOpt into Tailwind by importing it from `global.css` — June compiles it through\nthe app's **own** Tailwind v4 (resolved from your `node_modules`, never bundled\ninto the framework), so you control the version and Tailwind auto-detects the\nclasses it needs:\n\n```css\n/* app/global.css */\n@import \"tailwindcss\";\n```\n\nThe starter ships this line and Tailwind-styled pages. Plain CSS and Tailwind\ncoexist in the same file.\n\n## CSS Modules\n\nName a file `*.module.css` and `import` it for locally-scoped class names:\n\n```css\n/* app/Card.module.css */\n.card { padding: 1rem; border: 1px solid #ddd }\n.title { font-weight: 600 }\n```\n\n```tsx\nimport styles from \"./Card.module.css\";\n\nexport default function Card() {\n  return <div className={styles.card}><h2 className={styles.title}>…</h2></div>;\n}\n```\n\n`styles.card` resolves to a scoped name like `card_9e43e788`. The scoped name is\na **deterministic hash** of the file path + class — *not* a bundler-internal\ncounter — so the name is byte-identical in dev SSR, the production worker build,\nthe client islands bundle, and whether you run on Bun or Node. That identity is\nwhat makes hydration safe: the server and client always agree on the class.\n\nThe full CSS-Modules surface works:\n\n```css\n.btn { composes: base from \"./shared.module.css\"; color: white }\n:global(.prose a) { text-decoration: underline }\n```\n\n- **`composes`** pulls in another local (even across files); `styles.btn`\n  becomes the merged `\"btn_… base_…\"`.\n- **`:global(...)`** opts a selector out of scoping for genuinely global rules.\n\nFor app-wide globals, prefer `app/global.css`; reach for `:global` only inside a\nmodule.\n\n## What the build ships\n\nDev serves CSS readable and hot — edit a stylesheet and the page swaps it\nwithout a reload. `june build` optimizes it:\n\n- every stylesheet is **content-hashed** and emitted under the reserved\n  `/_june/` prefix (e.g. `/_june/global.a1b2c3d4.css`), so it can be served\n  **immutable** — the browser never revalidates it, and a change ships a new\n  filename;\n- output is **minified** with Lightning CSS (the same engine Tailwind v4\n  optimizes with), so global CSS, Tailwind, and CSS-Modules sheets all come out\n  consistently small.\n\nThe `/_june/` prefix is June's alone — your routes and your own asset paths can\nnever collide with framework output.\n\n## Why it matters\n\nCSS is the one surface that is purely for humans — it never touches the agent\nprojections (`.md` / `.json` / `/mcp`). So the rule is \"stay out of the way\":\nthe floor is plain CSS with no build to think about, the ceiling is Tailwind +\nscoped modules with deterministic names an agent can reason about, and the\ndev↔build difference is only the asset URL — never the rendered markup, which is\nwhat keeps the dual-audience parity contract intact.\n",
    "original": "---\ntitle: \"Styling: global CSS, Tailwind, CSS Modules\"\nnav: \"Styling\"\ndescription: app/global.css is auto-linked (no import); Tailwind v4 is the blessed default; *.module.css scopes deterministically. Dev serves readable CSS with HMR, build ships hashed, immutable, minified.\ndate: 2026-06-13\nsection: Features\norder: \"8.5\"\n---\n## The feature\n\nStyling is convention, not configuration. Drop `app/global.css` and June links\nit on every page — no `import \"./global.css\"`, no `<link>` to wire:\n\n```css\n/* app/global.css — auto-linked, recompiled on save, shipped as a static asset */\nbody { font-family: system-ui; }\n```\n\nPlain CSS works with zero dependencies. That's the floor; the rest is opt-in.\n\n## Tailwind v4 (the blessed default)\n\nOpt into Tailwind by importing it from `global.css` — June compiles it through\nthe app's **own** Tailwind v4 (resolved from your `node_modules`, never bundled\ninto the framework), so you control the version and Tailwind auto-detects the\nclasses it needs:\n\n```css\n/* app/global.css */\n@import \"tailwindcss\";\n```\n\nThe starter ships this line and Tailwind-styled pages. Plain CSS and Tailwind\ncoexist in the same file.\n\n## CSS Modules\n\nName a file `*.module.css` and `import` it for locally-scoped class names:\n\n```css\n/* app/Card.module.css */\n.card { padding: 1rem; border: 1px solid #ddd }\n.title { font-weight: 600 }\n```\n\n```tsx\nimport styles from \"./Card.module.css\";\n\nexport default function Card() {\n  return <div className={styles.card}><h2 className={styles.title}>…</h2></div>;\n}\n```\n\n`styles.card` resolves to a scoped name like `card_9e43e788`. The scoped name is\na **deterministic hash** of the file path + class — *not* a bundler-internal\ncounter — so the name is byte-identical in dev SSR, the production worker build,\nthe client islands bundle, and whether you run on Bun or Node. That identity is\nwhat makes hydration safe: the server and client always agree on the class.\n\nThe full CSS-Modules surface works:\n\n```css\n.btn { composes: base from \"./shared.module.css\"; color: white }\n:global(.prose a) { text-decoration: underline }\n```\n\n- **`composes`** pulls in another local (even across files); `styles.btn`\n  becomes the merged `\"btn_… base_…\"`.\n- **`:global(...)`** opts a selector out of scoping for genuinely global rules.\n\nFor app-wide globals, prefer `app/global.css`; reach for `:global` only inside a\nmodule.\n\n## What the build ships\n\nDev serves CSS readable and hot — edit a stylesheet and the page swaps it\nwithout a reload. `june build` optimizes it:\n\n- every stylesheet is **content-hashed** and emitted under the reserved\n  `/_june/` prefix (e.g. `/_june/global.a1b2c3d4.css`), so it can be served\n  **immutable** — the browser never revalidates it, and a change ships a new\n  filename;\n- output is **minified** with Lightning CSS (the same engine Tailwind v4\n  optimizes with), so global CSS, Tailwind, and CSS-Modules sheets all come out\n  consistently small.\n\nThe `/_june/` prefix is June's alone — your routes and your own asset paths can\nnever collide with framework output.\n\n## Why it matters\n\nCSS is the one surface that is purely for humans — it never touches the agent\nprojections (`.md` / `.json` / `/mcp`). So the rule is \"stay out of the way\":\nthe floor is plain CSS with no build to think about, the ceiling is Tailwind +\nscoped modules with deterministic names an agent can reason about, and the\ndev↔build difference is only the asset URL — never the rendered markup, which is\nwhat keeps the dual-audience parity contract intact.\n",
    "html": "<h2>The feature</h2>\n<p>Styling is convention, not configuration. Drop <code>app/global.css</code> and June links\nit on every page — no <code>import &quot;./global.css&quot;</code>, no <code>&lt;link&gt;</code> to wire:</p>\n<pre><code class=\"language-css\">/* app/global.css — auto-linked, recompiled on save, shipped as a static asset */\nbody { font-family: system-ui; }\n</code></pre>\n<p>Plain CSS works with zero dependencies. That&#39;s the floor; the rest is opt-in.</p>\n<h2>Tailwind v4 (the blessed default)</h2>\n<p>Opt into Tailwind by importing it from <code>global.css</code> — June compiles it through\nthe app&#39;s <strong>own</strong> Tailwind v4 (resolved from your <code>node_modules</code>, never bundled\ninto the framework), so you control the version and Tailwind auto-detects the\nclasses it needs:</p>\n<pre><code class=\"language-css\">/* app/global.css */\n@import &quot;tailwindcss&quot;;\n</code></pre>\n<p>The starter ships this line and Tailwind-styled pages. Plain CSS and Tailwind\ncoexist in the same file.</p>\n<h2>CSS Modules</h2>\n<p>Name a file <code>*.module.css</code> and <code>import</code> it for locally-scoped class names:</p>\n<pre><code class=\"language-css\">/* app/Card.module.css */\n.card { padding: 1rem; border: 1px solid #ddd }\n.title { font-weight: 600 }\n</code></pre>\n<pre><code class=\"language-tsx\">import styles from &quot;./Card.module.css&quot;;\n\nexport default function Card() {\n  return &lt;div className={styles.card}&gt;&lt;h2 className={styles.title}&gt;…&lt;/h2&gt;&lt;/div&gt;;\n}\n</code></pre>\n<p><code>styles.card</code> resolves to a scoped name like <code>card_9e43e788</code>. The scoped name is\na <strong>deterministic hash</strong> of the file path + class — <em>not</em> a bundler-internal\ncounter — so the name is byte-identical in dev SSR, the production worker build,\nthe client islands bundle, and whether you run on Bun or Node. That identity is\nwhat makes hydration safe: the server and client always agree on the class.</p>\n<p>The full CSS-Modules surface works:</p>\n<pre><code class=\"language-css\">.btn { composes: base from &quot;./shared.module.css&quot;; color: white }\n:global(.prose a) { text-decoration: underline }\n</code></pre>\n<ul>\n<li><strong><code>composes</code></strong> pulls in another local (even across files); <code>styles.btn</code>\nbecomes the merged <code>&quot;btn_… base_…&quot;</code>.</li>\n<li><strong><code>:global(...)</code></strong> opts a selector out of scoping for genuinely global rules.</li>\n</ul>\n<p>For app-wide globals, prefer <code>app/global.css</code>; reach for <code>:global</code> only inside a\nmodule.</p>\n<h2>What the build ships</h2>\n<p>Dev serves CSS readable and hot — edit a stylesheet and the page swaps it\nwithout a reload. <code>june build</code> optimizes it:</p>\n<ul>\n<li>every stylesheet is <strong>content-hashed</strong> and emitted under the reserved\n<code>/_june/</code> prefix (e.g. <code>/_june/global.a1b2c3d4.css</code>), so it can be served\n<strong>immutable</strong> — the browser never revalidates it, and a change ships a new\nfilename;</li>\n<li>output is <strong>minified</strong> with Lightning CSS (the same engine Tailwind v4\noptimizes with), so global CSS, Tailwind, and CSS-Modules sheets all come out\nconsistently small.</li>\n</ul>\n<p>The <code>/_june/</code> prefix is June&#39;s alone — your routes and your own asset paths can\nnever collide with framework output.</p>\n<h2>Why it matters</h2>\n<p>CSS is the one surface that is purely for humans — it never touches the agent\nprojections (<code>.md</code> / <code>.json</code> / <code>/mcp</code>). So the rule is &quot;stay out of the way&quot;:\nthe floor is plain CSS with no build to think about, the ceiling is Tailwind +\nscoped modules with deterministic names an agent can reason about, and the\ndev↔build difference is only the asset URL — never the rendered markup, which is\nwhat keeps the dual-audience parity contract intact.</p>\n"
  },
  {
    "slug": "01-getting-started",
    "data": {
      "title": "Getting started",
      "description": "Scaffold, develop, build, and deploy a June app in four commands.",
      "date": "2026-06-12"
    },
    "body": "## Install\n\n```bash\nnpm create june my-app          # canonical (package: create-june)\ncd my-app && npm run dev\n```\n\nThe framework packages are `@junejs/core` (the contract layer: the page module, `defineAction()`,\nislands) and `@junejs/cli` (the `june` command) — NOT `june`\n(an unrelated npm package), not `junejs`, and not `@june/*` (that scope isn't\nours).\n\n## The loop\n\n```bash\njune dev          # dev server (Bun/Node host)\njune build        # Workers bundle: dist/worker.js + prerendered assets\njune deploy       # build + wrangler upload (--dry-run validates only)\njune gen          # freeze content/**/*.md → app/_content.ts\njune info         # show routes + the agent surface\n```\n\n`june build` freezes what the dev server discovers at request time: routes →\na static manifest, `june.config.ts` → literals, `content/**/*.md` →\n`app/_content.ts`, pages exporting `prerender = true` → static files.\n\n## Where things go\n\n```\napp/            file-system routes: page.tsx, layout.tsx, [param]/\napp/_client.*   the client entry — its presence turns on islands hydration\ncontent/        markdown collections (frozen at build, verbatim .md projections)\njune.config.ts  site metadata, agent-surface switches, resources\n```\n",
    "original": "---\ntitle: Getting started\ndescription: Scaffold, develop, build, and deploy a June app in four commands.\ndate: 2026-06-12\n---\n## Install\n\n```bash\nnpm create june my-app          # canonical (package: create-june)\ncd my-app && npm run dev\n```\n\nThe framework packages are `@junejs/core` (the contract layer: the page module, `defineAction()`,\nislands) and `@junejs/cli` (the `june` command) — NOT `june`\n(an unrelated npm package), not `junejs`, and not `@june/*` (that scope isn't\nours).\n\n## The loop\n\n```bash\njune dev          # dev server (Bun/Node host)\njune build        # Workers bundle: dist/worker.js + prerendered assets\njune deploy       # build + wrangler upload (--dry-run validates only)\njune gen          # freeze content/**/*.md → app/_content.ts\njune info         # show routes + the agent surface\n```\n\n`june build` freezes what the dev server discovers at request time: routes →\na static manifest, `june.config.ts` → literals, `content/**/*.md` →\n`app/_content.ts`, pages exporting `prerender = true` → static files.\n\n## Where things go\n\n```\napp/            file-system routes: page.tsx, layout.tsx, [param]/\napp/_client.*   the client entry — its presence turns on islands hydration\ncontent/        markdown collections (frozen at build, verbatim .md projections)\njune.config.ts  site metadata, agent-surface switches, resources\n```\n",
    "html": "<h2>Install</h2>\n<pre><code class=\"language-bash\">npm create june my-app          # canonical (package: create-june)\ncd my-app &amp;&amp; npm run dev\n</code></pre>\n<p>The framework packages are <code>@junejs/core</code> (the contract layer: the page module, <code>defineAction()</code>,\nislands) and <code>@junejs/cli</code> (the <code>june</code> command) — NOT <code>june</code>\n(an unrelated npm package), not <code>junejs</code>, and not <code>@june/*</code> (that scope isn&#39;t\nours).</p>\n<h2>The loop</h2>\n<pre><code class=\"language-bash\">june dev          # dev server (Bun/Node host)\njune build        # Workers bundle: dist/worker.js + prerendered assets\njune deploy       # build + wrangler upload (--dry-run validates only)\njune gen          # freeze content/**/*.md → app/_content.ts\njune info         # show routes + the agent surface\n</code></pre>\n<p><code>june build</code> freezes what the dev server discovers at request time: routes →\na static manifest, <code>june.config.ts</code> → literals, <code>content/**/*.md</code> →\n<code>app/_content.ts</code>, pages exporting <code>prerender = true</code> → static files.</p>\n<h2>Where things go</h2>\n<pre><code>app/            file-system routes: page.tsx, layout.tsx, [param]/\napp/_client.*   the client entry — its presence turns on islands hydration\ncontent/        markdown collections (frozen at build, verbatim .md projections)\njune.config.ts  site metadata, agent-surface switches, resources\n</code></pre>\n"
  },
  {
    "slug": "02-dual-audience",
    "data": {
      "title": "Dual audience",
      "description": "One page definition serves humans (HTML) and agents (markdown, JSON, MCP).",
      "date": "2026-06-12"
    },
    "body": "## One definition, four surfaces\n\nA page's default export is the view; named exports configure the other\nsurfaces. `.json` auto-derives from the loader data, so you only write what\nyou customize:\n\n```tsx\nimport type { RouteContext, Loaded } from \"@junejs/core/route\";\n\nexport const loader = (ctx: RouteContext<{ slug: string }>) => fetchPost(ctx.params.slug);\n\nexport default function Post(post: Loaded<typeof loader>) {  // GET /posts/x   → streamed HTML\n  return <article>…</article>;\n}\n\nexport const md = (post: Loaded<typeof loader>) => post.original;   // GET /posts/x.md → AUTHORED markdown\nexport const metadata = (post: Loaded<typeof loader>) => ({ title: post.title });\n// GET /posts/x.json → the loader data, auto-derived (no export needed)\n```\n\nThe view receives loader data as **props**, the same shape the other three\nprojections take it (`md(data)`, `metadata(data)`, `.json = data`): one loader,\nfour surfaces, each a pure function of the data — nothing to drift. Deep child\ncomponents can reach the same data without prop-drilling via the escape-hatch\nhook `useLoaderData<typeof loader>()` (also catches the Remix muscle-memory).\n\nThe `.md` projection serves the file you wrote, byte-for-byte — frontmatter\nincluded. Most frameworks reconstruct markdown from rendered HTML; June serves\nthe source, so there is nothing to drift. Actions are the capability surface —\neach `defineAction()` is an MCP tool at `/mcp` (and a browser WebMCP tool).\n\n## What agents discover automatically\n\n- `/llms.txt` — route map + the framework's canonical names\n- `/sitemap.xml`, `/robots.txt`, `/.well-known/api-catalog`\n- `/mcp` — your `defineAction()`s as MCP tools: one definition is a UI server\n  action AND an MCP tool AND a browser WebMCP tool\n\n## Actions are one gate\n\n`defineAction({ id, description, input, run })` — `run(input, ctx)` receives\nthe same context (user, session, resources) whether the caller is your UI or\nan agent at `/mcp`. One authorization model, two kinds of callers.\n\n## The posture\n\nHuman-intent optimizations (Speculation-Rules prerender, View Transitions)\napply to the HTML surface only; agent surfaces stay deliberately plain.\nMachines don't need view transitions.\n",
    "original": "---\ntitle: Dual audience\ndescription: One page definition serves humans (HTML) and agents (markdown, JSON, MCP).\ndate: 2026-06-12\n---\n## One definition, four surfaces\n\nA page's default export is the view; named exports configure the other\nsurfaces. `.json` auto-derives from the loader data, so you only write what\nyou customize:\n\n```tsx\nimport type { RouteContext, Loaded } from \"@junejs/core/route\";\n\nexport const loader = (ctx: RouteContext<{ slug: string }>) => fetchPost(ctx.params.slug);\n\nexport default function Post(post: Loaded<typeof loader>) {  // GET /posts/x   → streamed HTML\n  return <article>…</article>;\n}\n\nexport const md = (post: Loaded<typeof loader>) => post.original;   // GET /posts/x.md → AUTHORED markdown\nexport const metadata = (post: Loaded<typeof loader>) => ({ title: post.title });\n// GET /posts/x.json → the loader data, auto-derived (no export needed)\n```\n\nThe view receives loader data as **props**, the same shape the other three\nprojections take it (`md(data)`, `metadata(data)`, `.json = data`): one loader,\nfour surfaces, each a pure function of the data — nothing to drift. Deep child\ncomponents can reach the same data without prop-drilling via the escape-hatch\nhook `useLoaderData<typeof loader>()` (also catches the Remix muscle-memory).\n\nThe `.md` projection serves the file you wrote, byte-for-byte — frontmatter\nincluded. Most frameworks reconstruct markdown from rendered HTML; June serves\nthe source, so there is nothing to drift. Actions are the capability surface —\neach `defineAction()` is an MCP tool at `/mcp` (and a browser WebMCP tool).\n\n## What agents discover automatically\n\n- `/llms.txt` — route map + the framework's canonical names\n- `/sitemap.xml`, `/robots.txt`, `/.well-known/api-catalog`\n- `/mcp` — your `defineAction()`s as MCP tools: one definition is a UI server\n  action AND an MCP tool AND a browser WebMCP tool\n\n## Actions are one gate\n\n`defineAction({ id, description, input, run })` — `run(input, ctx)` receives\nthe same context (user, session, resources) whether the caller is your UI or\nan agent at `/mcp`. One authorization model, two kinds of callers.\n\n## The posture\n\nHuman-intent optimizations (Speculation-Rules prerender, View Transitions)\napply to the HTML surface only; agent surfaces stay deliberately plain.\nMachines don't need view transitions.\n",
    "html": "<h2>One definition, four surfaces</h2>\n<p>A page&#39;s default export is the view; named exports configure the other\nsurfaces. <code>.json</code> auto-derives from the loader data, so you only write what\nyou customize:</p>\n<pre><code class=\"language-tsx\">import type { RouteContext, Loaded } from &quot;@junejs/core/route&quot;;\n\nexport const loader = (ctx: RouteContext&lt;{ slug: string }&gt;) =&gt; fetchPost(ctx.params.slug);\n\nexport default function Post(post: Loaded&lt;typeof loader&gt;) {  // GET /posts/x   → streamed HTML\n  return &lt;article&gt;…&lt;/article&gt;;\n}\n\nexport const md = (post: Loaded&lt;typeof loader&gt;) =&gt; post.original;   // GET /posts/x.md → AUTHORED markdown\nexport const metadata = (post: Loaded&lt;typeof loader&gt;) =&gt; ({ title: post.title });\n// GET /posts/x.json → the loader data, auto-derived (no export needed)\n</code></pre>\n<p>The view receives loader data as <strong>props</strong>, the same shape the other three\nprojections take it (<code>md(data)</code>, <code>metadata(data)</code>, <code>.json = data</code>): one loader,\nfour surfaces, each a pure function of the data — nothing to drift. Deep child\ncomponents can reach the same data without prop-drilling via the escape-hatch\nhook <code>useLoaderData&lt;typeof loader&gt;()</code> (also catches the Remix muscle-memory).</p>\n<p>The <code>.md</code> projection serves the file you wrote, byte-for-byte — frontmatter\nincluded. Most frameworks reconstruct markdown from rendered HTML; June serves\nthe source, so there is nothing to drift. Actions are the capability surface —\neach <code>defineAction()</code> is an MCP tool at <code>/mcp</code> (and a browser WebMCP tool).</p>\n<h2>What agents discover automatically</h2>\n<ul>\n<li><code>/llms.txt</code> — route map + the framework&#39;s canonical names</li>\n<li><code>/sitemap.xml</code>, <code>/robots.txt</code>, <code>/.well-known/api-catalog</code></li>\n<li><code>/mcp</code> — your <code>defineAction()</code>s as MCP tools: one definition is a UI server\naction AND an MCP tool AND a browser WebMCP tool</li>\n</ul>\n<h2>Actions are one gate</h2>\n<p><code>defineAction({ id, description, input, run })</code> — <code>run(input, ctx)</code> receives\nthe same context (user, session, resources) whether the caller is your UI or\nan agent at <code>/mcp</code>. One authorization model, two kinds of callers.</p>\n<h2>The posture</h2>\n<p>Human-intent optimizations (Speculation-Rules prerender, View Transitions)\napply to the HTML surface only; agent surfaces stay deliberately plain.\nMachines don&#39;t need view transitions.</p>\n"
  },
  {
    "slug": "03-build-and-deploy",
    "data": {
      "title": "Build & deploy",
      "description": "What june build generates, how prerender works, and the deploy adapters.",
      "date": "2026-06-12"
    },
    "body": "## `june build`\n\nEverything dynamic about dev gets frozen at build time:\n\n| input | output |\n| --- | --- |\n| `app/**/page.*` | static route manifest (+ nested layout chains) |\n| `june.config.ts` | inlined literals (site, speculation, agent config) |\n| `content/<dir>/*.md` | `app/_content.ts` — the manifest dev AND workerd read |\n| a page exporting `prerender = true` | static html/md/json rendered THROUGH the built worker |\n| `app/_client.*` | the islands bundle served at `/client.js` |\n\nThe bundle is self-contained ESM (rolldown; conditions baked in — workerd has\nno runtime conditions). Binary assets and `build.external` packages stay\nexternal for wrangler's own rules.\n\n## Prerender\n\nOpt-in per route, static routes only. What ships is what was tested: the\nbuild renders through the bundled worker, not a parallel code path. Don't opt\nin routes whose output depends on `ctx.url.origin`.\n\n## `june deploy`\n\nThe verb is fixed; the target is an adapter — `workers` today, more later.\n`june deploy` = build → resolve wrangler config → upload → URL.\n`--dry-run` validates everything without uploading — it's also the CI test.\n",
    "original": "---\ntitle: Build & deploy\ndescription: What june build generates, how prerender works, and the deploy adapters.\ndate: 2026-06-12\n---\n## `june build`\n\nEverything dynamic about dev gets frozen at build time:\n\n| input | output |\n| --- | --- |\n| `app/**/page.*` | static route manifest (+ nested layout chains) |\n| `june.config.ts` | inlined literals (site, speculation, agent config) |\n| `content/<dir>/*.md` | `app/_content.ts` — the manifest dev AND workerd read |\n| a page exporting `prerender = true` | static html/md/json rendered THROUGH the built worker |\n| `app/_client.*` | the islands bundle served at `/client.js` |\n\nThe bundle is self-contained ESM (rolldown; conditions baked in — workerd has\nno runtime conditions). Binary assets and `build.external` packages stay\nexternal for wrangler's own rules.\n\n## Prerender\n\nOpt-in per route, static routes only. What ships is what was tested: the\nbuild renders through the bundled worker, not a parallel code path. Don't opt\nin routes whose output depends on `ctx.url.origin`.\n\n## `june deploy`\n\nThe verb is fixed; the target is an adapter — `workers` today, more later.\n`june deploy` = build → resolve wrangler config → upload → URL.\n`--dry-run` validates everything without uploading — it's also the CI test.\n",
    "html": "<h2><code>june build</code></h2>\n<p>Everything dynamic about dev gets frozen at build time:</p>\n<table>\n<thead>\n<tr>\n<th>input</th>\n<th>output</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>app/**/page.*</code></td>\n<td>static route manifest (+ nested layout chains)</td>\n</tr>\n<tr>\n<td><code>june.config.ts</code></td>\n<td>inlined literals (site, speculation, agent config)</td>\n</tr>\n<tr>\n<td><code>content/&lt;dir&gt;/*.md</code></td>\n<td><code>app/_content.ts</code> — the manifest dev AND workerd read</td>\n</tr>\n<tr>\n<td>a page exporting <code>prerender = true</code></td>\n<td>static html/md/json rendered THROUGH the built worker</td>\n</tr>\n<tr>\n<td><code>app/_client.*</code></td>\n<td>the islands bundle served at <code>/client.js</code></td>\n</tr>\n</tbody></table>\n<p>The bundle is self-contained ESM (rolldown; conditions baked in — workerd has\nno runtime conditions). Binary assets and <code>build.external</code> packages stay\nexternal for wrangler&#39;s own rules.</p>\n<h2>Prerender</h2>\n<p>Opt-in per route, static routes only. What ships is what was tested: the\nbuild renders through the bundled worker, not a parallel code path. Don&#39;t opt\nin routes whose output depends on <code>ctx.url.origin</code>.</p>\n<h2><code>june deploy</code></h2>\n<p>The verb is fixed; the target is an adapter — <code>workers</code> today, more later.\n<code>june deploy</code> = build → resolve wrangler config → upload → URL.\n<code>--dry-run</code> validates everything without uploading — it&#39;s also the CI test.</p>\n"
  },
  {
    "slug": "04-deployment",
    "data": {
      "title": "Deployment",
      "description": "One portable core, one host interface, one adapter per target — Workers today, self-hosted and Vercel on the same seam.",
      "date": "2026-06-12"
    },
    "body": "## The design\n\nJune separates what renders from where it runs:\n\n- **The framework core is portable.** Routes, projections, actions, the agent\n  surface — all of it speaks Web standards (`Request` in, `Response` out) and\n  assumes no runtime. It is the same code on every target; deployment never\n  changes behavior.\n- **The host interface is the seam.** Everything standards don't cover —\n  binding a port, opening a local database, spawning a module — lives behind\n  one small interface. A deploy target is an *implementation of that\n  interface*, not a fork of the framework.\n\nSo adding a target means writing an adapter, and an adapter cannot diverge\nfrom the framework: dev, self-hosted, and edge all run the same render core\n(our parity suite asserts it byte-for-byte).\n\n## Targets\n\n| target | how | status |\n| --- | --- | --- |\n| **Cloudflare Workers** | `june deploy` — Rolldown bundle + frozen manifest, wrangler upload | shipped; `--dry-run` runs in CI |\n| **Self-hosted (Bun / Node)** | the host layer serves the same pipeline over `Bun.serve` / `node:http` | serving layer CI-tested on both runtimes; a production `june start` verb is roadmap |\n| **Vercel** | an adapter over the same host seam (Build Output API) | planned |\n| **Own Rust+V8 runtime** | the experimental flagship — V8 snapshot boot, un-bundled dev | experimental track ([Runtime](/docs/features-runtime)) |\n\nThe deploy verb is fixed; the target is configuration. Resources follow the\nsame model: declare `db` / `blob` / `kv` once, and each target maps them to\nits native services (D1/R2/KV on Workers, SQLite + disk locally).\n\n## Why this shape\n\nDeployment lock-in is usually accidental: the framework leaks its favorite\nruntime's APIs until leaving costs a rewrite. June's rule — core speaks\nstandards, hosts implement one interface — makes the exit door part of the\narchitecture. The target you pick is a decision you can revisit.\n\n## Honest notes\n\n- Workers is the one target you can deploy to today; the table above is the\n  design and its current state, not a promise that everything works now.\n- Node self-hosting note: the Node host actually exceeds the others in one\n  spot — real RFC 8297 `103 Early Hints` interim responses.\n",
    "original": "---\ntitle: \"Deployment\"\ndescription: One portable core, one host interface, one adapter per target — Workers today, self-hosted and Vercel on the same seam.\ndate: 2026-06-12\n---\n## The design\n\nJune separates what renders from where it runs:\n\n- **The framework core is portable.** Routes, projections, actions, the agent\n  surface — all of it speaks Web standards (`Request` in, `Response` out) and\n  assumes no runtime. It is the same code on every target; deployment never\n  changes behavior.\n- **The host interface is the seam.** Everything standards don't cover —\n  binding a port, opening a local database, spawning a module — lives behind\n  one small interface. A deploy target is an *implementation of that\n  interface*, not a fork of the framework.\n\nSo adding a target means writing an adapter, and an adapter cannot diverge\nfrom the framework: dev, self-hosted, and edge all run the same render core\n(our parity suite asserts it byte-for-byte).\n\n## Targets\n\n| target | how | status |\n| --- | --- | --- |\n| **Cloudflare Workers** | `june deploy` — Rolldown bundle + frozen manifest, wrangler upload | shipped; `--dry-run` runs in CI |\n| **Self-hosted (Bun / Node)** | the host layer serves the same pipeline over `Bun.serve` / `node:http` | serving layer CI-tested on both runtimes; a production `june start` verb is roadmap |\n| **Vercel** | an adapter over the same host seam (Build Output API) | planned |\n| **Own Rust+V8 runtime** | the experimental flagship — V8 snapshot boot, un-bundled dev | experimental track ([Runtime](/docs/features-runtime)) |\n\nThe deploy verb is fixed; the target is configuration. Resources follow the\nsame model: declare `db` / `blob` / `kv` once, and each target maps them to\nits native services (D1/R2/KV on Workers, SQLite + disk locally).\n\n## Why this shape\n\nDeployment lock-in is usually accidental: the framework leaks its favorite\nruntime's APIs until leaving costs a rewrite. June's rule — core speaks\nstandards, hosts implement one interface — makes the exit door part of the\narchitecture. The target you pick is a decision you can revisit.\n\n## Honest notes\n\n- Workers is the one target you can deploy to today; the table above is the\n  design and its current state, not a promise that everything works now.\n- Node self-hosting note: the Node host actually exceeds the others in one\n  spot — real RFC 8297 `103 Early Hints` interim responses.\n",
    "html": "<h2>The design</h2>\n<p>June separates what renders from where it runs:</p>\n<ul>\n<li><strong>The framework core is portable.</strong> Routes, projections, actions, the agent\nsurface — all of it speaks Web standards (<code>Request</code> in, <code>Response</code> out) and\nassumes no runtime. It is the same code on every target; deployment never\nchanges behavior.</li>\n<li><strong>The host interface is the seam.</strong> Everything standards don&#39;t cover —\nbinding a port, opening a local database, spawning a module — lives behind\none small interface. A deploy target is an <em>implementation of that\ninterface</em>, not a fork of the framework.</li>\n</ul>\n<p>So adding a target means writing an adapter, and an adapter cannot diverge\nfrom the framework: dev, self-hosted, and edge all run the same render core\n(our parity suite asserts it byte-for-byte).</p>\n<h2>Targets</h2>\n<table>\n<thead>\n<tr>\n<th>target</th>\n<th>how</th>\n<th>status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Cloudflare Workers</strong></td>\n<td><code>june deploy</code> — Rolldown bundle + frozen manifest, wrangler upload</td>\n<td>shipped; <code>--dry-run</code> runs in CI</td>\n</tr>\n<tr>\n<td><strong>Self-hosted (Bun / Node)</strong></td>\n<td>the host layer serves the same pipeline over <code>Bun.serve</code> / <code>node:http</code></td>\n<td>serving layer CI-tested on both runtimes; a production <code>june start</code> verb is roadmap</td>\n</tr>\n<tr>\n<td><strong>Vercel</strong></td>\n<td>an adapter over the same host seam (Build Output API)</td>\n<td>planned</td>\n</tr>\n<tr>\n<td><strong>Own Rust+V8 runtime</strong></td>\n<td>the experimental flagship — V8 snapshot boot, un-bundled dev</td>\n<td>experimental track (<a href=\"/docs/features-runtime\">Runtime</a>)</td>\n</tr>\n</tbody></table>\n<p>The deploy verb is fixed; the target is configuration. Resources follow the\nsame model: declare <code>db</code> / <code>blob</code> / <code>kv</code> once, and each target maps them to\nits native services (D1/R2/KV on Workers, SQLite + disk locally).</p>\n<h2>Why this shape</h2>\n<p>Deployment lock-in is usually accidental: the framework leaks its favorite\nruntime&#39;s APIs until leaving costs a rewrite. June&#39;s rule — core speaks\nstandards, hosts implement one interface — makes the exit door part of the\narchitecture. The target you pick is a decision you can revisit.</p>\n<h2>Honest notes</h2>\n<ul>\n<li>Workers is the one target you can deploy to today; the table above is the\ndesign and its current state, not a promise that everything works now.</li>\n<li>Node self-hosting note: the Node host actually exceeds the others in one\nspot — real RFC 8297 <code>103 Early Hints</code> interim responses.</li>\n</ul>\n"
  },
  {
    "slug": "features-app-router",
    "data": {
      "title": "App Router: the filesystem is the route table",
      "nav": "App Router",
      "description": "page.tsx is a route, [slug] is a param, (group) shapes the tree without the URL, _anything colocates — one matcher drives dev and the build.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "6"
    },
    "body": "## The conventions\n\n| entry | role |\n| --- | --- |\n| `page.tsx` / `index.tsx` | the route leaf — a page module (default-export view + loader/json/md) |\n| `layout.tsx` | wraps everything below this segment ([Layouts](/docs/features-layouts)) |\n| `not-found.tsx` | the 404 page (app root today; per-segment is wired in the router, pipeline next) |\n| `[slug]/` | dynamic segment → `ctx.params.slug` |\n| `[[slug]]/` | optional segment — matches with the param set or absent |\n| `[...path]/` | catch-all → `ctx.params.path` (joined string) |\n| `[[...path]]/` | optional catch-all — also matches zero segments |\n| `(group)/` | route group — shapes the filesystem, invisible in the URL |\n| `_anything` | never a route — colocate components, tests, models freely |\n\nMatching priority at each level is **exact static > `[param]` >\n`[...catchAll]`**, with backtracking: a static directory that dead-ends\ndoesn't shadow a dynamic sibling.\n\n## One matcher, no drift\n\nThe same matcher drives `june dev` (resolved per request from the\nfilesystem) and `june build` (frozen into the worker manifest) — so the\nroutes you see in dev are the routes that deploy, by construction rather\nthan by discipline. `june info` prints the resolved table at any moment.\n\nThis very site is the demo: `/docs/[slug]` and `/blog/[slug]` are dynamic\nsegments, the docs sidebar is a nested layout, and `_content.ts` /\n`_sections.ts` sit colocated inside `app/` without ever becoming routes.\n\n## Stated limits\n\n`loading.tsx` and `error.tsx` are recognized by the matcher (each segment's\nspecial files travel with the match) but not yet wired: loading needs\nstreamed Suspense, segment error boundaries are a later milestone — see\n[RSC](/docs/features-rsc) for the same honesty about streaming.\n\n## Why it matters\n\nA route table you write by hand is a route table that drifts. Conventions a\nhuman can't misread are also conventions a coding agent can't misread — the\nfilesystem is the one source both audiences already know how to navigate.\n",
    "original": "---\ntitle: \"App Router: the filesystem is the route table\"\nnav: \"App Router\"\ndescription: page.tsx is a route, [slug] is a param, (group) shapes the tree without the URL, _anything colocates — one matcher drives dev and the build.\ndate: 2026-06-12\nsection: Features\norder: \"6\"\n---\n## The conventions\n\n| entry | role |\n| --- | --- |\n| `page.tsx` / `index.tsx` | the route leaf — a page module (default-export view + loader/json/md) |\n| `layout.tsx` | wraps everything below this segment ([Layouts](/docs/features-layouts)) |\n| `not-found.tsx` | the 404 page (app root today; per-segment is wired in the router, pipeline next) |\n| `[slug]/` | dynamic segment → `ctx.params.slug` |\n| `[[slug]]/` | optional segment — matches with the param set or absent |\n| `[...path]/` | catch-all → `ctx.params.path` (joined string) |\n| `[[...path]]/` | optional catch-all — also matches zero segments |\n| `(group)/` | route group — shapes the filesystem, invisible in the URL |\n| `_anything` | never a route — colocate components, tests, models freely |\n\nMatching priority at each level is **exact static > `[param]` >\n`[...catchAll]`**, with backtracking: a static directory that dead-ends\ndoesn't shadow a dynamic sibling.\n\n## One matcher, no drift\n\nThe same matcher drives `june dev` (resolved per request from the\nfilesystem) and `june build` (frozen into the worker manifest) — so the\nroutes you see in dev are the routes that deploy, by construction rather\nthan by discipline. `june info` prints the resolved table at any moment.\n\nThis very site is the demo: `/docs/[slug]` and `/blog/[slug]` are dynamic\nsegments, the docs sidebar is a nested layout, and `_content.ts` /\n`_sections.ts` sit colocated inside `app/` without ever becoming routes.\n\n## Stated limits\n\n`loading.tsx` and `error.tsx` are recognized by the matcher (each segment's\nspecial files travel with the match) but not yet wired: loading needs\nstreamed Suspense, segment error boundaries are a later milestone — see\n[RSC](/docs/features-rsc) for the same honesty about streaming.\n\n## Why it matters\n\nA route table you write by hand is a route table that drifts. Conventions a\nhuman can't misread are also conventions a coding agent can't misread — the\nfilesystem is the one source both audiences already know how to navigate.\n",
    "html": "<h2>The conventions</h2>\n<table>\n<thead>\n<tr>\n<th>entry</th>\n<th>role</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>page.tsx</code> / <code>index.tsx</code></td>\n<td>the route leaf — a page module (default-export view + loader/json/md)</td>\n</tr>\n<tr>\n<td><code>layout.tsx</code></td>\n<td>wraps everything below this segment (<a href=\"/docs/features-layouts\">Layouts</a>)</td>\n</tr>\n<tr>\n<td><code>not-found.tsx</code></td>\n<td>the 404 page (app root today; per-segment is wired in the router, pipeline next)</td>\n</tr>\n<tr>\n<td><code>[slug]/</code></td>\n<td>dynamic segment → <code>ctx.params.slug</code></td>\n</tr>\n<tr>\n<td><code>[[slug]]/</code></td>\n<td>optional segment — matches with the param set or absent</td>\n</tr>\n<tr>\n<td><code>[...path]/</code></td>\n<td>catch-all → <code>ctx.params.path</code> (joined string)</td>\n</tr>\n<tr>\n<td><code>[[...path]]/</code></td>\n<td>optional catch-all — also matches zero segments</td>\n</tr>\n<tr>\n<td><code>(group)/</code></td>\n<td>route group — shapes the filesystem, invisible in the URL</td>\n</tr>\n<tr>\n<td><code>_anything</code></td>\n<td>never a route — colocate components, tests, models freely</td>\n</tr>\n</tbody></table>\n<p>Matching priority at each level is <strong>exact static &gt; <code>[param]</code> &gt;\n<code>[...catchAll]</code></strong>, with backtracking: a static directory that dead-ends\ndoesn&#39;t shadow a dynamic sibling.</p>\n<h2>One matcher, no drift</h2>\n<p>The same matcher drives <code>june dev</code> (resolved per request from the\nfilesystem) and <code>june build</code> (frozen into the worker manifest) — so the\nroutes you see in dev are the routes that deploy, by construction rather\nthan by discipline. <code>june info</code> prints the resolved table at any moment.</p>\n<p>This very site is the demo: <code>/docs/[slug]</code> and <code>/blog/[slug]</code> are dynamic\nsegments, the docs sidebar is a nested layout, and <code>_content.ts</code> /\n<code>_sections.ts</code> sit colocated inside <code>app/</code> without ever becoming routes.</p>\n<h2>Stated limits</h2>\n<p><code>loading.tsx</code> and <code>error.tsx</code> are recognized by the matcher (each segment&#39;s\nspecial files travel with the match) but not yet wired: loading needs\nstreamed Suspense, segment error boundaries are a later milestone — see\n<a href=\"/docs/features-rsc\">RSC</a> for the same honesty about streaming.</p>\n<h2>Why it matters</h2>\n<p>A route table you write by hand is a route table that drifts. Conventions a\nhuman can&#39;t misread are also conventions a coding agent can&#39;t misread — the\nfilesystem is the one source both audiences already know how to navigate.</p>\n"
  },
  {
    "slug": "features-cli",
    "data": {
      "title": "The june CLI",
      "nav": "CLI",
      "description": "Five verbs — dev, build, deploy, gen, info — installed locally by the scaffold, version-pinned per project, with --dry-run as the CI contract.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "14"
    },
    "body": "## The feature\n\n```bash\nnpm create june my-app    # scaffold (package: create-june) — wires the CLI locally\ncd my-app && npm run dev\n```\n\nThe scaffold installs `june` as a local devDependency: no global install,\nversion pinned per project, reproducible for every collaborator — human or\nagent. Then the whole loop is five verbs:\n\n```bash\njune dev          # dev server (Bun/Node host), zero config\njune build        # Workers bundle: dist/worker.js + prerendered assets\njune deploy       # build → wrangler upload → URL (--dry-run validates only)\njune gen          # freeze content/**/*.md → app/_content.ts\njune info         # show routes + the agent surface\n```\n\n## The contract verbs\n\n- `june deploy --dry-run` runs the full build and config resolution without\n  uploading — it's also the CI test, so \"deployable\" is continuously asserted.\n- `june info` is the app's oracle: what routes exist, what tools an agent\n  would see at `/mcp`, what discovery endpoints are live. If `info` shows it,\n  it's served; if it doesn't, it isn't.\n\n## Canonical names\n\nScaffold with `create-june`; the packages are `@junejs/core` and\n`@junejs/cli`. NOT `june` (an unrelated npm package), not `junejs`, not\n`@june/*` (not our scope). The same warning ships in every app's `/llms.txt`\nso agents installing dependencies get it from the source.\n\n## Why it matters\n\nA CLI you \"own\" the moment you scaffold — local, pinned, reproducible — is\nthe difference between onboarding that's one command and onboarding that's a\nwiki page. The dry-run contract keeps deployability a tested property instead\nof a Friday surprise.\n",
    "original": "---\ntitle: \"The june CLI\"\nnav: \"CLI\"\ndescription: Five verbs — dev, build, deploy, gen, info — installed locally by the scaffold, version-pinned per project, with --dry-run as the CI contract.\ndate: 2026-06-12\nsection: Features\norder: \"14\"\n---\n## The feature\n\n```bash\nnpm create june my-app    # scaffold (package: create-june) — wires the CLI locally\ncd my-app && npm run dev\n```\n\nThe scaffold installs `june` as a local devDependency: no global install,\nversion pinned per project, reproducible for every collaborator — human or\nagent. Then the whole loop is five verbs:\n\n```bash\njune dev          # dev server (Bun/Node host), zero config\njune build        # Workers bundle: dist/worker.js + prerendered assets\njune deploy       # build → wrangler upload → URL (--dry-run validates only)\njune gen          # freeze content/**/*.md → app/_content.ts\njune info         # show routes + the agent surface\n```\n\n## The contract verbs\n\n- `june deploy --dry-run` runs the full build and config resolution without\n  uploading — it's also the CI test, so \"deployable\" is continuously asserted.\n- `june info` is the app's oracle: what routes exist, what tools an agent\n  would see at `/mcp`, what discovery endpoints are live. If `info` shows it,\n  it's served; if it doesn't, it isn't.\n\n## Canonical names\n\nScaffold with `create-june`; the packages are `@junejs/core` and\n`@junejs/cli`. NOT `june` (an unrelated npm package), not `junejs`, not\n`@june/*` (not our scope). The same warning ships in every app's `/llms.txt`\nso agents installing dependencies get it from the source.\n\n## Why it matters\n\nA CLI you \"own\" the moment you scaffold — local, pinned, reproducible — is\nthe difference between onboarding that's one command and onboarding that's a\nwiki page. The dry-run contract keeps deployability a tested property instead\nof a Friday surprise.\n",
    "html": "<h2>The feature</h2>\n<pre><code class=\"language-bash\">npm create june my-app    # scaffold (package: create-june) — wires the CLI locally\ncd my-app &amp;&amp; npm run dev\n</code></pre>\n<p>The scaffold installs <code>june</code> as a local devDependency: no global install,\nversion pinned per project, reproducible for every collaborator — human or\nagent. Then the whole loop is five verbs:</p>\n<pre><code class=\"language-bash\">june dev          # dev server (Bun/Node host), zero config\njune build        # Workers bundle: dist/worker.js + prerendered assets\njune deploy       # build → wrangler upload → URL (--dry-run validates only)\njune gen          # freeze content/**/*.md → app/_content.ts\njune info         # show routes + the agent surface\n</code></pre>\n<h2>The contract verbs</h2>\n<ul>\n<li><code>june deploy --dry-run</code> runs the full build and config resolution without\nuploading — it&#39;s also the CI test, so &quot;deployable&quot; is continuously asserted.</li>\n<li><code>june info</code> is the app&#39;s oracle: what routes exist, what tools an agent\nwould see at <code>/mcp</code>, what discovery endpoints are live. If <code>info</code> shows it,\nit&#39;s served; if it doesn&#39;t, it isn&#39;t.</li>\n</ul>\n<h2>Canonical names</h2>\n<p>Scaffold with <code>create-june</code>; the packages are <code>@junejs/core</code> and\n<code>@junejs/cli</code>. NOT <code>june</code> (an unrelated npm package), not <code>junejs</code>, not\n<code>@june/*</code> (not our scope). The same warning ships in every app&#39;s <code>/llms.txt</code>\nso agents installing dependencies get it from the source.</p>\n<h2>Why it matters</h2>\n<p>A CLI you &quot;own&quot; the moment you scaffold — local, pinned, reproducible — is\nthe difference between onboarding that&#39;s one command and onboarding that&#39;s a\nwiki page. The dry-run contract keeps deployability a tested property instead\nof a Friday surprise.</p>\n"
  },
  {
    "slug": "features-dx",
    "data": {
      "title": "DX: a loop with no assembly",
      "nav": "DX",
      "description": "Zero-config dev, dev/built parity by construction, an oracle for every artifact — and push-based HMR on the experimental runtime track.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "13"
    },
    "body": "## Bun-first toolchain, runtime-agnostic core\n\nOne tool — Bun — runs install, test, and dev; the no-glue philosophy applied\nto the toolchain. Meanwhile `@junejs/core` assumes no runtime at all, which\nis what lets the same app serve from Bun, Node, and workerd (see\n[Runtime](/docs/features-runtime)).\n\n## Zero-config dev\n\n`june dev` runs with nothing installed around it: declared resources get\nlocal defaults — `db` is an embedded SQLite file, `blob` a local directory,\n`kv` in-memory. No Docker, no service to start. The same `sqlite()` declaration\nbecomes a Cloudflare binding (D1, R2, KV) on deploy, so the SAME code runs on\nWorkers.\n\n## Parity by construction\n\nDev and the built worker share one render core — projections, discovery,\ndocument, layout wrapping are the same code. `june build` freezes what dev\ndiscovers at request time (routes, config, content, prerender) and the golden\nparity tests assert byte-level agreement. \"Works in dev, breaks deployed\" is\ntreated as a framework bug, not your bug.\n\n## An oracle for every artifact\n\n```bash\njune info        # routes + the agent surface (tools, discovery endpoints)\njune gen         # freeze content — and see exactly what the manifest holds\njune deploy --dry-run   # the full build + config resolution, no upload — CI runs this\n```\n\nConventions a coding agent can't misread are conventions a human can't\nmisread either: file-system routing, plain SQL migrations, one config file\nthat exists to turn things off.\n\n## Reload on save — server and browser\n\n`june dev` watches your app and restarts the server on change — content\nedits regenerate the frozen manifest first, so the next request is fresh.\nA restart is the *honest* reload on a JS host (a module cache can't be\nselectively invalidated without lying about state); `--no-watch` opts out.\n\nThe browser follows by itself: every dev page holds an SSE connection to the\ndev server, and the restart *is* the signal — the connection drops, the page\nreconnects, and reloads on success. Dev-only by construction (injected by\nthe dev server wrapper, never the render pipeline), so it cannot leak into a\nbuild or skew dev/built parity.\n\nPush-based HMR belongs to the experimental Rust+V8 runtime track: on save,\nthe server re-renders and pushes the RSC diff over the live channel —\nmeasured median **73ms** from save to flight pushed, isolate rebuild at\n**16ms** ([benchmarks](/benchmarks), `scripts/bench-hmr.ts`). The push loop\nis the same machinery as production live-RSC — dev speed and the live\nfeatures are one investment.\n\n## Why it matters\n\nDX debt compounds: every adapter you hand-wire is something to misconfigure,\nand now there are two kinds of authors (humans and agents) misconfiguring it.\nThe framework's job is to make the default loop need no assembly at all.\n",
    "original": "---\ntitle: \"DX: a loop with no assembly\"\nnav: \"DX\"\ndescription: Zero-config dev, dev/built parity by construction, an oracle for every artifact — and push-based HMR on the experimental runtime track.\ndate: 2026-06-12\nsection: Features\norder: \"13\"\n---\n## Bun-first toolchain, runtime-agnostic core\n\nOne tool — Bun — runs install, test, and dev; the no-glue philosophy applied\nto the toolchain. Meanwhile `@junejs/core` assumes no runtime at all, which\nis what lets the same app serve from Bun, Node, and workerd (see\n[Runtime](/docs/features-runtime)).\n\n## Zero-config dev\n\n`june dev` runs with nothing installed around it: declared resources get\nlocal defaults — `db` is an embedded SQLite file, `blob` a local directory,\n`kv` in-memory. No Docker, no service to start. The same `sqlite()` declaration\nbecomes a Cloudflare binding (D1, R2, KV) on deploy, so the SAME code runs on\nWorkers.\n\n## Parity by construction\n\nDev and the built worker share one render core — projections, discovery,\ndocument, layout wrapping are the same code. `june build` freezes what dev\ndiscovers at request time (routes, config, content, prerender) and the golden\nparity tests assert byte-level agreement. \"Works in dev, breaks deployed\" is\ntreated as a framework bug, not your bug.\n\n## An oracle for every artifact\n\n```bash\njune info        # routes + the agent surface (tools, discovery endpoints)\njune gen         # freeze content — and see exactly what the manifest holds\njune deploy --dry-run   # the full build + config resolution, no upload — CI runs this\n```\n\nConventions a coding agent can't misread are conventions a human can't\nmisread either: file-system routing, plain SQL migrations, one config file\nthat exists to turn things off.\n\n## Reload on save — server and browser\n\n`june dev` watches your app and restarts the server on change — content\nedits regenerate the frozen manifest first, so the next request is fresh.\nA restart is the *honest* reload on a JS host (a module cache can't be\nselectively invalidated without lying about state); `--no-watch` opts out.\n\nThe browser follows by itself: every dev page holds an SSE connection to the\ndev server, and the restart *is* the signal — the connection drops, the page\nreconnects, and reloads on success. Dev-only by construction (injected by\nthe dev server wrapper, never the render pipeline), so it cannot leak into a\nbuild or skew dev/built parity.\n\nPush-based HMR belongs to the experimental Rust+V8 runtime track: on save,\nthe server re-renders and pushes the RSC diff over the live channel —\nmeasured median **73ms** from save to flight pushed, isolate rebuild at\n**16ms** ([benchmarks](/benchmarks), `scripts/bench-hmr.ts`). The push loop\nis the same machinery as production live-RSC — dev speed and the live\nfeatures are one investment.\n\n## Why it matters\n\nDX debt compounds: every adapter you hand-wire is something to misconfigure,\nand now there are two kinds of authors (humans and agents) misconfiguring it.\nThe framework's job is to make the default loop need no assembly at all.\n",
    "html": "<h2>Bun-first toolchain, runtime-agnostic core</h2>\n<p>One tool — Bun — runs install, test, and dev; the no-glue philosophy applied\nto the toolchain. Meanwhile <code>@junejs/core</code> assumes no runtime at all, which\nis what lets the same app serve from Bun, Node, and workerd (see\n<a href=\"/docs/features-runtime\">Runtime</a>).</p>\n<h2>Zero-config dev</h2>\n<p><code>june dev</code> runs with nothing installed around it: declared resources get\nlocal defaults — <code>db</code> is an embedded SQLite file, <code>blob</code> a local directory,\n<code>kv</code> in-memory. No Docker, no service to start. The same <code>sqlite()</code> declaration\nbecomes a Cloudflare binding (D1, R2, KV) on deploy, so the SAME code runs on\nWorkers.</p>\n<h2>Parity by construction</h2>\n<p>Dev and the built worker share one render core — projections, discovery,\ndocument, layout wrapping are the same code. <code>june build</code> freezes what dev\ndiscovers at request time (routes, config, content, prerender) and the golden\nparity tests assert byte-level agreement. &quot;Works in dev, breaks deployed&quot; is\ntreated as a framework bug, not your bug.</p>\n<h2>An oracle for every artifact</h2>\n<pre><code class=\"language-bash\">june info        # routes + the agent surface (tools, discovery endpoints)\njune gen         # freeze content — and see exactly what the manifest holds\njune deploy --dry-run   # the full build + config resolution, no upload — CI runs this\n</code></pre>\n<p>Conventions a coding agent can&#39;t misread are conventions a human can&#39;t\nmisread either: file-system routing, plain SQL migrations, one config file\nthat exists to turn things off.</p>\n<h2>Reload on save — server and browser</h2>\n<p><code>june dev</code> watches your app and restarts the server on change — content\nedits regenerate the frozen manifest first, so the next request is fresh.\nA restart is the <em>honest</em> reload on a JS host (a module cache can&#39;t be\nselectively invalidated without lying about state); <code>--no-watch</code> opts out.</p>\n<p>The browser follows by itself: every dev page holds an SSE connection to the\ndev server, and the restart <em>is</em> the signal — the connection drops, the page\nreconnects, and reloads on success. Dev-only by construction (injected by\nthe dev server wrapper, never the render pipeline), so it cannot leak into a\nbuild or skew dev/built parity.</p>\n<p>Push-based HMR belongs to the experimental Rust+V8 runtime track: on save,\nthe server re-renders and pushes the RSC diff over the live channel —\nmeasured median <strong>73ms</strong> from save to flight pushed, isolate rebuild at\n<strong>16ms</strong> (<a href=\"/benchmarks\">benchmarks</a>, <code>scripts/bench-hmr.ts</code>). The push loop\nis the same machinery as production live-RSC — dev speed and the live\nfeatures are one investment.</p>\n<h2>Why it matters</h2>\n<p>DX debt compounds: every adapter you hand-wire is something to misconfigure,\nand now there are two kinds of authors (humans and agents) misconfiguring it.\nThe framework&#39;s job is to make the default loop need no assembly at all.</p>\n"
  },
  {
    "slug": "features-islands",
    "data": {
      "title": "Islands: zero client JS by default",
      "nav": "Islands",
      "description": "Pages ship no JavaScript unless a subtree opts in — one <Island> hydrates against an explicit registry while the rest stays server-rendered HTML.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "8"
    },
    "body": "## The feature\n\nJune pages are server-rendered and ship **zero client JavaScript** — this\nsite's pages, including the one you're reading, send none. Interactivity is\nopt-in per subtree:\n\n```tsx\n// app/page.tsx — server component (the default export IS the view)\nimport { Island } from \"@junejs/core/islands\";\n\nexport default function Page() {\n  return (\n    <main>\n      <h1>Mostly static</h1>\n      <Island name=\"Counter\" props={{ start: 0 }} />  {/* the ONE live subtree */}\n    </main>\n  );\n}\n```\n\n```ts\n// app/_client.ts — the explicit registry; its presence is what enables /client.js\nimport { hydrateIslands } from \"@junejs/core/islands-client\";\nimport { Counter } from \"./Counter\";\n\nhydrateIslands({ Counter });\n```\n\n`<Island>` server-renders a marker carrying the registry name + JSON props;\n`hydrateIslands()` scans the page and `hydrateRoot()`s each marker. No\n`app/_client.*` file → no bundle, no script tag, output unchanged.\n\n## Try it\n\n```bash\nnpm create june my-app    # the starter ships a working Counter island\n```\n\nThe starter's counter — and the framework's own e2e test — run the REAL\nproduction bundle a `june build` ships against the built worker's SSR markup:\nwhat's tested is what deploys.\n\n## Why it matters\n\nHover-prerendering and View Transitions already make navigation feel\ninstant with no client code ([Navigation](/docs/features-navigation)).\nIslands keep the budget at zero until a specific subtree earns it.\n",
    "original": "---\ntitle: \"Islands: zero client JS by default\"\nnav: \"Islands\"\ndescription: Pages ship no JavaScript unless a subtree opts in — one <Island> hydrates against an explicit registry while the rest stays server-rendered HTML.\ndate: 2026-06-12\nsection: Features\norder: \"8\"\n---\n## The feature\n\nJune pages are server-rendered and ship **zero client JavaScript** — this\nsite's pages, including the one you're reading, send none. Interactivity is\nopt-in per subtree:\n\n```tsx\n// app/page.tsx — server component (the default export IS the view)\nimport { Island } from \"@junejs/core/islands\";\n\nexport default function Page() {\n  return (\n    <main>\n      <h1>Mostly static</h1>\n      <Island name=\"Counter\" props={{ start: 0 }} />  {/* the ONE live subtree */}\n    </main>\n  );\n}\n```\n\n```ts\n// app/_client.ts — the explicit registry; its presence is what enables /client.js\nimport { hydrateIslands } from \"@junejs/core/islands-client\";\nimport { Counter } from \"./Counter\";\n\nhydrateIslands({ Counter });\n```\n\n`<Island>` server-renders a marker carrying the registry name + JSON props;\n`hydrateIslands()` scans the page and `hydrateRoot()`s each marker. No\n`app/_client.*` file → no bundle, no script tag, output unchanged.\n\n## Try it\n\n```bash\nnpm create june my-app    # the starter ships a working Counter island\n```\n\nThe starter's counter — and the framework's own e2e test — run the REAL\nproduction bundle a `june build` ships against the built worker's SSR markup:\nwhat's tested is what deploys.\n\n## Why it matters\n\nHover-prerendering and View Transitions already make navigation feel\ninstant with no client code ([Navigation](/docs/features-navigation)).\nIslands keep the budget at zero until a specific subtree earns it.\n",
    "html": "<h2>The feature</h2>\n<p>June pages are server-rendered and ship <strong>zero client JavaScript</strong> — this\nsite&#39;s pages, including the one you&#39;re reading, send none. Interactivity is\nopt-in per subtree:</p>\n<pre><code class=\"language-tsx\">// app/page.tsx — server component (the default export IS the view)\nimport { Island } from &quot;@junejs/core/islands&quot;;\n\nexport default function Page() {\n  return (\n    &lt;main&gt;\n      &lt;h1&gt;Mostly static&lt;/h1&gt;\n      &lt;Island name=&quot;Counter&quot; props={{ start: 0 }} /&gt;  {/* the ONE live subtree */}\n    &lt;/main&gt;\n  );\n}\n</code></pre>\n<pre><code class=\"language-ts\">// app/_client.ts — the explicit registry; its presence is what enables /client.js\nimport { hydrateIslands } from &quot;@junejs/core/islands-client&quot;;\nimport { Counter } from &quot;./Counter&quot;;\n\nhydrateIslands({ Counter });\n</code></pre>\n<p><code>&lt;Island&gt;</code> server-renders a marker carrying the registry name + JSON props;\n<code>hydrateIslands()</code> scans the page and <code>hydrateRoot()</code>s each marker. No\n<code>app/_client.*</code> file → no bundle, no script tag, output unchanged.</p>\n<h2>Try it</h2>\n<pre><code class=\"language-bash\">npm create june my-app    # the starter ships a working Counter island\n</code></pre>\n<p>The starter&#39;s counter — and the framework&#39;s own e2e test — run the REAL\nproduction bundle a <code>june build</code> ships against the built worker&#39;s SSR markup:\nwhat&#39;s tested is what deploys.</p>\n<h2>Why it matters</h2>\n<p>Hover-prerendering and View Transitions already make navigation feel\ninstant with no client code (<a href=\"/docs/features-navigation\">Navigation</a>).\nIslands keep the budget at zero until a specific subtree earns it.</p>\n"
  },
  {
    "slug": "features-layouts",
    "data": {
      "title": "Layouts: nested, composed, frozen",
      "nav": "Layouts",
      "description": "layout.tsx wraps its segment and everything below; chains compose root → leaf, and the build freezes the same chain dev resolves.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "7"
    },
    "body": "## The feature\n\nA `layout.tsx` anywhere in `app/` wraps its segment and everything beneath\nit. Chains compose outside-in, root → leaf:\n\n```\napp/layout.tsx            ← nav + footer, on every page\n└── app/docs/layout.tsx   ← the docs sidebar, on every /docs/* page\n    └── app/docs/[slug]/page.tsx\n```\n\nA layout is a plain server component — `({ children }) => JSX` — with no\nregistration step: its presence in the directory is the wiring.\n\n```tsx\n// app/docs/layout.tsx (this site's actual sidebar)\nexport default function DocsLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <div data-layout=\"docs\">\n      <nav>{/* sections + short labels */}</nav>\n      <div>{children}</div>\n    </div>\n  );\n}\n```\n\n## Frozen, not re-derived\n\n`june build` freezes each route's layout chain into the worker manifest —\nthe deployed worker composes the exact chain dev resolved from the\nfilesystem. Our parity suite asserts the result byte-for-byte, which is how\na layout bug stays a bug you see in dev rather than one you meet in\nproduction.\n\n## See it on this page\n\nYou are inside two layouts right now: the root layout (the nav above, the\nfooter below) and the docs layout (the sidebar). The blog renders through\nthe same root layout with no docs sidebar — same mechanism, different\nsubtree. Zero client JavaScript is involved in any of it.\n\n## Why it matters\n\nShared chrome is the classic source of duplication (and of agent-written\npages that forget the wrapper). A layout that attaches by location makes\nthe right structure the path of least resistance — for both kinds of\nauthors.\n",
    "original": "---\ntitle: \"Layouts: nested, composed, frozen\"\nnav: \"Layouts\"\ndescription: layout.tsx wraps its segment and everything below; chains compose root → leaf, and the build freezes the same chain dev resolves.\ndate: 2026-06-12\nsection: Features\norder: \"7\"\n---\n## The feature\n\nA `layout.tsx` anywhere in `app/` wraps its segment and everything beneath\nit. Chains compose outside-in, root → leaf:\n\n```\napp/layout.tsx            ← nav + footer, on every page\n└── app/docs/layout.tsx   ← the docs sidebar, on every /docs/* page\n    └── app/docs/[slug]/page.tsx\n```\n\nA layout is a plain server component — `({ children }) => JSX` — with no\nregistration step: its presence in the directory is the wiring.\n\n```tsx\n// app/docs/layout.tsx (this site's actual sidebar)\nexport default function DocsLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <div data-layout=\"docs\">\n      <nav>{/* sections + short labels */}</nav>\n      <div>{children}</div>\n    </div>\n  );\n}\n```\n\n## Frozen, not re-derived\n\n`june build` freezes each route's layout chain into the worker manifest —\nthe deployed worker composes the exact chain dev resolved from the\nfilesystem. Our parity suite asserts the result byte-for-byte, which is how\na layout bug stays a bug you see in dev rather than one you meet in\nproduction.\n\n## See it on this page\n\nYou are inside two layouts right now: the root layout (the nav above, the\nfooter below) and the docs layout (the sidebar). The blog renders through\nthe same root layout with no docs sidebar — same mechanism, different\nsubtree. Zero client JavaScript is involved in any of it.\n\n## Why it matters\n\nShared chrome is the classic source of duplication (and of agent-written\npages that forget the wrapper). A layout that attaches by location makes\nthe right structure the path of least resistance — for both kinds of\nauthors.\n",
    "html": "<h2>The feature</h2>\n<p>A <code>layout.tsx</code> anywhere in <code>app/</code> wraps its segment and everything beneath\nit. Chains compose outside-in, root → leaf:</p>\n<pre><code>app/layout.tsx            ← nav + footer, on every page\n└── app/docs/layout.tsx   ← the docs sidebar, on every /docs/* page\n    └── app/docs/[slug]/page.tsx\n</code></pre>\n<p>A layout is a plain server component — <code>({ children }) =&gt; JSX</code> — with no\nregistration step: its presence in the directory is the wiring.</p>\n<pre><code class=\"language-tsx\">// app/docs/layout.tsx (this site&#39;s actual sidebar)\nexport default function DocsLayout({ children }: { children: React.ReactNode }) {\n  return (\n    &lt;div data-layout=&quot;docs&quot;&gt;\n      &lt;nav&gt;{/* sections + short labels */}&lt;/nav&gt;\n      &lt;div&gt;{children}&lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\n</code></pre>\n<h2>Frozen, not re-derived</h2>\n<p><code>june build</code> freezes each route&#39;s layout chain into the worker manifest —\nthe deployed worker composes the exact chain dev resolved from the\nfilesystem. Our parity suite asserts the result byte-for-byte, which is how\na layout bug stays a bug you see in dev rather than one you meet in\nproduction.</p>\n<h2>See it on this page</h2>\n<p>You are inside two layouts right now: the root layout (the nav above, the\nfooter below) and the docs layout (the sidebar). The blog renders through\nthe same root layout with no docs sidebar — same mechanism, different\nsubtree. Zero client JavaScript is involved in any of it.</p>\n<h2>Why it matters</h2>\n<p>Shared chrome is the classic source of duplication (and of agent-written\npages that forget the wrapper). A layout that attaches by location makes\nthe right structure the path of least resistance — for both kinds of\nauthors.</p>\n"
  },
  {
    "slug": "features-llms-txt",
    "data": {
      "title": "Built-in llms.txt",
      "nav": "llms.txt",
      "description": "The agent discovery surface — llms.txt, sitemap, robots, api-catalog, and per-route manifests — derives from your routes automatically.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "2"
    },
    "body": "## The feature\n\nAn agent landing on a June app finds its way without scraping. These derive\nfrom the route graph and your actions — you author none of them:\n\n| surface | what an agent learns |\n| --- | --- |\n| `/llms.txt` | the route map + each route's markdown projection |\n| `/sitemap.xml`, `/robots.txt` | the classic crawler contract |\n| `/.well-known/api-catalog` | machine-readable API listing |\n| `/mcp` | your actions as MCP tools an agent can call |\n| `Link` response header | discovery advertised on every HTML response |\n\nIt's on by default and one switch turns it all off (`agent: { enabled:\nfalse }`) — the framework's defaults philosophy: removable, not assembly\nrequired.\n\n## Try it on this site\n\n```bash\ncurl https://june.build/llms.txt\ncurl https://june.build/.well-known/api-catalog\ncurl -sI https://june.build/why | grep -i '^link:'\n```\n\nThe `llms.txt` here also carries the framework's canonical names — which\npackage is ours (`@junejs/core`, `create-june`) and which similarly-named\nones are not. Agents misinstalling lookalike packages is a real failure mode;\nthe discovery surface is where you correct it.\n\n## Why it matters\n\nDiscovery is the cheapest half of being agent-ready: if an agent's first\nfetch answers \"what is here and how do I read it,\" everything downstream\n(markdown projections, MCP tools) gets found instead of guessed at.\n",
    "original": "---\ntitle: \"Built-in llms.txt\"\nnav: \"llms.txt\"\ndescription: The agent discovery surface — llms.txt, sitemap, robots, api-catalog, and per-route manifests — derives from your routes automatically.\ndate: 2026-06-12\nsection: Features\norder: \"2\"\n---\n## The feature\n\nAn agent landing on a June app finds its way without scraping. These derive\nfrom the route graph and your actions — you author none of them:\n\n| surface | what an agent learns |\n| --- | --- |\n| `/llms.txt` | the route map + each route's markdown projection |\n| `/sitemap.xml`, `/robots.txt` | the classic crawler contract |\n| `/.well-known/api-catalog` | machine-readable API listing |\n| `/mcp` | your actions as MCP tools an agent can call |\n| `Link` response header | discovery advertised on every HTML response |\n\nIt's on by default and one switch turns it all off (`agent: { enabled:\nfalse }`) — the framework's defaults philosophy: removable, not assembly\nrequired.\n\n## Try it on this site\n\n```bash\ncurl https://june.build/llms.txt\ncurl https://june.build/.well-known/api-catalog\ncurl -sI https://june.build/why | grep -i '^link:'\n```\n\nThe `llms.txt` here also carries the framework's canonical names — which\npackage is ours (`@junejs/core`, `create-june`) and which similarly-named\nones are not. Agents misinstalling lookalike packages is a real failure mode;\nthe discovery surface is where you correct it.\n\n## Why it matters\n\nDiscovery is the cheapest half of being agent-ready: if an agent's first\nfetch answers \"what is here and how do I read it,\" everything downstream\n(markdown projections, MCP tools) gets found instead of guessed at.\n",
    "html": "<h2>The feature</h2>\n<p>An agent landing on a June app finds its way without scraping. These derive\nfrom the route graph and your actions — you author none of them:</p>\n<table>\n<thead>\n<tr>\n<th>surface</th>\n<th>what an agent learns</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/llms.txt</code></td>\n<td>the route map + each route&#39;s markdown projection</td>\n</tr>\n<tr>\n<td><code>/sitemap.xml</code>, <code>/robots.txt</code></td>\n<td>the classic crawler contract</td>\n</tr>\n<tr>\n<td><code>/.well-known/api-catalog</code></td>\n<td>machine-readable API listing</td>\n</tr>\n<tr>\n<td><code>/mcp</code></td>\n<td>your actions as MCP tools an agent can call</td>\n</tr>\n<tr>\n<td><code>Link</code> response header</td>\n<td>discovery advertised on every HTML response</td>\n</tr>\n</tbody></table>\n<p>It&#39;s on by default and one switch turns it all off (<code>agent: { enabled: false }</code>) — the framework&#39;s defaults philosophy: removable, not assembly\nrequired.</p>\n<h2>Try it on this site</h2>\n<pre><code class=\"language-bash\">curl https://june.build/llms.txt\ncurl https://june.build/.well-known/api-catalog\ncurl -sI https://june.build/why | grep -i &#39;^link:&#39;\n</code></pre>\n<p>The <code>llms.txt</code> here also carries the framework&#39;s canonical names — which\npackage is ours (<code>@junejs/core</code>, <code>create-june</code>) and which similarly-named\nones are not. Agents misinstalling lookalike packages is a real failure mode;\nthe discovery surface is where you correct it.</p>\n<h2>Why it matters</h2>\n<p>Discovery is the cheapest half of being agent-ready: if an agent&#39;s first\nfetch answers &quot;what is here and how do I read it,&quot; everything downstream\n(markdown projections, MCP tools) gets found instead of guessed at.</p>\n"
  },
  {
    "slug": "features-markdown",
    "data": {
      "title": "Markdown without drift",
      "nav": "Markdown",
      "description": "Append .md to any page and get markdown — for authored content it's your source file byte-for-byte, never a lossy HTML reconstruction.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "3"
    },
    "body": "## The feature\n\nEvery route projects a markdown surface: `GET /why.md`, `GET\n/docs/<slug>.md`. For content-backed pages the projection serves `original` —\nthe file you wrote, **byte for byte, frontmatter included**:\n\n```\ncontent/posts/*.md  →  june gen  →  app/_content.ts  →  POSTS / DOCS\n                                         │\n        HTML view ◄──── one manifest ────┼──── .md projection (original, verbatim)\n                                         └──── search_site / get_page MCP tools\n```\n\nDev and the built worker read the SAME frozen manifest, so there is no\n\"works locally, differs deployed\" for content. Most frameworks reconstruct\nmarkdown from rendered HTML; June serves the source, so a diff against your\nrepo is empty.\n\n## Try it on this page\n\n```bash\ncurl -s https://june.build/docs/features-markdown.md\n```\n\nThat response IS this file in our repo — the same bytes `git show` would give\nyou. Our site tests assert it with strict equality, not a contains.\n\n## Why it matters\n\nMarkdown is the densest, least ambiguous surface you can serve an agent.\nServing the authored source means what an agent reads is exactly what you\nwrote — and frontmatter arrives as structured metadata instead of being\nboiled away by a renderer.\n",
    "original": "---\ntitle: \"Markdown without drift\"\nnav: \"Markdown\"\ndescription: Append .md to any page and get markdown — for authored content it's your source file byte-for-byte, never a lossy HTML reconstruction.\ndate: 2026-06-12\nsection: Features\norder: \"3\"\n---\n## The feature\n\nEvery route projects a markdown surface: `GET /why.md`, `GET\n/docs/<slug>.md`. For content-backed pages the projection serves `original` —\nthe file you wrote, **byte for byte, frontmatter included**:\n\n```\ncontent/posts/*.md  →  june gen  →  app/_content.ts  →  POSTS / DOCS\n                                         │\n        HTML view ◄──── one manifest ────┼──── .md projection (original, verbatim)\n                                         └──── search_site / get_page MCP tools\n```\n\nDev and the built worker read the SAME frozen manifest, so there is no\n\"works locally, differs deployed\" for content. Most frameworks reconstruct\nmarkdown from rendered HTML; June serves the source, so a diff against your\nrepo is empty.\n\n## Try it on this page\n\n```bash\ncurl -s https://june.build/docs/features-markdown.md\n```\n\nThat response IS this file in our repo — the same bytes `git show` would give\nyou. Our site tests assert it with strict equality, not a contains.\n\n## Why it matters\n\nMarkdown is the densest, least ambiguous surface you can serve an agent.\nServing the authored source means what an agent reads is exactly what you\nwrote — and frontmatter arrives as structured metadata instead of being\nboiled away by a renderer.\n",
    "html": "<h2>The feature</h2>\n<p>Every route projects a markdown surface: <code>GET /why.md</code>, <code>GET /docs/&lt;slug&gt;.md</code>. For content-backed pages the projection serves <code>original</code> —\nthe file you wrote, <strong>byte for byte, frontmatter included</strong>:</p>\n<pre><code>content/posts/*.md  →  june gen  →  app/_content.ts  →  POSTS / DOCS\n                                         │\n        HTML view ◄──── one manifest ────┼──── .md projection (original, verbatim)\n                                         └──── search_site / get_page MCP tools\n</code></pre>\n<p>Dev and the built worker read the SAME frozen manifest, so there is no\n&quot;works locally, differs deployed&quot; for content. Most frameworks reconstruct\nmarkdown from rendered HTML; June serves the source, so a diff against your\nrepo is empty.</p>\n<h2>Try it on this page</h2>\n<pre><code class=\"language-bash\">curl -s https://june.build/docs/features-markdown.md\n</code></pre>\n<p>That response IS this file in our repo — the same bytes <code>git show</code> would give\nyou. Our site tests assert it with strict equality, not a contains.</p>\n<h2>Why it matters</h2>\n<p>Markdown is the densest, least ambiguous surface you can serve an agent.\nServing the authored source means what an agent reads is exactly what you\nwrote — and frontmatter arrives as structured metadata instead of being\nboiled away by a renderer.</p>\n"
  },
  {
    "slug": "features-mcp",
    "data": {
      "title": "Built-in MCP",
      "nav": "MCP",
      "description": "Every June app is an MCP server — defineAction() is simultaneously a server action, an MCP tool, and a manifest entry, behind one authorization gate.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "1"
    },
    "body": "## The feature\n\nTurn on `agent.mcp` (it's on by default) and your app serves MCP at `/mcp` —\nno separate server, no adapter, no tool re-declaration:\n\n```ts\nimport { db } from \"@junejs/db\";\n\nexport const createUser = defineAction({\n  id: \"createUser\",\n  description: \"Create a user\",          // description → listed as an MCP tool\n  input: { type: \"object\", properties: { name: { type: \"string\" } }, required: [\"name\"] },\n  run: ({ name }, ctx) => {\n    // ctx carries the principal (identity) — the SAME ctx whether the caller\n    // is your React UI or an agent calling /mcp. Authorize here, once; the db\n    // is ambient (import { db }), not threaded through ctx.\n    return db.run(\"insert into users (name) values (?)\", [name]);\n  },\n});\n```\n\nOne `defineAction()` is simultaneously a **server action** (pass it to client\ncomponents as a prop), an **MCP tool** (auto-listed with its schema), and a\n**manifest entry** agents discover. There is no \"expose to agents\" step and\nno second permission system: `run(input, ctx)` is the only gate, so an agent\ncan never do anything your UI's authorization wouldn't allow.\n\n## Try it on this site\n\nThis site's search is an action. Call it the way an agent would:\n\n```bash\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/list\",\"params\":{}}'\n\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":2,\"method\":\"tools/call\",\"params\":{\"name\":\"search_site\",\"arguments\":{\"query\":\"cold start\"}}}'\n```\n\n## Why it matters\n\nTools are intent-shaped and policy-checked — never auto-generated CRUD. The\nagent surface is exactly as capable as you declared, and exactly as\nauthorized as your UI.\n",
    "original": "---\ntitle: \"Built-in MCP\"\nnav: \"MCP\"\ndescription: Every June app is an MCP server — defineAction() is simultaneously a server action, an MCP tool, and a manifest entry, behind one authorization gate.\ndate: 2026-06-12\nsection: Features\norder: \"1\"\n---\n## The feature\n\nTurn on `agent.mcp` (it's on by default) and your app serves MCP at `/mcp` —\nno separate server, no adapter, no tool re-declaration:\n\n```ts\nimport { db } from \"@junejs/db\";\n\nexport const createUser = defineAction({\n  id: \"createUser\",\n  description: \"Create a user\",          // description → listed as an MCP tool\n  input: { type: \"object\", properties: { name: { type: \"string\" } }, required: [\"name\"] },\n  run: ({ name }, ctx) => {\n    // ctx carries the principal (identity) — the SAME ctx whether the caller\n    // is your React UI or an agent calling /mcp. Authorize here, once; the db\n    // is ambient (import { db }), not threaded through ctx.\n    return db.run(\"insert into users (name) values (?)\", [name]);\n  },\n});\n```\n\nOne `defineAction()` is simultaneously a **server action** (pass it to client\ncomponents as a prop), an **MCP tool** (auto-listed with its schema), and a\n**manifest entry** agents discover. There is no \"expose to agents\" step and\nno second permission system: `run(input, ctx)` is the only gate, so an agent\ncan never do anything your UI's authorization wouldn't allow.\n\n## Try it on this site\n\nThis site's search is an action. Call it the way an agent would:\n\n```bash\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/list\",\"params\":{}}'\n\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":2,\"method\":\"tools/call\",\"params\":{\"name\":\"search_site\",\"arguments\":{\"query\":\"cold start\"}}}'\n```\n\n## Why it matters\n\nTools are intent-shaped and policy-checked — never auto-generated CRUD. The\nagent surface is exactly as capable as you declared, and exactly as\nauthorized as your UI.\n",
    "html": "<h2>The feature</h2>\n<p>Turn on <code>agent.mcp</code> (it&#39;s on by default) and your app serves MCP at <code>/mcp</code> —\nno separate server, no adapter, no tool re-declaration:</p>\n<pre><code class=\"language-ts\">import { db } from &quot;@junejs/db&quot;;\n\nexport const createUser = defineAction({\n  id: &quot;createUser&quot;,\n  description: &quot;Create a user&quot;,          // description → listed as an MCP tool\n  input: { type: &quot;object&quot;, properties: { name: { type: &quot;string&quot; } }, required: [&quot;name&quot;] },\n  run: ({ name }, ctx) =&gt; {\n    // ctx carries the principal (identity) — the SAME ctx whether the caller\n    // is your React UI or an agent calling /mcp. Authorize here, once; the db\n    // is ambient (import { db }), not threaded through ctx.\n    return db.run(&quot;insert into users (name) values (?)&quot;, [name]);\n  },\n});\n</code></pre>\n<p>One <code>defineAction()</code> is simultaneously a <strong>server action</strong> (pass it to client\ncomponents as a prop), an <strong>MCP tool</strong> (auto-listed with its schema), and a\n<strong>manifest entry</strong> agents discover. There is no &quot;expose to agents&quot; step and\nno second permission system: <code>run(input, ctx)</code> is the only gate, so an agent\ncan never do anything your UI&#39;s authorization wouldn&#39;t allow.</p>\n<h2>Try it on this site</h2>\n<p>This site&#39;s search is an action. Call it the way an agent would:</p>\n<pre><code class=\"language-bash\">curl -X POST https://june.build/mcp \\\n  -H &#39;content-type: application/json&#39; \\\n  -d &#39;{&quot;jsonrpc&quot;:&quot;2.0&quot;,&quot;id&quot;:1,&quot;method&quot;:&quot;tools/list&quot;,&quot;params&quot;:{}}&#39;\n\ncurl -X POST https://june.build/mcp \\\n  -H &#39;content-type: application/json&#39; \\\n  -d &#39;{&quot;jsonrpc&quot;:&quot;2.0&quot;,&quot;id&quot;:2,&quot;method&quot;:&quot;tools/call&quot;,&quot;params&quot;:{&quot;name&quot;:&quot;search_site&quot;,&quot;arguments&quot;:{&quot;query&quot;:&quot;cold start&quot;}}}&#39;\n</code></pre>\n<h2>Why it matters</h2>\n<p>Tools are intent-shaped and policy-checked — never auto-generated CRUD. The\nagent surface is exactly as capable as you declared, and exactly as\nauthorized as your UI.</p>\n"
  },
  {
    "slug": "features-navigation",
    "data": {
      "title": "Navigation: instant without a client router",
      "nav": "Navigation",
      "description": "Hover prerenders the next page (Speculation Rules), the click is zero-network, View Transitions animate the swap — the browser is the router.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "9"
    },
    "body": "## The feature\n\nJune ships no client router — and navigation is still instant, because the\nbrowser already has one. Every HTML page carries three things:\n\n1. **Speculation Rules.** A `<script type=\"speculationrules\">` block tells\n   the browser to prerender same-origin links on hover (`eagerness:\n   moderate`). By the time you click, the next page is already rendered —\n   the click is a zero-network activation.\n2. **A fallback for the rest.** Browsers without Speculation Rules get a\n   tiny inline script that turns `pointerover` into `<link rel=\"prefetch\">`\n   — progressive enhancement, not a polyfill bundle.\n3. **View Transitions.** `@view-transition { navigation: auto }` animates\n   the document swap (and respects `prefers-reduced-motion`). Cross-document\n   navigation stops looking like a full reload because the browser paints it\n   as one continuous surface.\n\nThe agent surfaces are deliberately excluded: `.md`, `.json`, and `/mcp`\nnever prerender — machines don't hover.\n\n## Try it right now\n\nThis page is the demo. Hover any link in the sidebar, watch the network\npanel prerender it, then click: no fetch, one smooth transition. Everything\nyou just experienced shipped as ~1KB of declarative rules — not a router,\nnot a virtual DOM, not a client cache to invalidate.\n\n## The posture\n\nClient routers exist to make navigation fast and transitions smooth. The\nplatform now does both natively, so June's default is the same choice it\nmakes everywhere ([Standards](/docs/features-web-standards)): use the\nbrowser's mechanism and ship nothing. This is also why a full document — not\na Flight payload ([RSC](/docs/features-rsc)) — is what crosses the wire: it\ncovers most sites with zero client state to manage.\n\nWhen an app-like surface genuinely needs in-memory state to survive a\nnavigation (a dashboard, an open websocket), the opt-in\n[Client Router](/docs/features-client-router) adds soft swaps as progressive\nenhancement — off by default, and this site keeps it off.\n\nConfigured by `speculation` in `june.config.ts`, on by default — the config\nexists to turn it off.\n\n## Why it matters\n\nA client router is the single biggest source of accidental JavaScript: the\nrouter pulls in state, the state pulls in hydration, and suddenly a\ndocument site ships a runtime. Letting the browser navigate keeps the\nzero-JS default ([Islands](/docs/features-islands)) honest.\n",
    "original": "---\ntitle: \"Navigation: instant without a client router\"\nnav: \"Navigation\"\ndescription: Hover prerenders the next page (Speculation Rules), the click is zero-network, View Transitions animate the swap — the browser is the router.\ndate: 2026-06-12\nsection: Features\norder: \"9\"\n---\n## The feature\n\nJune ships no client router — and navigation is still instant, because the\nbrowser already has one. Every HTML page carries three things:\n\n1. **Speculation Rules.** A `<script type=\"speculationrules\">` block tells\n   the browser to prerender same-origin links on hover (`eagerness:\n   moderate`). By the time you click, the next page is already rendered —\n   the click is a zero-network activation.\n2. **A fallback for the rest.** Browsers without Speculation Rules get a\n   tiny inline script that turns `pointerover` into `<link rel=\"prefetch\">`\n   — progressive enhancement, not a polyfill bundle.\n3. **View Transitions.** `@view-transition { navigation: auto }` animates\n   the document swap (and respects `prefers-reduced-motion`). Cross-document\n   navigation stops looking like a full reload because the browser paints it\n   as one continuous surface.\n\nThe agent surfaces are deliberately excluded: `.md`, `.json`, and `/mcp`\nnever prerender — machines don't hover.\n\n## Try it right now\n\nThis page is the demo. Hover any link in the sidebar, watch the network\npanel prerender it, then click: no fetch, one smooth transition. Everything\nyou just experienced shipped as ~1KB of declarative rules — not a router,\nnot a virtual DOM, not a client cache to invalidate.\n\n## The posture\n\nClient routers exist to make navigation fast and transitions smooth. The\nplatform now does both natively, so June's default is the same choice it\nmakes everywhere ([Standards](/docs/features-web-standards)): use the\nbrowser's mechanism and ship nothing. This is also why a full document — not\na Flight payload ([RSC](/docs/features-rsc)) — is what crosses the wire: it\ncovers most sites with zero client state to manage.\n\nWhen an app-like surface genuinely needs in-memory state to survive a\nnavigation (a dashboard, an open websocket), the opt-in\n[Client Router](/docs/features-client-router) adds soft swaps as progressive\nenhancement — off by default, and this site keeps it off.\n\nConfigured by `speculation` in `june.config.ts`, on by default — the config\nexists to turn it off.\n\n## Why it matters\n\nA client router is the single biggest source of accidental JavaScript: the\nrouter pulls in state, the state pulls in hydration, and suddenly a\ndocument site ships a runtime. Letting the browser navigate keeps the\nzero-JS default ([Islands](/docs/features-islands)) honest.\n",
    "html": "<h2>The feature</h2>\n<p>June ships no client router — and navigation is still instant, because the\nbrowser already has one. Every HTML page carries three things:</p>\n<ol>\n<li><strong>Speculation Rules.</strong> A <code>&lt;script type=&quot;speculationrules&quot;&gt;</code> block tells\nthe browser to prerender same-origin links on hover (<code>eagerness: moderate</code>). By the time you click, the next page is already rendered —\nthe click is a zero-network activation.</li>\n<li><strong>A fallback for the rest.</strong> Browsers without Speculation Rules get a\ntiny inline script that turns <code>pointerover</code> into <code>&lt;link rel=&quot;prefetch&quot;&gt;</code>\n— progressive enhancement, not a polyfill bundle.</li>\n<li><strong>View Transitions.</strong> <code>@view-transition { navigation: auto }</code> animates\nthe document swap (and respects <code>prefers-reduced-motion</code>). Cross-document\nnavigation stops looking like a full reload because the browser paints it\nas one continuous surface.</li>\n</ol>\n<p>The agent surfaces are deliberately excluded: <code>.md</code>, <code>.json</code>, and <code>/mcp</code>\nnever prerender — machines don&#39;t hover.</p>\n<h2>Try it right now</h2>\n<p>This page is the demo. Hover any link in the sidebar, watch the network\npanel prerender it, then click: no fetch, one smooth transition. Everything\nyou just experienced shipped as ~1KB of declarative rules — not a router,\nnot a virtual DOM, not a client cache to invalidate.</p>\n<h2>The posture</h2>\n<p>Client routers exist to make navigation fast and transitions smooth. The\nplatform now does both natively, so June&#39;s default is the same choice it\nmakes everywhere (<a href=\"/docs/features-web-standards\">Standards</a>): use the\nbrowser&#39;s mechanism and ship nothing. This is also why a full document — not\na Flight payload (<a href=\"/docs/features-rsc\">RSC</a>) — is what crosses the wire: it\ncovers most sites with zero client state to manage.</p>\n<p>When an app-like surface genuinely needs in-memory state to survive a\nnavigation (a dashboard, an open websocket), the opt-in\n<a href=\"/docs/features-client-router\">Client Router</a> adds soft swaps as progressive\nenhancement — off by default, and this site keeps it off.</p>\n<p>Configured by <code>speculation</code> in <code>june.config.ts</code>, on by default — the config\nexists to turn it off.</p>\n<h2>Why it matters</h2>\n<p>A client router is the single biggest source of accidental JavaScript: the\nrouter pulls in state, the state pulls in hydration, and suddenly a\ndocument site ships a runtime. Letting the browser navigate keeps the\nzero-JS default (<a href=\"/docs/features-islands\">Islands</a>) honest.</p>\n"
  },
  {
    "slug": "features-og-image",
    "data": {
      "title": "OG images, typeset at the edge",
      "nav": "OG Image",
      "description": "Social cards as a route that returns a PNG — satori + resvg in the worker, with runtime font subsetting so CJK titles work.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "4"
    },
    "body": "## The feature\n\nAn og:image should not be a pre-generated file you forget to regenerate — it\nis a route. This site serves `/og/<slug>.png` from the worker for EVERY page\n(posts, docs, core pages), and each page's `og:image` meta tag points at its\nown card. This page's, rendered live as you read:\n\n![The social card for this very page, typeset at request time](/og/features-og-image.png)\n\n```\n/og/<slug>.png → detect CJK → fetch font subset (text=title) → satori → resvg → PNG\n```\n\nsatori lays out JSX inside the V8 isolate; resvg (Rust compiled to WASM)\nrasterizes it. No browser, no puppeteer fleet — it runs where the rest of\nyour app runs.\n\n## The CJK part\n\nFull CJK families (Noto Sans TC / SC / JP / KR) weigh several MB per script —\nfar too heavy to ship in a worker, and a build can't know the glyph set of a\ndynamic title ahead of time. The answer is runtime subsetting via Google\nFonts' `text=` parameter: download only the glyphs the title actually uses\n(a few dozen KB), cache the subset for a week through workerd's Cache API.\nTraditional and Simplified Chinese resolve to different fonts; Japanese mixes\nkana with its own kanji forms — the detection step picks per title.\n\nThe full design walkthrough, with per-script samples, is in\n[Typesetting CJK at the edge](/blog/2026-06-10-typesetting-cjk-at-the-edge).\n\n## Try it\n\n```bash\ncurl -o card.png https://june.build/og/2026-06-10-typesetting-cjk-at-the-edge.png\n```\n\nThe route lives behind `app/_extra.tsx` — June's pre-route escape hatch for\nresponses a page has no projection for yet (binary bodies). One card\ndefinition renders everywhere: workers-og rasterizes it on workerd, satori +\nresvg-js rasterize the same JSX on the dev host — so the social card you\npreview at `localhost:3000/og/…` is the one that deploys.\n\n## Why it matters\n\nSocial cards are the page most teams generate with a headless browser in a\ncron job. Making them a route means they're always current, deploy with the\napp, and cost a font subset — not a Chromium.\n",
    "original": "---\ntitle: \"OG images, typeset at the edge\"\nnav: \"OG Image\"\ndescription: Social cards as a route that returns a PNG — satori + resvg in the worker, with runtime font subsetting so CJK titles work.\ndate: 2026-06-12\nsection: Features\norder: \"4\"\n---\n## The feature\n\nAn og:image should not be a pre-generated file you forget to regenerate — it\nis a route. This site serves `/og/<slug>.png` from the worker for EVERY page\n(posts, docs, core pages), and each page's `og:image` meta tag points at its\nown card. This page's, rendered live as you read:\n\n![The social card for this very page, typeset at request time](/og/features-og-image.png)\n\n```\n/og/<slug>.png → detect CJK → fetch font subset (text=title) → satori → resvg → PNG\n```\n\nsatori lays out JSX inside the V8 isolate; resvg (Rust compiled to WASM)\nrasterizes it. No browser, no puppeteer fleet — it runs where the rest of\nyour app runs.\n\n## The CJK part\n\nFull CJK families (Noto Sans TC / SC / JP / KR) weigh several MB per script —\nfar too heavy to ship in a worker, and a build can't know the glyph set of a\ndynamic title ahead of time. The answer is runtime subsetting via Google\nFonts' `text=` parameter: download only the glyphs the title actually uses\n(a few dozen KB), cache the subset for a week through workerd's Cache API.\nTraditional and Simplified Chinese resolve to different fonts; Japanese mixes\nkana with its own kanji forms — the detection step picks per title.\n\nThe full design walkthrough, with per-script samples, is in\n[Typesetting CJK at the edge](/blog/2026-06-10-typesetting-cjk-at-the-edge).\n\n## Try it\n\n```bash\ncurl -o card.png https://june.build/og/2026-06-10-typesetting-cjk-at-the-edge.png\n```\n\nThe route lives behind `app/_extra.tsx` — June's pre-route escape hatch for\nresponses a page has no projection for yet (binary bodies). One card\ndefinition renders everywhere: workers-og rasterizes it on workerd, satori +\nresvg-js rasterize the same JSX on the dev host — so the social card you\npreview at `localhost:3000/og/…` is the one that deploys.\n\n## Why it matters\n\nSocial cards are the page most teams generate with a headless browser in a\ncron job. Making them a route means they're always current, deploy with the\napp, and cost a font subset — not a Chromium.\n",
    "html": "<h2>The feature</h2>\n<p>An og:image should not be a pre-generated file you forget to regenerate — it\nis a route. This site serves <code>/og/&lt;slug&gt;.png</code> from the worker for EVERY page\n(posts, docs, core pages), and each page&#39;s <code>og:image</code> meta tag points at its\nown card. This page&#39;s, rendered live as you read:</p>\n<p><img src=\"/og/features-og-image.png\" alt=\"The social card for this very page, typeset at request time\"></p>\n<pre><code>/og/&lt;slug&gt;.png → detect CJK → fetch font subset (text=title) → satori → resvg → PNG\n</code></pre>\n<p>satori lays out JSX inside the V8 isolate; resvg (Rust compiled to WASM)\nrasterizes it. No browser, no puppeteer fleet — it runs where the rest of\nyour app runs.</p>\n<h2>The CJK part</h2>\n<p>Full CJK families (Noto Sans TC / SC / JP / KR) weigh several MB per script —\nfar too heavy to ship in a worker, and a build can&#39;t know the glyph set of a\ndynamic title ahead of time. The answer is runtime subsetting via Google\nFonts&#39; <code>text=</code> parameter: download only the glyphs the title actually uses\n(a few dozen KB), cache the subset for a week through workerd&#39;s Cache API.\nTraditional and Simplified Chinese resolve to different fonts; Japanese mixes\nkana with its own kanji forms — the detection step picks per title.</p>\n<p>The full design walkthrough, with per-script samples, is in\n<a href=\"/blog/2026-06-10-typesetting-cjk-at-the-edge\">Typesetting CJK at the edge</a>.</p>\n<h2>Try it</h2>\n<pre><code class=\"language-bash\">curl -o card.png https://june.build/og/2026-06-10-typesetting-cjk-at-the-edge.png\n</code></pre>\n<p>The route lives behind <code>app/_extra.tsx</code> — June&#39;s pre-route escape hatch for\nresponses a page has no projection for yet (binary bodies). One card\ndefinition renders everywhere: workers-og rasterizes it on workerd, satori +\nresvg-js rasterize the same JSX on the dev host — so the social card you\npreview at <code>localhost:3000/og/…</code> is the one that deploys.</p>\n<h2>Why it matters</h2>\n<p>Social cards are the page most teams generate with a headless browser in a\ncron job. Making them a route means they&#39;re always current, deploy with the\napp, and cost a font subset — not a Chromium.</p>\n"
  },
  {
    "slug": "features-rsc",
    "data": {
      "title": "React Server Components, server-first",
      "nav": "RSC",
      "description": "Every page is a server component tree rendered on the server; client code is an explicit island; actions are server functions with one authorization gate.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "5"
    },
    "body": "## What June does today\n\n- **Server components by default.** Every `page.tsx` and its layout chain\n  render on the server — `view()` returns a server component tree, and the\n  HTML that leaves the worker is fully resolved. No component code ships to\n  the browser unless you ask.\n- **Client components are explicit.** `<Island name=\"Counter\">` marks the one\n  subtree that hydrates, against a registry you write ([Islands](/docs/features-islands)).\n  The boundary is visible in the code, not inferred by a bundler.\n- **Actions are server functions.** `defineAction.run(input, ctx)` executes\n  on the server and is passable to client components as a prop — the same\n  definition is also an MCP tool behind the same gate ([MCP](/docs/features-mcp)).\n- **One render core, everywhere.** Dev and the deployed worker render through\n  the same pipeline; our parity suite asserts the output byte-for-byte.\n\n## What June does NOT do yet\n\nStated plainly, because RSC claims are easy to inflate:\n\n- **No streamed Suspense fallbacks** — pages flush fully resolved (the\n  pipeline waits for `allReady`). Out-of-order streaming is on the roadmap.\n- **No Flight-payload navigation** — navigations are full documents made\n  instant by Speculation-Rules prerendering\n  ([Navigation](/docs/features-navigation)), not RSC payload diffs.\n- **Live RSC (server-push re-render)** exists and is measured on the\n  experimental native runtime track — its push loop is the machinery behind\n  the 73ms HMR number — but it is not part of the v0.1 host.\n\n## Why server-first\n\nMost pages are documents with islands of interactivity, and two audiences\nread them: people (HTML) and agents (markdown, JSON — projections of the\nsame render, see [Markdown](/docs/features-markdown)). Rendering on the\nserver is what makes one definition serve both without shipping a runtime to\nthe client.\n",
    "original": "---\ntitle: \"React Server Components, server-first\"\nnav: \"RSC\"\ndescription: Every page is a server component tree rendered on the server; client code is an explicit island; actions are server functions with one authorization gate.\ndate: 2026-06-12\nsection: Features\norder: \"5\"\n---\n## What June does today\n\n- **Server components by default.** Every `page.tsx` and its layout chain\n  render on the server — `view()` returns a server component tree, and the\n  HTML that leaves the worker is fully resolved. No component code ships to\n  the browser unless you ask.\n- **Client components are explicit.** `<Island name=\"Counter\">` marks the one\n  subtree that hydrates, against a registry you write ([Islands](/docs/features-islands)).\n  The boundary is visible in the code, not inferred by a bundler.\n- **Actions are server functions.** `defineAction.run(input, ctx)` executes\n  on the server and is passable to client components as a prop — the same\n  definition is also an MCP tool behind the same gate ([MCP](/docs/features-mcp)).\n- **One render core, everywhere.** Dev and the deployed worker render through\n  the same pipeline; our parity suite asserts the output byte-for-byte.\n\n## What June does NOT do yet\n\nStated plainly, because RSC claims are easy to inflate:\n\n- **No streamed Suspense fallbacks** — pages flush fully resolved (the\n  pipeline waits for `allReady`). Out-of-order streaming is on the roadmap.\n- **No Flight-payload navigation** — navigations are full documents made\n  instant by Speculation-Rules prerendering\n  ([Navigation](/docs/features-navigation)), not RSC payload diffs.\n- **Live RSC (server-push re-render)** exists and is measured on the\n  experimental native runtime track — its push loop is the machinery behind\n  the 73ms HMR number — but it is not part of the v0.1 host.\n\n## Why server-first\n\nMost pages are documents with islands of interactivity, and two audiences\nread them: people (HTML) and agents (markdown, JSON — projections of the\nsame render, see [Markdown](/docs/features-markdown)). Rendering on the\nserver is what makes one definition serve both without shipping a runtime to\nthe client.\n",
    "html": "<h2>What June does today</h2>\n<ul>\n<li><strong>Server components by default.</strong> Every <code>page.tsx</code> and its layout chain\nrender on the server — <code>view()</code> returns a server component tree, and the\nHTML that leaves the worker is fully resolved. No component code ships to\nthe browser unless you ask.</li>\n<li><strong>Client components are explicit.</strong> <code>&lt;Island name=&quot;Counter&quot;&gt;</code> marks the one\nsubtree that hydrates, against a registry you write (<a href=\"/docs/features-islands\">Islands</a>).\nThe boundary is visible in the code, not inferred by a bundler.</li>\n<li><strong>Actions are server functions.</strong> <code>defineAction.run(input, ctx)</code> executes\non the server and is passable to client components as a prop — the same\ndefinition is also an MCP tool behind the same gate (<a href=\"/docs/features-mcp\">MCP</a>).</li>\n<li><strong>One render core, everywhere.</strong> Dev and the deployed worker render through\nthe same pipeline; our parity suite asserts the output byte-for-byte.</li>\n</ul>\n<h2>What June does NOT do yet</h2>\n<p>Stated plainly, because RSC claims are easy to inflate:</p>\n<ul>\n<li><strong>No streamed Suspense fallbacks</strong> — pages flush fully resolved (the\npipeline waits for <code>allReady</code>). Out-of-order streaming is on the roadmap.</li>\n<li><strong>No Flight-payload navigation</strong> — navigations are full documents made\ninstant by Speculation-Rules prerendering\n(<a href=\"/docs/features-navigation\">Navigation</a>), not RSC payload diffs.</li>\n<li><strong>Live RSC (server-push re-render)</strong> exists and is measured on the\nexperimental native runtime track — its push loop is the machinery behind\nthe 73ms HMR number — but it is not part of the v0.1 host.</li>\n</ul>\n<h2>Why server-first</h2>\n<p>Most pages are documents with islands of interactivity, and two audiences\nread them: people (HTML) and agents (markdown, JSON — projections of the\nsame render, see <a href=\"/docs/features-markdown\">Markdown</a>). Rendering on the\nserver is what makes one definition serve both without shipping a runtime to\nthe client.</p>\n"
  },
  {
    "slug": "features-runtime",
    "data": {
      "title": "Runtime: Bun-first toolchain, runtime-agnostic core",
      "nav": "Runtime",
      "description": "One tool runs install/test/dev; the core assumes no runtime at all — which is why the same app serves from Bun, Node, and workerd.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "12"
    },
    "body": "## The feature\n\nJune's runtime story is three layers, each with a different promise:\n\n| layer | promise |\n| --- | --- |\n| `@junejs/core` | **runtime-agnostic** — Web standards only, zero `node:*`, zero drivers |\n| host interface | Bun and Node implementations of one seam (serve, spawn, sqlite) |\n| production | **workerd** — V8 isolates, neither Node nor Bun in the serving path |\n\nThe toolchain is **Bun-first**: one tool covers install, test, and `june\ndev` — no tsx + nodemon + jest + esbuild assembly. That's the no-glue\nphilosophy applied to the toolchain itself.\n\nThe core being runtime-agnostic is not hygiene, it's the deploy story:\nbecause nothing in the render core assumes a runtime, dev (Bun or Node) and\nproduction (workerd) run the SAME code, and parity is a property instead of\na test burden.\n\n## The Node host is a tested claim\n\n`june dev` detects its runtime; on Node it serves through `node:http` — with\none bonus Bun can't offer: real RFC 8297 `103 Early Hints` interim responses.\nCI runs the full dev server under Node against real HTTP on every push\n(`scripts/smoke-node.ts`), so \"works on Node\" is continuously asserted, not\nassumed.\n\n```bash\nnode --import tsx scripts/smoke-node.ts\n# june dev → http://localhost:4399  (host: node)\n# node-host smoke: OK (serve, routes, discovery, mcp)\n```\n\n## Local SQLite: built-in, with one version floor on Node\n\nThe dev `db` uses the runtime's BUILT-IN SQLite — zero install, no native\nbuild: `bun:sqlite` on Bun, `node:sqlite` on Node. The one catch is Node's\nversion: `node:sqlite` is flag-free only on **Node ≥ 22.13.0** (the 22 LTS line)\nor **≥ 23.4.0**. Node 22.5–22.12 / 23.0–23.3 keep it behind\n`--experimental-sqlite`, and older Node doesn't ship it at all. June detects this\nand, if the import fails, tells you exactly that — upgrade Node to 22.13+, or run\nwith **Bun** (built-in `bun:sqlite`, no version floor). Bun is the always-works\npath; Node 22.13+ is the always-works Node.\n\n## The experimental track\n\nThere is also an owned Rust+V8 runtime (V8 snapshot boot ~12ms, bundled\nfirst render ~22ms, push-based HMR) — measured and public\n([benchmarks](/benchmarks)) but explicitly experimental. v0.1 ships on the\nBun/Node host; the native runtime is the roadmap, not the present.\n\n## Why it matters\n\nRuntime lock-in is glue you can't see until you migrate. A core that speaks\nonly Web standards keeps every door open — including the one we're building\ntoward.\n",
    "original": "---\ntitle: \"Runtime: Bun-first toolchain, runtime-agnostic core\"\nnav: \"Runtime\"\ndescription: One tool runs install/test/dev; the core assumes no runtime at all — which is why the same app serves from Bun, Node, and workerd.\ndate: 2026-06-12\nsection: Features\norder: \"12\"\n---\n## The feature\n\nJune's runtime story is three layers, each with a different promise:\n\n| layer | promise |\n| --- | --- |\n| `@junejs/core` | **runtime-agnostic** — Web standards only, zero `node:*`, zero drivers |\n| host interface | Bun and Node implementations of one seam (serve, spawn, sqlite) |\n| production | **workerd** — V8 isolates, neither Node nor Bun in the serving path |\n\nThe toolchain is **Bun-first**: one tool covers install, test, and `june\ndev` — no tsx + nodemon + jest + esbuild assembly. That's the no-glue\nphilosophy applied to the toolchain itself.\n\nThe core being runtime-agnostic is not hygiene, it's the deploy story:\nbecause nothing in the render core assumes a runtime, dev (Bun or Node) and\nproduction (workerd) run the SAME code, and parity is a property instead of\na test burden.\n\n## The Node host is a tested claim\n\n`june dev` detects its runtime; on Node it serves through `node:http` — with\none bonus Bun can't offer: real RFC 8297 `103 Early Hints` interim responses.\nCI runs the full dev server under Node against real HTTP on every push\n(`scripts/smoke-node.ts`), so \"works on Node\" is continuously asserted, not\nassumed.\n\n```bash\nnode --import tsx scripts/smoke-node.ts\n# june dev → http://localhost:4399  (host: node)\n# node-host smoke: OK (serve, routes, discovery, mcp)\n```\n\n## Local SQLite: built-in, with one version floor on Node\n\nThe dev `db` uses the runtime's BUILT-IN SQLite — zero install, no native\nbuild: `bun:sqlite` on Bun, `node:sqlite` on Node. The one catch is Node's\nversion: `node:sqlite` is flag-free only on **Node ≥ 22.13.0** (the 22 LTS line)\nor **≥ 23.4.0**. Node 22.5–22.12 / 23.0–23.3 keep it behind\n`--experimental-sqlite`, and older Node doesn't ship it at all. June detects this\nand, if the import fails, tells you exactly that — upgrade Node to 22.13+, or run\nwith **Bun** (built-in `bun:sqlite`, no version floor). Bun is the always-works\npath; Node 22.13+ is the always-works Node.\n\n## The experimental track\n\nThere is also an owned Rust+V8 runtime (V8 snapshot boot ~12ms, bundled\nfirst render ~22ms, push-based HMR) — measured and public\n([benchmarks](/benchmarks)) but explicitly experimental. v0.1 ships on the\nBun/Node host; the native runtime is the roadmap, not the present.\n\n## Why it matters\n\nRuntime lock-in is glue you can't see until you migrate. A core that speaks\nonly Web standards keeps every door open — including the one we're building\ntoward.\n",
    "html": "<h2>The feature</h2>\n<p>June&#39;s runtime story is three layers, each with a different promise:</p>\n<table>\n<thead>\n<tr>\n<th>layer</th>\n<th>promise</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>@junejs/core</code></td>\n<td><strong>runtime-agnostic</strong> — Web standards only, zero <code>node:*</code>, zero drivers</td>\n</tr>\n<tr>\n<td>host interface</td>\n<td>Bun and Node implementations of one seam (serve, spawn, sqlite)</td>\n</tr>\n<tr>\n<td>production</td>\n<td><strong>workerd</strong> — V8 isolates, neither Node nor Bun in the serving path</td>\n</tr>\n</tbody></table>\n<p>The toolchain is <strong>Bun-first</strong>: one tool covers install, test, and <code>june dev</code> — no tsx + nodemon + jest + esbuild assembly. That&#39;s the no-glue\nphilosophy applied to the toolchain itself.</p>\n<p>The core being runtime-agnostic is not hygiene, it&#39;s the deploy story:\nbecause nothing in the render core assumes a runtime, dev (Bun or Node) and\nproduction (workerd) run the SAME code, and parity is a property instead of\na test burden.</p>\n<h2>The Node host is a tested claim</h2>\n<p><code>june dev</code> detects its runtime; on Node it serves through <code>node:http</code> — with\none bonus Bun can&#39;t offer: real RFC 8297 <code>103 Early Hints</code> interim responses.\nCI runs the full dev server under Node against real HTTP on every push\n(<code>scripts/smoke-node.ts</code>), so &quot;works on Node&quot; is continuously asserted, not\nassumed.</p>\n<pre><code class=\"language-bash\">node --import tsx scripts/smoke-node.ts\n# june dev → http://localhost:4399  (host: node)\n# node-host smoke: OK (serve, routes, discovery, mcp)\n</code></pre>\n<h2>Local SQLite: built-in, with one version floor on Node</h2>\n<p>The dev <code>db</code> uses the runtime&#39;s BUILT-IN SQLite — zero install, no native\nbuild: <code>bun:sqlite</code> on Bun, <code>node:sqlite</code> on Node. The one catch is Node&#39;s\nversion: <code>node:sqlite</code> is flag-free only on <strong>Node ≥ 22.13.0</strong> (the 22 LTS line)\nor <strong>≥ 23.4.0</strong>. Node 22.5–22.12 / 23.0–23.3 keep it behind\n<code>--experimental-sqlite</code>, and older Node doesn&#39;t ship it at all. June detects this\nand, if the import fails, tells you exactly that — upgrade Node to 22.13+, or run\nwith <strong>Bun</strong> (built-in <code>bun:sqlite</code>, no version floor). Bun is the always-works\npath; Node 22.13+ is the always-works Node.</p>\n<h2>The experimental track</h2>\n<p>There is also an owned Rust+V8 runtime (V8 snapshot boot ~12ms, bundled\nfirst render ~22ms, push-based HMR) — measured and public\n(<a href=\"/benchmarks\">benchmarks</a>) but explicitly experimental. v0.1 ships on the\nBun/Node host; the native runtime is the roadmap, not the present.</p>\n<h2>Why it matters</h2>\n<p>Runtime lock-in is glue you can&#39;t see until you migrate. A core that speaks\nonly Web standards keeps every door open — including the one we&#39;re building\ntoward.</p>\n"
  },
  {
    "slug": "features-web-standards",
    "data": {
      "title": "Web Standards end to end",
      "nav": "Standards",
      "description": "The whole framework is one fetch handler — Request in, Response out, Web Streams in between. Standards are the portability mechanism, not a checkbox.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "11"
    },
    "body": "## The feature\n\nJune's entire request path is the Web platform's own vocabulary:\n\n```ts\nfetch(request: Request): Promise<Response>\n```\n\nThat signature *is* the framework. Inside it: `URL` for routing, `Headers`\nfor negotiation, `ReadableStream` for HTML streaming\n(`renderToReadableStream`, not the Node-only pipeable variant), Web `crypto`\nfor IDs, the Cache API where the host offers one. `@junejs/core` imports\nzero `node:*` modules — enforced as an architectural rule, not a habit.\n\n## Standards as the portability mechanism\n\nThis is why deployment is an adapter instead of a rewrite\n([Deployment](/docs/04-deployment)):\n\n- **workerd** speaks fetch natively — the built worker's `fetch()` IS the\n  pipeline, no shim.\n- **Bun** serves it directly (`Bun.serve({ fetch })`).\n- **Node** needs only an edge adapter: `node:http` requests are converted to\n  `Request` / from `Response` at the boundary (`Readable.toWeb` / `fromWeb`)\n  — and nothing below the boundary knows.\n\nSame story for data: the `db` contract is async-first because edge databases\n(D1) are async — the standard-shaped surface is the one every target can\nimplement.\n\n## Try it\n\nThe MCP endpoint is a plain standards handler too — `mcpHandler(request)`\ntakes a `Request` like everything else. There is no framework-specific\nrequest object to learn and no middleware tower to thread:\n\n```ts\n// a June app is embeddable anywhere a fetch handler runs\nconst app = createApp({ appDir, config });\nexport default { fetch: (req: Request) => app.fetch(req) };\n```\n\n## Why it matters\n\nFramework-specific request objects are how lock-in starts. When the\ninterface is the platform's own, your knowledge transfers in, your code\ntransfers out, and new runtimes are adapters — for us *and* for you.\n",
    "original": "---\ntitle: \"Web Standards end to end\"\nnav: \"Standards\"\ndescription: The whole framework is one fetch handler — Request in, Response out, Web Streams in between. Standards are the portability mechanism, not a checkbox.\ndate: 2026-06-12\nsection: Features\norder: \"11\"\n---\n## The feature\n\nJune's entire request path is the Web platform's own vocabulary:\n\n```ts\nfetch(request: Request): Promise<Response>\n```\n\nThat signature *is* the framework. Inside it: `URL` for routing, `Headers`\nfor negotiation, `ReadableStream` for HTML streaming\n(`renderToReadableStream`, not the Node-only pipeable variant), Web `crypto`\nfor IDs, the Cache API where the host offers one. `@junejs/core` imports\nzero `node:*` modules — enforced as an architectural rule, not a habit.\n\n## Standards as the portability mechanism\n\nThis is why deployment is an adapter instead of a rewrite\n([Deployment](/docs/04-deployment)):\n\n- **workerd** speaks fetch natively — the built worker's `fetch()` IS the\n  pipeline, no shim.\n- **Bun** serves it directly (`Bun.serve({ fetch })`).\n- **Node** needs only an edge adapter: `node:http` requests are converted to\n  `Request` / from `Response` at the boundary (`Readable.toWeb` / `fromWeb`)\n  — and nothing below the boundary knows.\n\nSame story for data: the `db` contract is async-first because edge databases\n(D1) are async — the standard-shaped surface is the one every target can\nimplement.\n\n## Try it\n\nThe MCP endpoint is a plain standards handler too — `mcpHandler(request)`\ntakes a `Request` like everything else. There is no framework-specific\nrequest object to learn and no middleware tower to thread:\n\n```ts\n// a June app is embeddable anywhere a fetch handler runs\nconst app = createApp({ appDir, config });\nexport default { fetch: (req: Request) => app.fetch(req) };\n```\n\n## Why it matters\n\nFramework-specific request objects are how lock-in starts. When the\ninterface is the platform's own, your knowledge transfers in, your code\ntransfers out, and new runtimes are adapters — for us *and* for you.\n",
    "html": "<h2>The feature</h2>\n<p>June&#39;s entire request path is the Web platform&#39;s own vocabulary:</p>\n<pre><code class=\"language-ts\">fetch(request: Request): Promise&lt;Response&gt;\n</code></pre>\n<p>That signature <em>is</em> the framework. Inside it: <code>URL</code> for routing, <code>Headers</code>\nfor negotiation, <code>ReadableStream</code> for HTML streaming\n(<code>renderToReadableStream</code>, not the Node-only pipeable variant), Web <code>crypto</code>\nfor IDs, the Cache API where the host offers one. <code>@junejs/core</code> imports\nzero <code>node:*</code> modules — enforced as an architectural rule, not a habit.</p>\n<h2>Standards as the portability mechanism</h2>\n<p>This is why deployment is an adapter instead of a rewrite\n(<a href=\"/docs/04-deployment\">Deployment</a>):</p>\n<ul>\n<li><strong>workerd</strong> speaks fetch natively — the built worker&#39;s <code>fetch()</code> IS the\npipeline, no shim.</li>\n<li><strong>Bun</strong> serves it directly (<code>Bun.serve({ fetch })</code>).</li>\n<li><strong>Node</strong> needs only an edge adapter: <code>node:http</code> requests are converted to\n<code>Request</code> / from <code>Response</code> at the boundary (<code>Readable.toWeb</code> / <code>fromWeb</code>)\n— and nothing below the boundary knows.</li>\n</ul>\n<p>Same story for data: the <code>db</code> contract is async-first because edge databases\n(D1) are async — the standard-shaped surface is the one every target can\nimplement.</p>\n<h2>Try it</h2>\n<p>The MCP endpoint is a plain standards handler too — <code>mcpHandler(request)</code>\ntakes a <code>Request</code> like everything else. There is no framework-specific\nrequest object to learn and no middleware tower to thread:</p>\n<pre><code class=\"language-ts\">// a June app is embeddable anywhere a fetch handler runs\nconst app = createApp({ appDir, config });\nexport default { fetch: (req: Request) =&gt; app.fetch(req) };\n</code></pre>\n<h2>Why it matters</h2>\n<p>Framework-specific request objects are how lock-in starts. When the\ninterface is the platform&#39;s own, your knowledge transfers in, your code\ntransfers out, and new runtimes are adapters — for us <em>and</em> for you.</p>\n"
  }
];
export const doc = (slug: string): ContentEntry | null => DOCS.find((p) => p.slug === slug) ?? null;
