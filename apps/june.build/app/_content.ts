// AUTO-GENERATED by `june build` — edit content/**/*.md, not this file.
export type ContentEntry = { slug: string; data: Record<string, string | string[]>; body: string; original: string; html: string };
export const POSTS: ContentEntry[] = [
  {
    "slug": "2026-06-12-building-june-build-with-june",
    "data": {
      "title": "Building june.build with June",
      "date": "2026-06-12",
      "description": "Our site is its own demo — every page serves humans and agents from one definition.",
      "tags": [
        "dogfood",
        "dual-audience"
      ]
    },
    "body": "This site is built with June and deployed on Cloudflare Workers. That sentence\nis most of the pitch, so let's prove it instead of repeating it.\n\n## One route, five surfaces\n\nEvery page here is a single `route()` definition with projections:\n\n```\nGET /why            → streamed HTML (you, probably)\nGET /why.md         → clean markdown (an agent, probably)\nGET /blog.json      → structured frontmatter (a crawler with taste)\nGET /why.agent      → a capability manifest (an agent deciding what to do)\nPOST /mcp           → tools/call search_site, get_page (an agent at work)\n```\n\nThe markdown an agent fetches for this very post is **the file we authored,\nbyte for byte** — frontmatter included. Most frameworks reconstruct markdown\nfrom rendered HTML; June serves the source. There is nothing to drift.\n\n## Try it from your terminal\n\n```bash\ncurl https://june.build/blog/2026-06-12-building-june-build-with-june.md\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/call\",\"params\":{\"name\":\"search_site\",\"arguments\":{\"query\":\"cold start\"}}}'\n```\n\n## What the human side gets\n\nHover any link: the page prerenders in the background (Speculation Rules) and\nthe click is a zero-network activation with a view transition. No client\nJavaScript was written for any of this — the browser is the client runtime.\n",
    "original": "---\ntitle: Building june.build with June\ndate: 2026-06-12\ndescription: Our site is its own demo — every page serves humans and agents from one definition.\ntags: [dogfood, dual-audience]\n---\n\nThis site is built with June and deployed on Cloudflare Workers. That sentence\nis most of the pitch, so let's prove it instead of repeating it.\n\n## One route, five surfaces\n\nEvery page here is a single `route()` definition with projections:\n\n```\nGET /why            → streamed HTML (you, probably)\nGET /why.md         → clean markdown (an agent, probably)\nGET /blog.json      → structured frontmatter (a crawler with taste)\nGET /why.agent      → a capability manifest (an agent deciding what to do)\nPOST /mcp           → tools/call search_site, get_page (an agent at work)\n```\n\nThe markdown an agent fetches for this very post is **the file we authored,\nbyte for byte** — frontmatter included. Most frameworks reconstruct markdown\nfrom rendered HTML; June serves the source. There is nothing to drift.\n\n## Try it from your terminal\n\n```bash\ncurl https://june.build/blog/2026-06-12-building-june-build-with-june.md\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/call\",\"params\":{\"name\":\"search_site\",\"arguments\":{\"query\":\"cold start\"}}}'\n```\n\n## What the human side gets\n\nHover any link: the page prerenders in the background (Speculation Rules) and\nthe click is a zero-network activation with a view transition. No client\nJavaScript was written for any of this — the browser is the client runtime.\n",
    "html": "<p>This site is built with June and deployed on Cloudflare Workers. That sentence\nis most of the pitch, so let&#39;s prove it instead of repeating it.</p>\n<h2>One route, five surfaces</h2>\n<p>Every page here is a single <code>route()</code> definition with projections:</p>\n<pre><code>GET /why            → streamed HTML (you, probably)\nGET /why.md         → clean markdown (an agent, probably)\nGET /blog.json      → structured frontmatter (a crawler with taste)\nGET /why.agent      → a capability manifest (an agent deciding what to do)\nPOST /mcp           → tools/call search_site, get_page (an agent at work)\n</code></pre>\n<p>The markdown an agent fetches for this very post is <strong>the file we authored,\nbyte for byte</strong> — frontmatter included. Most frameworks reconstruct markdown\nfrom rendered HTML; June serves the source. There is nothing to drift.</p>\n<h2>Try it from your terminal</h2>\n<pre><code class=\"language-bash\">curl https://june.build/blog/2026-06-12-building-june-build-with-june.md\ncurl -X POST https://june.build/mcp \\\n  -H &#39;content-type: application/json&#39; \\\n  -d &#39;{&quot;jsonrpc&quot;:&quot;2.0&quot;,&quot;id&quot;:1,&quot;method&quot;:&quot;tools/call&quot;,&quot;params&quot;:{&quot;name&quot;:&quot;search_site&quot;,&quot;arguments&quot;:{&quot;query&quot;:&quot;cold start&quot;}}}&#39;\n</code></pre>\n<h2>What the human side gets</h2>\n<p>Hover any link: the page prerenders in the background (Speculation Rules) and\nthe click is a zero-network activation with a view transition. No client\nJavaScript was written for any of this — the browser is the client runtime.</p>\n"
  },
  {
    "slug": "2026-06-10-anatomy-of-a-59ms-cold-start",
    "data": {
      "title": "59ms: anatomy of a dev cold start",
      "date": "2026-06-10",
      "description": "Where every millisecond of `june dev` goes, and the three cuts that took 244ms to 59ms.",
      "tags": [
        "performance",
        "runtime"
      ]
    },
    "body": "*(The native Rust runtime is an experimental track — v0.1's `june dev` is the\nBun/Node host. These numbers are why the track exists.)*\n\nWe benchmarked our Rust dev runtime's cold start — process spawn to first HTML\nbyte — and then took it apart. Three cuts, all measured (medians, reproducible\nscripts in the repo):\n\n| cut | before | after |\n| --- | --- | --- |\n| listen-early (dev scripts off the critical path) | ~100ms blocking | 3ms to listening |\n| V8 startup snapshot (Web globals baked into the heap) | 24.4ms | 5µs |\n| React vendors in the snapshot's module map | 98ms | 18ms |\n\nEnd to end: **244ms → 59ms** on a hello-world page, with the remaining budget\nsplit between process spawn (~10ms) and first-render JIT (~30ms) — the latter\nis V8 physics for unbundled dev, and the bundled production path has already\nmeasured **22ms**.\n\nThe honest caveats: one dev machine, medians not marketing minimums, and\n`next dev`/Vite comparisons are class-level, not same-app shootouts yet.\nBenchmark rigor is its own roadmap item.\n\nOne number we did not expect: rebuilding a worker isolate after a file edit\ndropped from 137ms to **16ms**, which is what makes push-based HMR (the server\nre-renders and pushes the diff over the live channel) feel instant.\n",
    "original": "---\ntitle: \"59ms: anatomy of a dev cold start\"\ndate: 2026-06-10\ndescription: Where every millisecond of `june dev` goes, and the three cuts that took 244ms to 59ms.\ntags: [performance, runtime]\n---\n\n*(The native Rust runtime is an experimental track — v0.1's `june dev` is the\nBun/Node host. These numbers are why the track exists.)*\n\nWe benchmarked our Rust dev runtime's cold start — process spawn to first HTML\nbyte — and then took it apart. Three cuts, all measured (medians, reproducible\nscripts in the repo):\n\n| cut | before | after |\n| --- | --- | --- |\n| listen-early (dev scripts off the critical path) | ~100ms blocking | 3ms to listening |\n| V8 startup snapshot (Web globals baked into the heap) | 24.4ms | 5µs |\n| React vendors in the snapshot's module map | 98ms | 18ms |\n\nEnd to end: **244ms → 59ms** on a hello-world page, with the remaining budget\nsplit between process spawn (~10ms) and first-render JIT (~30ms) — the latter\nis V8 physics for unbundled dev, and the bundled production path has already\nmeasured **22ms**.\n\nThe honest caveats: one dev machine, medians not marketing minimums, and\n`next dev`/Vite comparisons are class-level, not same-app shootouts yet.\nBenchmark rigor is its own roadmap item.\n\nOne number we did not expect: rebuilding a worker isolate after a file edit\ndropped from 137ms to **16ms**, which is what makes push-based HMR (the server\nre-renders and pushes the diff over the live channel) feel instant.\n",
    "html": "<p><em>(The native Rust runtime is an experimental track — v0.1&#39;s <code>june dev</code> is the\nBun/Node host. These numbers are why the track exists.)</em></p>\n<p>We benchmarked our Rust dev runtime&#39;s cold start — process spawn to first HTML\nbyte — and then took it apart. Three cuts, all measured (medians, reproducible\nscripts in the repo):</p>\n<table>\n<thead>\n<tr>\n<th>cut</th>\n<th>before</th>\n<th>after</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>listen-early (dev scripts off the critical path)</td>\n<td>~100ms blocking</td>\n<td>3ms to listening</td>\n</tr>\n<tr>\n<td>V8 startup snapshot (Web globals baked into the heap)</td>\n<td>24.4ms</td>\n<td>5µs</td>\n</tr>\n<tr>\n<td>React vendors in the snapshot&#39;s module map</td>\n<td>98ms</td>\n<td>18ms</td>\n</tr>\n</tbody></table>\n<p>End to end: <strong>244ms → 59ms</strong> on a hello-world page, with the remaining budget\nsplit between process spawn (~10ms) and first-render JIT (~30ms) — the latter\nis V8 physics for unbundled dev, and the bundled production path has already\nmeasured <strong>22ms</strong>.</p>\n<p>The honest caveats: one dev machine, medians not marketing minimums, and\n<code>next dev</code>/Vite comparisons are class-level, not same-app shootouts yet.\nBenchmark rigor is its own roadmap item.</p>\n<p>One number we did not expect: rebuilding a worker isolate after a file edit\ndropped from 137ms to <strong>16ms</strong>, which is what makes push-based HMR (the server\nre-renders and pushes the diff over the live channel) feel instant.</p>\n"
  },
  {
    "slug": "2026-06-10-typesetting-cjk-at-the-edge",
    "data": {
      "title": "Typesetting CJK at the edge: og:image and font subsetting",
      "date": "2026-06-10",
      "description": "This post's social card is typeset live on Cloudflare Workers — CJK titles included.",
      "tags": [
        "og-image",
        "fonts",
        "edge",
        "cjk"
      ]
    },
    "body": "The og:image for a post here is not a pre-generated file — it's a route that\nreturns a PNG: satori lays out JSX inside a V8 isolate, resvg (Rust compiled\nto WASM) rasterizes it, and no browser is involved anywhere.\n\nCJK is the real test: a full CJK family (Noto Sans TC/JP/KR) weighs several\nMB per script — far too heavy to ship in a worker. The answer is Google\nFonts' `text=` parameter — **download only the glyphs the title actually\nuses**. A subset is a few dozen KB, cached for a week through workerd's\nCache API.\n\n```\n/og/<slug>.png → detect CJK → fetch font subset (text=title) → satori → resvg → PNG\n```\n\nThe same pipeline covers every CJK script with no per-language work — and\nthe distinctions matter: Traditional and Simplified Chinese are different\nglyph sets served by different fonts (Noto Sans TC vs SC), and Japanese\nmixes kana with its own kanji forms:\n\n- エッジで日本語を組版する (Japanese — kana and kanji)\n- 邊緣排版與字型子集化 (Traditional Chinese)\n- 边缘排版与字体子集化 (Simplified Chinese)\n- 글꼴 서브셋 (Korean)\n\nA common alternative is self-hosting fonts at build time (our asset pipeline\nwill do that too), but a build cannot know the glyph set of a dynamic title\nahead of time — runtime subsetting is the right call for the og:image case.\n",
    "original": "---\ntitle: \"Typesetting CJK at the edge: og:image and font subsetting\"\ndate: 2026-06-10\ndescription: This post's social card is typeset live on Cloudflare Workers — CJK titles included.\ntags: [og-image, fonts, edge, cjk]\n---\n\nThe og:image for a post here is not a pre-generated file — it's a route that\nreturns a PNG: satori lays out JSX inside a V8 isolate, resvg (Rust compiled\nto WASM) rasterizes it, and no browser is involved anywhere.\n\nCJK is the real test: a full CJK family (Noto Sans TC/JP/KR) weighs several\nMB per script — far too heavy to ship in a worker. The answer is Google\nFonts' `text=` parameter — **download only the glyphs the title actually\nuses**. A subset is a few dozen KB, cached for a week through workerd's\nCache API.\n\n```\n/og/<slug>.png → detect CJK → fetch font subset (text=title) → satori → resvg → PNG\n```\n\nThe same pipeline covers every CJK script with no per-language work — and\nthe distinctions matter: Traditional and Simplified Chinese are different\nglyph sets served by different fonts (Noto Sans TC vs SC), and Japanese\nmixes kana with its own kanji forms:\n\n- エッジで日本語を組版する (Japanese — kana and kanji)\n- 邊緣排版與字型子集化 (Traditional Chinese)\n- 边缘排版与字体子集化 (Simplified Chinese)\n- 글꼴 서브셋 (Korean)\n\nA common alternative is self-hosting fonts at build time (our asset pipeline\nwill do that too), but a build cannot know the glyph set of a dynamic title\nahead of time — runtime subsetting is the right call for the og:image case.\n",
    "html": "<p>The og:image for a post here is not a pre-generated file — it&#39;s a route that\nreturns a PNG: satori lays out JSX inside a V8 isolate, resvg (Rust compiled\nto WASM) rasterizes it, and no browser is involved anywhere.</p>\n<p>CJK is the real test: a full CJK family (Noto Sans TC/JP/KR) weighs several\nMB per script — far too heavy to ship in a worker. The answer is Google\nFonts&#39; <code>text=</code> parameter — <strong>download only the glyphs the title actually\nuses</strong>. A subset is a few dozen KB, cached for a week through workerd&#39;s\nCache API.</p>\n<pre><code>/og/&lt;slug&gt;.png → detect CJK → fetch font subset (text=title) → satori → resvg → PNG\n</code></pre>\n<p>The same pipeline covers every CJK script with no per-language work — and\nthe distinctions matter: Traditional and Simplified Chinese are different\nglyph sets served by different fonts (Noto Sans TC vs SC), and Japanese\nmixes kana with its own kanji forms:</p>\n<ul>\n<li>エッジで日本語を組版する (Japanese — kana and kanji)</li>\n<li>邊緣排版與字型子集化 (Traditional Chinese)</li>\n<li>边缘排版与字体子集化 (Simplified Chinese)</li>\n<li>글꼴 서브셋 (Korean)</li>\n</ul>\n<p>A common alternative is self-hosting fonts at build time (our asset pipeline\nwill do that too), but a build cannot know the glyph set of a dynamic title\nahead of time — runtime subsetting is the right call for the og:image case.</p>\n"
  }
];
export const post = (slug: string): ContentEntry | null => POSTS.find((p) => p.slug === slug) ?? null;
export const DOCS: ContentEntry[] = [
  {
    "slug": "01-getting-started",
    "data": {
      "title": "Getting started",
      "description": "Scaffold, develop, build, and deploy a June app in four commands.",
      "date": "2026-06-12"
    },
    "body": "## Install\n\n```bash\nnpm create june my-app          # canonical (package: create-june)\ncd my-app && npm run dev\n```\n\nThe framework packages are `@junejs/core` (the contract layer: `route()`,\n`defineAction()`, islands) and `@junejs/cli` (the `june` command) — NOT `june`\n(an unrelated npm package), not `junejs`, and not `@june/*` (that scope isn't\nours).\n\n## The loop\n\n```bash\njune dev          # dev server (Bun/Node host)\njune build        # Workers bundle: dist/worker.js + prerendered assets\njune deploy       # build + wrangler upload (--dry-run validates only)\njune gen          # freeze content/**/*.md → app/_content.ts\njune info         # show routes + the agent surface\n```\n\n`june build` freezes what the dev server discovers at request time: routes →\na static manifest, `june.config.ts` → literals, `content/**/*.md` →\n`app/_content.ts`, `route({ prerender: true })` pages → static files.\n\n## Where things go\n\n```\napp/            file-system routes: page.tsx, layout.tsx, [param]/\napp/_client.*   the client entry — its presence turns on islands hydration\ncontent/        markdown collections (frozen at build, verbatim .md projections)\njune.config.ts  site metadata, agent-surface switches, resources\n```\n",
    "original": "---\ntitle: Getting started\ndescription: Scaffold, develop, build, and deploy a June app in four commands.\ndate: 2026-06-12\n---\n## Install\n\n```bash\nnpm create june my-app          # canonical (package: create-june)\ncd my-app && npm run dev\n```\n\nThe framework packages are `@junejs/core` (the contract layer: `route()`,\n`defineAction()`, islands) and `@junejs/cli` (the `june` command) — NOT `june`\n(an unrelated npm package), not `junejs`, and not `@june/*` (that scope isn't\nours).\n\n## The loop\n\n```bash\njune dev          # dev server (Bun/Node host)\njune build        # Workers bundle: dist/worker.js + prerendered assets\njune deploy       # build + wrangler upload (--dry-run validates only)\njune gen          # freeze content/**/*.md → app/_content.ts\njune info         # show routes + the agent surface\n```\n\n`june build` freezes what the dev server discovers at request time: routes →\na static manifest, `june.config.ts` → literals, `content/**/*.md` →\n`app/_content.ts`, `route({ prerender: true })` pages → static files.\n\n## Where things go\n\n```\napp/            file-system routes: page.tsx, layout.tsx, [param]/\napp/_client.*   the client entry — its presence turns on islands hydration\ncontent/        markdown collections (frozen at build, verbatim .md projections)\njune.config.ts  site metadata, agent-surface switches, resources\n```\n",
    "html": "<h2>Install</h2>\n<pre><code class=\"language-bash\">npm create june my-app          # canonical (package: create-june)\ncd my-app &amp;&amp; npm run dev\n</code></pre>\n<p>The framework packages are <code>@junejs/core</code> (the contract layer: <code>route()</code>,\n<code>defineAction()</code>, islands) and <code>@junejs/cli</code> (the <code>june</code> command) — NOT <code>june</code>\n(an unrelated npm package), not <code>junejs</code>, and not <code>@june/*</code> (that scope isn&#39;t\nours).</p>\n<h2>The loop</h2>\n<pre><code class=\"language-bash\">june dev          # dev server (Bun/Node host)\njune build        # Workers bundle: dist/worker.js + prerendered assets\njune deploy       # build + wrangler upload (--dry-run validates only)\njune gen          # freeze content/**/*.md → app/_content.ts\njune info         # show routes + the agent surface\n</code></pre>\n<p><code>june build</code> freezes what the dev server discovers at request time: routes →\na static manifest, <code>june.config.ts</code> → literals, <code>content/**/*.md</code> →\n<code>app/_content.ts</code>, <code>route({ prerender: true })</code> pages → static files.</p>\n<h2>Where things go</h2>\n<pre><code>app/            file-system routes: page.tsx, layout.tsx, [param]/\napp/_client.*   the client entry — its presence turns on islands hydration\ncontent/        markdown collections (frozen at build, verbatim .md projections)\njune.config.ts  site metadata, agent-surface switches, resources\n</code></pre>\n"
  },
  {
    "slug": "02-dual-audience",
    "data": {
      "title": "Dual audience",
      "description": "One route() definition serves humans (HTML) and agents (JSON, markdown, agent manifest, MCP).",
      "date": "2026-06-12"
    },
    "body": "## One definition, five surfaces\n\n```tsx\nexport default route({\n  load: (ctx) => fetchPost(ctx.params.slug),\n  view: (post) => <article>…</article>,   // GET /posts/x        → streamed HTML\n  json: (post) => post,                   // GET /posts/x.json   → structured data\n  md:   (post) => post.original,          // GET /posts/x.md     → AUTHORED markdown\n  agent: (post) => manifest.resource(…),  // GET /posts/x.agent  → capability manifest\n  metadata: (post) => ({ title: post.title }),\n});\n```\n\nThe `.md` projection serves the file you wrote, byte-for-byte — frontmatter\nincluded. Most frameworks reconstruct markdown from rendered HTML; June serves\nthe source, so there is nothing to drift.\n\n## What agents discover automatically\n\n- `/llms.txt` — route map + the framework's canonical names\n- `/sitemap.xml`, `/robots.txt`, `/.well-known/api-catalog`\n- `/mcp` — your `defineAction()`s as MCP tools: one definition is a UI server\n  action AND a tool AND a manifest entry\n\n## Actions are one gate\n\n`defineAction({ id, description, input, run })` — `run(input, ctx)` receives\nthe same context (user, session, resources) whether the caller is your UI or\nan agent at `/mcp`. One authorization model, two kinds of callers.\n\n## The posture\n\nHuman-intent optimizations (Speculation-Rules prerender, View Transitions)\napply to the HTML surface only; agent surfaces stay deliberately plain.\nMachines don't need view transitions.\n",
    "original": "---\ntitle: Dual audience\ndescription: One route() definition serves humans (HTML) and agents (JSON, markdown, agent manifest, MCP).\ndate: 2026-06-12\n---\n## One definition, five surfaces\n\n```tsx\nexport default route({\n  load: (ctx) => fetchPost(ctx.params.slug),\n  view: (post) => <article>…</article>,   // GET /posts/x        → streamed HTML\n  json: (post) => post,                   // GET /posts/x.json   → structured data\n  md:   (post) => post.original,          // GET /posts/x.md     → AUTHORED markdown\n  agent: (post) => manifest.resource(…),  // GET /posts/x.agent  → capability manifest\n  metadata: (post) => ({ title: post.title }),\n});\n```\n\nThe `.md` projection serves the file you wrote, byte-for-byte — frontmatter\nincluded. Most frameworks reconstruct markdown from rendered HTML; June serves\nthe source, so there is nothing to drift.\n\n## What agents discover automatically\n\n- `/llms.txt` — route map + the framework's canonical names\n- `/sitemap.xml`, `/robots.txt`, `/.well-known/api-catalog`\n- `/mcp` — your `defineAction()`s as MCP tools: one definition is a UI server\n  action AND a tool AND a manifest entry\n\n## Actions are one gate\n\n`defineAction({ id, description, input, run })` — `run(input, ctx)` receives\nthe same context (user, session, resources) whether the caller is your UI or\nan agent at `/mcp`. One authorization model, two kinds of callers.\n\n## The posture\n\nHuman-intent optimizations (Speculation-Rules prerender, View Transitions)\napply to the HTML surface only; agent surfaces stay deliberately plain.\nMachines don't need view transitions.\n",
    "html": "<h2>One definition, five surfaces</h2>\n<pre><code class=\"language-tsx\">export default route({\n  load: (ctx) =&gt; fetchPost(ctx.params.slug),\n  view: (post) =&gt; &lt;article&gt;…&lt;/article&gt;,   // GET /posts/x        → streamed HTML\n  json: (post) =&gt; post,                   // GET /posts/x.json   → structured data\n  md:   (post) =&gt; post.original,          // GET /posts/x.md     → AUTHORED markdown\n  agent: (post) =&gt; manifest.resource(…),  // GET /posts/x.agent  → capability manifest\n  metadata: (post) =&gt; ({ title: post.title }),\n});\n</code></pre>\n<p>The <code>.md</code> projection serves the file you wrote, byte-for-byte — frontmatter\nincluded. Most frameworks reconstruct markdown from rendered HTML; June serves\nthe source, so there is nothing to drift.</p>\n<h2>What agents discover automatically</h2>\n<ul>\n<li><code>/llms.txt</code> — route map + the framework&#39;s canonical names</li>\n<li><code>/sitemap.xml</code>, <code>/robots.txt</code>, <code>/.well-known/api-catalog</code></li>\n<li><code>/mcp</code> — your <code>defineAction()</code>s as MCP tools: one definition is a UI server\naction AND a tool AND a manifest entry</li>\n</ul>\n<h2>Actions are one gate</h2>\n<p><code>defineAction({ id, description, input, run })</code> — <code>run(input, ctx)</code> receives\nthe same context (user, session, resources) whether the caller is your UI or\nan agent at <code>/mcp</code>. One authorization model, two kinds of callers.</p>\n<h2>The posture</h2>\n<p>Human-intent optimizations (Speculation-Rules prerender, View Transitions)\napply to the HTML surface only; agent surfaces stay deliberately plain.\nMachines don&#39;t need view transitions.</p>\n"
  },
  {
    "slug": "03-build-and-deploy",
    "data": {
      "title": "Build & deploy",
      "description": "What june build generates, how prerender works, and the deploy adapters.",
      "date": "2026-06-12"
    },
    "body": "## `june build`\n\nEverything dynamic about dev gets frozen at build time:\n\n| input | output |\n| --- | --- |\n| `app/**/page.*` | static route manifest (+ nested layout chains) |\n| `june.config.ts` | inlined literals (site, speculation, agent config) |\n| `content/<dir>/*.md` | `app/_content.ts` — the manifest dev AND workerd read |\n| `route({ prerender: true })` | static html/md/json rendered THROUGH the built worker |\n| `app/_client.*` | the islands bundle served at `/client.js` |\n\nThe bundle is self-contained ESM (rolldown; conditions baked in — workerd has\nno runtime conditions). Binary assets and `build.external` packages stay\nexternal for wrangler's own rules.\n\n## Prerender\n\nOpt-in per route, static routes only. What ships is what was tested: the\nbuild renders through the bundled worker, not a parallel code path. Don't opt\nin routes whose output depends on `ctx.url.origin`.\n\n## `june deploy`\n\nThe verb is fixed; the target is an adapter — `workers` today, more later.\n`june deploy` = build → resolve wrangler config → upload → URL.\n`--dry-run` validates everything without uploading — it's also the CI test.\n",
    "original": "---\ntitle: Build & deploy\ndescription: What june build generates, how prerender works, and the deploy adapters.\ndate: 2026-06-12\n---\n## `june build`\n\nEverything dynamic about dev gets frozen at build time:\n\n| input | output |\n| --- | --- |\n| `app/**/page.*` | static route manifest (+ nested layout chains) |\n| `june.config.ts` | inlined literals (site, speculation, agent config) |\n| `content/<dir>/*.md` | `app/_content.ts` — the manifest dev AND workerd read |\n| `route({ prerender: true })` | static html/md/json rendered THROUGH the built worker |\n| `app/_client.*` | the islands bundle served at `/client.js` |\n\nThe bundle is self-contained ESM (rolldown; conditions baked in — workerd has\nno runtime conditions). Binary assets and `build.external` packages stay\nexternal for wrangler's own rules.\n\n## Prerender\n\nOpt-in per route, static routes only. What ships is what was tested: the\nbuild renders through the bundled worker, not a parallel code path. Don't opt\nin routes whose output depends on `ctx.url.origin`.\n\n## `june deploy`\n\nThe verb is fixed; the target is an adapter — `workers` today, more later.\n`june deploy` = build → resolve wrangler config → upload → URL.\n`--dry-run` validates everything without uploading — it's also the CI test.\n",
    "html": "<h2><code>june build</code></h2>\n<p>Everything dynamic about dev gets frozen at build time:</p>\n<table>\n<thead>\n<tr>\n<th>input</th>\n<th>output</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>app/**/page.*</code></td>\n<td>static route manifest (+ nested layout chains)</td>\n</tr>\n<tr>\n<td><code>june.config.ts</code></td>\n<td>inlined literals (site, speculation, agent config)</td>\n</tr>\n<tr>\n<td><code>content/&lt;dir&gt;/*.md</code></td>\n<td><code>app/_content.ts</code> — the manifest dev AND workerd read</td>\n</tr>\n<tr>\n<td><code>route({ prerender: true })</code></td>\n<td>static html/md/json rendered THROUGH the built worker</td>\n</tr>\n<tr>\n<td><code>app/_client.*</code></td>\n<td>the islands bundle served at <code>/client.js</code></td>\n</tr>\n</tbody></table>\n<p>The bundle is self-contained ESM (rolldown; conditions baked in — workerd has\nno runtime conditions). Binary assets and <code>build.external</code> packages stay\nexternal for wrangler&#39;s own rules.</p>\n<h2>Prerender</h2>\n<p>Opt-in per route, static routes only. What ships is what was tested: the\nbuild renders through the bundled worker, not a parallel code path. Don&#39;t opt\nin routes whose output depends on <code>ctx.url.origin</code>.</p>\n<h2><code>june deploy</code></h2>\n<p>The verb is fixed; the target is an adapter — <code>workers</code> today, more later.\n<code>june deploy</code> = build → resolve wrangler config → upload → URL.\n<code>--dry-run</code> validates everything without uploading — it&#39;s also the CI test.</p>\n"
  },
  {
    "slug": "features-actions",
    "data": {
      "title": "Actions: one gate for UI and agents",
      "description": "defineAction() is a server action, an MCP tool, and a manifest entry — run(input, ctx) is the single authorization gate for every caller.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "2"
    },
    "body": "## The feature\n\n```ts\nexport const createUser = defineAction({\n  id: \"createUser\",\n  description: \"Create a user\",          // description → exposed as an MCP tool\n  input: { type: \"object\", properties: { name: { type: \"string\" } }, required: [\"name\"] },\n  run: (input, ctx) => {\n    // ctx carries the principal + resources — the SAME ctx whether the caller\n    // is your React UI or an agent calling /mcp. Authorize here, once.\n    return ctx.db.insert(\"users\", input);\n  },\n});\n```\n\nOne `defineAction()` is simultaneously:\n\n- a **server action** you pass to client components as a prop,\n- an **MCP tool** at `/mcp` (auto-listed, schema included),\n- a **manifest entry** agents discover via `.agent` / `/llms.txt`.\n\nThere is no \"expose to agents\" step and no second permission system to get\nwrong: `run(input, ctx)` is the only gate, so an agent can never do anything\nyour UI's authorization wouldn't allow.\n\n## Try it on this site\n\nThis site's search is an action. Call it as an agent would:\n\n```bash\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/list\",\"params\":{}}'\n\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":2,\"method\":\"tools/call\",\"params\":{\"name\":\"search_site\",\"arguments\":{\"query\":\"cold start\"}}}'\n```\n\n## Why it matters\n\nTools are intent-shaped and policy-checked — never auto-generated CRUD. The\nagent surface is exactly as capable as you declared, and exactly as\nauthorized as your UI.\n",
    "original": "---\ntitle: \"Actions: one gate for UI and agents\"\ndescription: defineAction() is a server action, an MCP tool, and a manifest entry — run(input, ctx) is the single authorization gate for every caller.\ndate: 2026-06-12\nsection: Features\norder: \"2\"\n---\n## The feature\n\n```ts\nexport const createUser = defineAction({\n  id: \"createUser\",\n  description: \"Create a user\",          // description → exposed as an MCP tool\n  input: { type: \"object\", properties: { name: { type: \"string\" } }, required: [\"name\"] },\n  run: (input, ctx) => {\n    // ctx carries the principal + resources — the SAME ctx whether the caller\n    // is your React UI or an agent calling /mcp. Authorize here, once.\n    return ctx.db.insert(\"users\", input);\n  },\n});\n```\n\nOne `defineAction()` is simultaneously:\n\n- a **server action** you pass to client components as a prop,\n- an **MCP tool** at `/mcp` (auto-listed, schema included),\n- a **manifest entry** agents discover via `.agent` / `/llms.txt`.\n\nThere is no \"expose to agents\" step and no second permission system to get\nwrong: `run(input, ctx)` is the only gate, so an agent can never do anything\nyour UI's authorization wouldn't allow.\n\n## Try it on this site\n\nThis site's search is an action. Call it as an agent would:\n\n```bash\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/list\",\"params\":{}}'\n\ncurl -X POST https://june.build/mcp \\\n  -H 'content-type: application/json' \\\n  -d '{\"jsonrpc\":\"2.0\",\"id\":2,\"method\":\"tools/call\",\"params\":{\"name\":\"search_site\",\"arguments\":{\"query\":\"cold start\"}}}'\n```\n\n## Why it matters\n\nTools are intent-shaped and policy-checked — never auto-generated CRUD. The\nagent surface is exactly as capable as you declared, and exactly as\nauthorized as your UI.\n",
    "html": "<h2>The feature</h2>\n<pre><code class=\"language-ts\">export const createUser = defineAction({\n  id: &quot;createUser&quot;,\n  description: &quot;Create a user&quot;,          // description → exposed as an MCP tool\n  input: { type: &quot;object&quot;, properties: { name: { type: &quot;string&quot; } }, required: [&quot;name&quot;] },\n  run: (input, ctx) =&gt; {\n    // ctx carries the principal + resources — the SAME ctx whether the caller\n    // is your React UI or an agent calling /mcp. Authorize here, once.\n    return ctx.db.insert(&quot;users&quot;, input);\n  },\n});\n</code></pre>\n<p>One <code>defineAction()</code> is simultaneously:</p>\n<ul>\n<li>a <strong>server action</strong> you pass to client components as a prop,</li>\n<li>an <strong>MCP tool</strong> at <code>/mcp</code> (auto-listed, schema included),</li>\n<li>a <strong>manifest entry</strong> agents discover via <code>.agent</code> / <code>/llms.txt</code>.</li>\n</ul>\n<p>There is no &quot;expose to agents&quot; step and no second permission system to get\nwrong: <code>run(input, ctx)</code> is the only gate, so an agent can never do anything\nyour UI&#39;s authorization wouldn&#39;t allow.</p>\n<h2>Try it on this site</h2>\n<p>This site&#39;s search is an action. Call it as an agent would:</p>\n<pre><code class=\"language-bash\">curl -X POST https://june.build/mcp \\\n  -H &#39;content-type: application/json&#39; \\\n  -d &#39;{&quot;jsonrpc&quot;:&quot;2.0&quot;,&quot;id&quot;:1,&quot;method&quot;:&quot;tools/list&quot;,&quot;params&quot;:{}}&#39;\n\ncurl -X POST https://june.build/mcp \\\n  -H &#39;content-type: application/json&#39; \\\n  -d &#39;{&quot;jsonrpc&quot;:&quot;2.0&quot;,&quot;id&quot;:2,&quot;method&quot;:&quot;tools/call&quot;,&quot;params&quot;:{&quot;name&quot;:&quot;search_site&quot;,&quot;arguments&quot;:{&quot;query&quot;:&quot;cold start&quot;}}}&#39;\n</code></pre>\n<h2>Why it matters</h2>\n<p>Tools are intent-shaped and policy-checked — never auto-generated CRUD. The\nagent surface is exactly as capable as you declared, and exactly as\nauthorized as your UI.</p>\n"
  },
  {
    "slug": "features-content",
    "data": {
      "title": "Content: markdown without drift",
      "description": "content/**/*.md freezes into a typed manifest at build time, and the .md projection serves your authored file byte-for-byte.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "5"
    },
    "body": "## The feature\n\nAuthor markdown in `content/`, and `june gen` (also run by every build)\nfreezes it into a typed manifest your routes import:\n\n```\ncontent/posts/*.md  →  june gen  →  app/_content.ts  →  POSTS / DOCS\n```\n\nThe manifest carries each entry four ways: parsed frontmatter (`data`), the\nbody, rendered `html` — and `original`, the authored file verbatim. Dev and\nthe built worker read the SAME manifest, so there is no \"works locally,\ndiffers deployed\" for content.\n\n## The drift-free part\n\nThe `.md` projection serves `original` — frontmatter included, byte for byte.\nMost frameworks reconstruct markdown from rendered HTML; June serves the\nsource, so a diff against your repo is empty. Verify it for this very page:\n\n```bash\ncurl -s https://june.build/docs/features-content.md\n```\n\nThat response IS the file in our repo — the same bytes `git show` would give\nyou. Our site tests assert this with a strict equality, not a contains.\n\n## Why it matters\n\nAgents prefer markdown — it's the densest, least ambiguous surface you can\nserve them. Serving the authored source (instead of a lossy HTML→md\nreconstruction) means what an agent reads is exactly what you wrote, with\nfrontmatter intact as structured metadata.\n",
    "original": "---\ntitle: \"Content: markdown without drift\"\ndescription: content/**/*.md freezes into a typed manifest at build time, and the .md projection serves your authored file byte-for-byte.\ndate: 2026-06-12\nsection: Features\norder: \"5\"\n---\n## The feature\n\nAuthor markdown in `content/`, and `june gen` (also run by every build)\nfreezes it into a typed manifest your routes import:\n\n```\ncontent/posts/*.md  →  june gen  →  app/_content.ts  →  POSTS / DOCS\n```\n\nThe manifest carries each entry four ways: parsed frontmatter (`data`), the\nbody, rendered `html` — and `original`, the authored file verbatim. Dev and\nthe built worker read the SAME manifest, so there is no \"works locally,\ndiffers deployed\" for content.\n\n## The drift-free part\n\nThe `.md` projection serves `original` — frontmatter included, byte for byte.\nMost frameworks reconstruct markdown from rendered HTML; June serves the\nsource, so a diff against your repo is empty. Verify it for this very page:\n\n```bash\ncurl -s https://june.build/docs/features-content.md\n```\n\nThat response IS the file in our repo — the same bytes `git show` would give\nyou. Our site tests assert this with a strict equality, not a contains.\n\n## Why it matters\n\nAgents prefer markdown — it's the densest, least ambiguous surface you can\nserve them. Serving the authored source (instead of a lossy HTML→md\nreconstruction) means what an agent reads is exactly what you wrote, with\nfrontmatter intact as structured metadata.\n",
    "html": "<h2>The feature</h2>\n<p>Author markdown in <code>content/</code>, and <code>june gen</code> (also run by every build)\nfreezes it into a typed manifest your routes import:</p>\n<pre><code>content/posts/*.md  →  june gen  →  app/_content.ts  →  POSTS / DOCS\n</code></pre>\n<p>The manifest carries each entry four ways: parsed frontmatter (<code>data</code>), the\nbody, rendered <code>html</code> — and <code>original</code>, the authored file verbatim. Dev and\nthe built worker read the SAME manifest, so there is no &quot;works locally,\ndiffers deployed&quot; for content.</p>\n<h2>The drift-free part</h2>\n<p>The <code>.md</code> projection serves <code>original</code> — frontmatter included, byte for byte.\nMost frameworks reconstruct markdown from rendered HTML; June serves the\nsource, so a diff against your repo is empty. Verify it for this very page:</p>\n<pre><code class=\"language-bash\">curl -s https://june.build/docs/features-content.md\n</code></pre>\n<p>That response IS the file in our repo — the same bytes <code>git show</code> would give\nyou. Our site tests assert this with a strict equality, not a contains.</p>\n<h2>Why it matters</h2>\n<p>Agents prefer markdown — it&#39;s the densest, least ambiguous surface you can\nserve them. Serving the authored source (instead of a lossy HTML→md\nreconstruction) means what an agent reads is exactly what you wrote, with\nfrontmatter intact as structured metadata.</p>\n"
  },
  {
    "slug": "features-data",
    "data": {
      "title": "Data: resources + cache magic",
      "description": "Declare db/kv/blob once; a write auto-invalidates cached reads and N component reads auto-batch — zero manual revalidate().",
      "date": "2026-06-12",
      "section": "Features",
      "order": "4"
    },
    "body": "## The feature\n\nData is declared, not wired. A resource gets a zero-config local default in\n`june dev` and a deploy adapter on Workers:\n\n```ts\n// june.config.ts — declaring a resource enables it; omit it and it doesn't exist.\nexport default defineJune({\n  resources: {\n    db: d1(\"DB\"),        // dev: embedded SQLite · deploy: D1\n    blob: r2(\"UPLOADS\"), // dev: ./.june/blob   · deploy: R2\n  },\n});\n```\n\n```ts\nroute({ load: async (ctx) => ({ posts: await ctx.db.query(\"select * from posts\") }) });\n```\n\n## The magic\n\nReads and writes through Juno (the default data layer) emit a public trace\ncontract — `recordTableRead` / `recordTableWrite`. That is all the framework\nneeds to:\n\n- **auto-tag** every `cache()` entry by the tables it read,\n- **auto-invalidate** those entries when an action writes the table —\n  no manual `revalidate()`, ever,\n- **auto-batch** N component reads of the same query into one.\n\n```ts\nconst listUsers = cache(() => db.table(\"users\").all());   // tagged [table:users]\nawait invokeAction(\"createUser\", { name: \"Ada\" });        // writes users →\nawait listUsers();                                        // cache MISS — fresh data\n```\n\nThe magic is a property of the *trace contract*, not of Juno: bring Prisma or\nDrizzle untouched (Tier 1), run them over `ctx.db` to share config (Tier 2),\nor add a thin shim that names the table and get the same magic (Tier 3).\n\n## Why it matters\n\nStale-cache bugs are the classic failure mode of hand-wired `revalidate()`\ncalls — and agents writing code forget them exactly as often as humans do.\nMaking invalidation a consequence of the write removes the class of bug.\n",
    "original": "---\ntitle: \"Data: resources + cache magic\"\ndescription: Declare db/kv/blob once; a write auto-invalidates cached reads and N component reads auto-batch — zero manual revalidate().\ndate: 2026-06-12\nsection: Features\norder: \"4\"\n---\n## The feature\n\nData is declared, not wired. A resource gets a zero-config local default in\n`june dev` and a deploy adapter on Workers:\n\n```ts\n// june.config.ts — declaring a resource enables it; omit it and it doesn't exist.\nexport default defineJune({\n  resources: {\n    db: d1(\"DB\"),        // dev: embedded SQLite · deploy: D1\n    blob: r2(\"UPLOADS\"), // dev: ./.june/blob   · deploy: R2\n  },\n});\n```\n\n```ts\nroute({ load: async (ctx) => ({ posts: await ctx.db.query(\"select * from posts\") }) });\n```\n\n## The magic\n\nReads and writes through Juno (the default data layer) emit a public trace\ncontract — `recordTableRead` / `recordTableWrite`. That is all the framework\nneeds to:\n\n- **auto-tag** every `cache()` entry by the tables it read,\n- **auto-invalidate** those entries when an action writes the table —\n  no manual `revalidate()`, ever,\n- **auto-batch** N component reads of the same query into one.\n\n```ts\nconst listUsers = cache(() => db.table(\"users\").all());   // tagged [table:users]\nawait invokeAction(\"createUser\", { name: \"Ada\" });        // writes users →\nawait listUsers();                                        // cache MISS — fresh data\n```\n\nThe magic is a property of the *trace contract*, not of Juno: bring Prisma or\nDrizzle untouched (Tier 1), run them over `ctx.db` to share config (Tier 2),\nor add a thin shim that names the table and get the same magic (Tier 3).\n\n## Why it matters\n\nStale-cache bugs are the classic failure mode of hand-wired `revalidate()`\ncalls — and agents writing code forget them exactly as often as humans do.\nMaking invalidation a consequence of the write removes the class of bug.\n",
    "html": "<h2>The feature</h2>\n<p>Data is declared, not wired. A resource gets a zero-config local default in\n<code>june dev</code> and a deploy adapter on Workers:</p>\n<pre><code class=\"language-ts\">// june.config.ts — declaring a resource enables it; omit it and it doesn&#39;t exist.\nexport default defineJune({\n  resources: {\n    db: d1(&quot;DB&quot;),        // dev: embedded SQLite · deploy: D1\n    blob: r2(&quot;UPLOADS&quot;), // dev: ./.june/blob   · deploy: R2\n  },\n});\n</code></pre>\n<pre><code class=\"language-ts\">route({ load: async (ctx) =&gt; ({ posts: await ctx.db.query(&quot;select * from posts&quot;) }) });\n</code></pre>\n<h2>The magic</h2>\n<p>Reads and writes through Juno (the default data layer) emit a public trace\ncontract — <code>recordTableRead</code> / <code>recordTableWrite</code>. That is all the framework\nneeds to:</p>\n<ul>\n<li><strong>auto-tag</strong> every <code>cache()</code> entry by the tables it read,</li>\n<li><strong>auto-invalidate</strong> those entries when an action writes the table —\nno manual <code>revalidate()</code>, ever,</li>\n<li><strong>auto-batch</strong> N component reads of the same query into one.</li>\n</ul>\n<pre><code class=\"language-ts\">const listUsers = cache(() =&gt; db.table(&quot;users&quot;).all());   // tagged [table:users]\nawait invokeAction(&quot;createUser&quot;, { name: &quot;Ada&quot; });        // writes users →\nawait listUsers();                                        // cache MISS — fresh data\n</code></pre>\n<p>The magic is a property of the <em>trace contract</em>, not of Juno: bring Prisma or\nDrizzle untouched (Tier 1), run them over <code>ctx.db</code> to share config (Tier 2),\nor add a thin shim that names the table and get the same magic (Tier 3).</p>\n<h2>Why it matters</h2>\n<p>Stale-cache bugs are the classic failure mode of hand-wired <code>revalidate()</code>\ncalls — and agents writing code forget them exactly as often as humans do.\nMaking invalidation a consequence of the write removes the class of bug.</p>\n"
  },
  {
    "slug": "features-islands",
    "data": {
      "title": "Islands: zero client JS by default",
      "description": "Pages ship no JavaScript unless a subtree opts in — one <Island> hydrates against an explicit registry while the rest stays server-rendered HTML.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "3"
    },
    "body": "## The feature\n\nJune pages are server-rendered and ship **zero client JavaScript** — this\nsite's pages, including the one you're reading, send none. Interactivity is\nopt-in per subtree:\n\n```tsx\n// app/page.tsx — server component\nimport { Island } from \"@junejs/core/islands\";\n\nexport default route({\n  view: () => (\n    <main>\n      <h1>Mostly static</h1>\n      <Island name=\"Counter\" props={{ start: 0 }} />  {/* the ONE live subtree */}\n    </main>\n  ),\n});\n```\n\n```ts\n// app/_client.ts — the explicit registry; its presence is what enables /client.js\nimport { hydrateIslands } from \"@junejs/core/islands-client\";\nimport { Counter } from \"./Counter\";\n\nhydrateIslands({ Counter });\n```\n\n`<Island>` server-renders a marker carrying the registry name + JSON props;\n`hydrateIslands()` scans the page and `hydrateRoot()`s each marker. No\n`app/_client.*` file → no bundle, no script tag, output unchanged.\n\n## Try it\n\n```bash\nnpm create june my-app    # the starter ships a working Counter island\n```\n\nThe starter's counter — and the framework's own e2e test — run the REAL\nproduction bundle a `june build` ships against the built worker's SSR markup:\nwhat's tested is what deploys.\n\n## Why it matters\n\nHover-prerendering (Speculation Rules) and View Transitions already make\nnavigation feel instant with no client code. Islands keep the budget at\nzero until a specific subtree earns it.\n",
    "original": "---\ntitle: \"Islands: zero client JS by default\"\ndescription: Pages ship no JavaScript unless a subtree opts in — one <Island> hydrates against an explicit registry while the rest stays server-rendered HTML.\ndate: 2026-06-12\nsection: Features\norder: \"3\"\n---\n## The feature\n\nJune pages are server-rendered and ship **zero client JavaScript** — this\nsite's pages, including the one you're reading, send none. Interactivity is\nopt-in per subtree:\n\n```tsx\n// app/page.tsx — server component\nimport { Island } from \"@junejs/core/islands\";\n\nexport default route({\n  view: () => (\n    <main>\n      <h1>Mostly static</h1>\n      <Island name=\"Counter\" props={{ start: 0 }} />  {/* the ONE live subtree */}\n    </main>\n  ),\n});\n```\n\n```ts\n// app/_client.ts — the explicit registry; its presence is what enables /client.js\nimport { hydrateIslands } from \"@junejs/core/islands-client\";\nimport { Counter } from \"./Counter\";\n\nhydrateIslands({ Counter });\n```\n\n`<Island>` server-renders a marker carrying the registry name + JSON props;\n`hydrateIslands()` scans the page and `hydrateRoot()`s each marker. No\n`app/_client.*` file → no bundle, no script tag, output unchanged.\n\n## Try it\n\n```bash\nnpm create june my-app    # the starter ships a working Counter island\n```\n\nThe starter's counter — and the framework's own e2e test — run the REAL\nproduction bundle a `june build` ships against the built worker's SSR markup:\nwhat's tested is what deploys.\n\n## Why it matters\n\nHover-prerendering (Speculation Rules) and View Transitions already make\nnavigation feel instant with no client code. Islands keep the budget at\nzero until a specific subtree earns it.\n",
    "html": "<h2>The feature</h2>\n<p>June pages are server-rendered and ship <strong>zero client JavaScript</strong> — this\nsite&#39;s pages, including the one you&#39;re reading, send none. Interactivity is\nopt-in per subtree:</p>\n<pre><code class=\"language-tsx\">// app/page.tsx — server component\nimport { Island } from &quot;@junejs/core/islands&quot;;\n\nexport default route({\n  view: () =&gt; (\n    &lt;main&gt;\n      &lt;h1&gt;Mostly static&lt;/h1&gt;\n      &lt;Island name=&quot;Counter&quot; props={{ start: 0 }} /&gt;  {/* the ONE live subtree */}\n    &lt;/main&gt;\n  ),\n});\n</code></pre>\n<pre><code class=\"language-ts\">// app/_client.ts — the explicit registry; its presence is what enables /client.js\nimport { hydrateIslands } from &quot;@junejs/core/islands-client&quot;;\nimport { Counter } from &quot;./Counter&quot;;\n\nhydrateIslands({ Counter });\n</code></pre>\n<p><code>&lt;Island&gt;</code> server-renders a marker carrying the registry name + JSON props;\n<code>hydrateIslands()</code> scans the page and <code>hydrateRoot()</code>s each marker. No\n<code>app/_client.*</code> file → no bundle, no script tag, output unchanged.</p>\n<h2>Try it</h2>\n<pre><code class=\"language-bash\">npm create june my-app    # the starter ships a working Counter island\n</code></pre>\n<p>The starter&#39;s counter — and the framework&#39;s own e2e test — run the REAL\nproduction bundle a <code>june build</code> ships against the built worker&#39;s SSR markup:\nwhat&#39;s tested is what deploys.</p>\n<h2>Why it matters</h2>\n<p>Hover-prerendering (Speculation Rules) and View Transitions already make\nnavigation feel instant with no client code. Islands keep the budget at\nzero until a specific subtree earns it.</p>\n"
  },
  {
    "slug": "features-projections",
    "data": {
      "title": "Projections: one route, five surfaces",
      "description": "Every route() projects HTML, JSON, markdown, and an agent manifest from one definition — nothing drifts because nothing is duplicated.",
      "date": "2026-06-12",
      "section": "Features",
      "order": "1"
    },
    "body": "## The feature\n\nA June route is not a page that *also* has an API — it is one definition that\n**projects** onto whatever surface the caller asks for:\n\n```tsx\nexport default route({\n  load: (ctx) => fetchPost(ctx.params.slug),\n  view: (post) => <article>…</article>,   // GET /posts/x        → streamed HTML\n  json: (post) => post,                   // GET /posts/x.json   → structured data\n  md:   (post) => post.original,          // GET /posts/x.md     → authored markdown\n  agent: (post) => manifest.resource(…),  // GET /posts/x.agent  → capability manifest\n});\n```\n\n`load()` runs once; each projection renders the same data. There is no second\n\"API route\" to keep in sync, no serializer drifting from the page.\n\n## Try it on this site\n\nEvery page you're reading works this way. From your terminal:\n\n```bash\ncurl https://june.build/why.md          # this page's authored markdown\ncurl https://june.build/benchmarks.json # the benchmark table as data\ncurl https://june.build/docs.json       # the docs index, structured\ncurl https://june.build/llms.txt        # the route map agents start from\n```\n\nThe HTML you're looking at and the markdown an agent fetches come from the\nsame `route()` — same `load()`, same deploy.\n\n## Why it matters\n\nHalf your traffic is becoming agents. A framework that only renders pixels\nmakes you hand-build (and hand-maintain) the machine surface. Projections\nmake it a property of the route, not a second codebase.\n",
    "original": "---\ntitle: \"Projections: one route, five surfaces\"\ndescription: Every route() projects HTML, JSON, markdown, and an agent manifest from one definition — nothing drifts because nothing is duplicated.\ndate: 2026-06-12\nsection: Features\norder: \"1\"\n---\n## The feature\n\nA June route is not a page that *also* has an API — it is one definition that\n**projects** onto whatever surface the caller asks for:\n\n```tsx\nexport default route({\n  load: (ctx) => fetchPost(ctx.params.slug),\n  view: (post) => <article>…</article>,   // GET /posts/x        → streamed HTML\n  json: (post) => post,                   // GET /posts/x.json   → structured data\n  md:   (post) => post.original,          // GET /posts/x.md     → authored markdown\n  agent: (post) => manifest.resource(…),  // GET /posts/x.agent  → capability manifest\n});\n```\n\n`load()` runs once; each projection renders the same data. There is no second\n\"API route\" to keep in sync, no serializer drifting from the page.\n\n## Try it on this site\n\nEvery page you're reading works this way. From your terminal:\n\n```bash\ncurl https://june.build/why.md          # this page's authored markdown\ncurl https://june.build/benchmarks.json # the benchmark table as data\ncurl https://june.build/docs.json       # the docs index, structured\ncurl https://june.build/llms.txt        # the route map agents start from\n```\n\nThe HTML you're looking at and the markdown an agent fetches come from the\nsame `route()` — same `load()`, same deploy.\n\n## Why it matters\n\nHalf your traffic is becoming agents. A framework that only renders pixels\nmakes you hand-build (and hand-maintain) the machine surface. Projections\nmake it a property of the route, not a second codebase.\n",
    "html": "<h2>The feature</h2>\n<p>A June route is not a page that <em>also</em> has an API — it is one definition that\n<strong>projects</strong> onto whatever surface the caller asks for:</p>\n<pre><code class=\"language-tsx\">export default route({\n  load: (ctx) =&gt; fetchPost(ctx.params.slug),\n  view: (post) =&gt; &lt;article&gt;…&lt;/article&gt;,   // GET /posts/x        → streamed HTML\n  json: (post) =&gt; post,                   // GET /posts/x.json   → structured data\n  md:   (post) =&gt; post.original,          // GET /posts/x.md     → authored markdown\n  agent: (post) =&gt; manifest.resource(…),  // GET /posts/x.agent  → capability manifest\n});\n</code></pre>\n<p><code>load()</code> runs once; each projection renders the same data. There is no second\n&quot;API route&quot; to keep in sync, no serializer drifting from the page.</p>\n<h2>Try it on this site</h2>\n<p>Every page you&#39;re reading works this way. From your terminal:</p>\n<pre><code class=\"language-bash\">curl https://june.build/why.md          # this page&#39;s authored markdown\ncurl https://june.build/benchmarks.json # the benchmark table as data\ncurl https://june.build/docs.json       # the docs index, structured\ncurl https://june.build/llms.txt        # the route map agents start from\n</code></pre>\n<p>The HTML you&#39;re looking at and the markdown an agent fetches come from the\nsame <code>route()</code> — same <code>load()</code>, same deploy.</p>\n<h2>Why it matters</h2>\n<p>Half your traffic is becoming agents. A framework that only renders pixels\nmakes you hand-build (and hand-maintain) the machine surface. Projections\nmake it a property of the route, not a second codebase.</p>\n"
  }
];
export const doc = (slug: string): ContentEntry | null => DOCS.find((p) => p.slug === slug) ?? null;
