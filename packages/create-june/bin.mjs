#!/usr/bin/env node
// create-june — the scaffolder. Placeholder until Phase 2 lands the dev server
// and there is a real app shape to copy. The PoC's scaffolder (copy template/,
// replace __APP_NAME__, non-interactive for CI) is the reference to port here.
console.error(
  "create-june: not implemented yet — the template lands with the Phase 2 dev server.",
);
process.exit(1);
