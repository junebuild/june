# examples/basic

Reserved for the Phase 2 fixture app — the golden-contract fixture whose dev
server and built worker must produce byte-equivalent surfaces (rebuild-plan
Phase 3). Empty until the host seam + dev server land.
